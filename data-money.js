// 金と在庫の値（2026-09-23・EX-144）。
//
// ★★ **すべて仮置き。** 出典は Notion「設計：金と在庫と買い出しクエスト」の
//   「仮の値（2026-09-23・自走用）」節。★ **値の調整は制作の最後に作者が行う**（同ページ）。
//   ⚠️ ここを詰めるために作業を止めない。直すときはこのファイルだけを直す（ロジックに値を書かない）。
//
// 通貨の名前は未定。数値だけで扱う（同ページ）。
window.masterMoneyRules = {
  // はじめからある（短の遠征費10回ぶん）
  initialMoney: 300,

  // 遠征費＝所要時間の帯（近／短／中／長）で決まる固定額。★ プレイヤーは選べない
  feeByBandLabel: { 近: 10, 短: 30, 中: 60, 長: 100 },

  // 報酬＝遠征費 × 倍率。結末の段（full／partial／fail）は依頼データの outcomes から引く
  //   ★ 赤字になるのは未達だけ。「撤退は荷物を置いてくること」が、そのまま遠征費を置いてくることになる
  rewardMultiplierByTier: { full: 2, partial: 1.2, fail: 0 },

  // 借入額＝赤字額 ＋ これ（★ 細則1「借りた直後の所持金が正」を常に満たす）
  borrowMargin: 150,
  // 借金はゲーム全体で2回まで／同時に抱えられるのは1つ
  maxBorrowCount: 2,

  // 支給品の価格（一律。品ごとの差は後で）
  itemPrice: 20,
  // 在庫の上限（★ 上限を上げるクエストは本編で追加する）
  stockLimit: 5,

  // 買い出しの値引き：パーティで一番「交渉」の高い者の値に応じて、最大2割引き
  //   ★ 交渉が育つほど安くなるが、半額のような極端にはならない
  shoppingDiscountMax: 0.2,

  // ★★ 文面はすべて仮（2026-09-23・EX-144）。**チャット側が書き直す待ち**（EX-106 の仮置きと同じ扱い）。
  //   ⚠️ 「借金イベントの中身」は設計ページで**未確定**。ここにあるのは、動かすための最小の言葉だけ。
  //   ★ 2回目の借金の言葉だけは設計ページに趣旨がある（「赤字で、次はなさそう」）。
  texts: {
    // 受付嬢の声（1回目の借入）
    borrowFirst: "ギルドの財布が、底をつきました。ひとまず、ギルドの名前で借りておきますね。返すのは、これからの報酬からで大丈夫です。",
    // 受付嬢の声（2回目の借入＝受付嬢との会話イベント）
    borrowSecond: "……また、赤字です。今回も借りておきました。でも、次はなさそうです。",
    // 運営不能（★ 記録の終わり方の一つ。「負け」ではない。ギルドは畳まれた）。記録係の地の文
    gameOverTitle: "ギルドは畳まれた",
    gameOverBody: "帳簿は赤字のまま閉じられた。記録は、ここで途切れる。",
    // 運営不能の間の受付嬢（★ 掲示板へ促さない）
    gameOverReception: "……帳簿は、ここまでです。"
  }
};
