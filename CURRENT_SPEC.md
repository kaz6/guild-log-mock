# CURRENT_SPEC.md

## 遠征ギルドログ 現在仕様

このプロジェクトは、HTML / CSS / JavaScript のローカルMockです。
プレイヤーはギルドの記録係として、冒険者を依頼へ送り出し、帰還後の報告書や記録を読むことを中心に遊びます。

---

## 基本ループ

1. 依頼掲示板を見る（時間帯・天候を確認する）
2. 依頼を選ぶ
3. 冒険者を選ぶ
4. 冒険者ごとに支給品を選ぶ（最大2個）
5. 遠征または作業へ送り出す
6. 報告書を読む
7. 必要に応じて報告メモやいきもの図鑑を確認する

---

## 現在の主な画面

- ギルド（ホーム）
- 依頼掲示板
- 冒険者名簿
- 報告メモ
- いきもの図鑑

---

## 依頼

現在実装済みの依頼：

| 依頼名 | 分類 |
|---|---|
| 届けられなかった手紙 | 探索 |
| 森の薬草採集 | 探索 |
| 古い道標の確認 | 探索 |
| 結婚式の手伝い | 生活 |
| 廃屋の片付け | 生活 |

今後追加予定：戦闘、調査、配達、護衛、救助、修繕 など

---

## 冒険者

現在登録済みの冒険者：

| 名前 | 通称 | 職業 | 性格 | 出身 |
|---|---|---|---|---|
| ミナ・レイフォード | ミナ | 斥候 | 慎重 | 郵便配達人 |
| ガッド・オルベイン | 鉄鍋 | 戦士 | 豪胆 | 宿場の用心棒 |
| エルネ・シェルカ | ─ | 薬草師 | 世話焼き | 村の調合係 |

各冒険者には `memory / caution / courage / kindness / curiosity` のパラメータがあり、ログ生成時の個性に影響する。

---

## 報告書

報告書に含まれる情報：

- 依頼名・地域・分類
- 編成（冒険者名一覧）
- 冒険者ごとの支給品
- 出発時の時間帯・天候
- 作業結果 / 結果
- 遠征ログ（天候行動 → 道中イベント → 個人行動 → 支給品行動 → 掛け合い → 結果）
- 観察記録票がある場合、その内容

---

## ログ生成の方針

**パーティ人数による分岐**

- 1人編成：「一行」「仲間」「互いに」は使わない。行動・判断・報告書文体中心。
- 複数人編成：冒険者同士の短い掛け合いや役割分担ログが出ることがある（最大2行程度）。

**支給品ログの依頼相性制御**

支給品を持っていても、依頼内容に合わない場合はログに出さない。`canUseItemInQuest()` で依頼ごとの許可アイテムを管理している。

| 依頼 | 出てよい支給品ログ |
|---|---|
| 結婚式の手伝い | 携帯鍋、包帯 |
| 廃屋の片付け | 笛、包帯、油紙の封筒入れ |
| 届けられなかった手紙 | 古地図、油紙の封筒入れ |
| 森の薬草採集 / 古い道標の確認 | 笛、古地図、包帯、観察記録票、携帯鍋 |

**天候とログ**

- 報告書の本文ログは `expedition.departWeather`（出発時天候）を参照する
- 油紙の封筒入れの雨対策ログは「小雨」時のみ出る
- 天候キーは `["晴れ", "曇り", "小雨", "風が強い", "霧"]` の5種類

---

## 支給品

- 冒険者1人につき最大2スロット
- 支給品ログの主語は原則として所持者
- 所持者以外に得意な冒険者がいれば、補助・助言として登場させる
- 所持者でない冒険者が勝手に使ったように見せない

---

## 時間帯・天候

- 依頼掲示板の上部に現在の時間帯・天候を表示している
- 時間帯は `Date().getHours()` から判定（朝 / 昼 / 夕方 / 夜）
- 天候は `totalExpeditions` と `archiveSeed` から決定論的に生成（5種類）
- `startExpedition()` 実行時に時間帯・天候を `expedition` に保存し、報告書に出発時情報として表示する
- render のたびに天候が変わらないよう、`totalExpeditions` は出発確定後にインクリメントする

---

## 観察記録票

- 観察対象がある依頼でのみ意味を持つ
- 観察記録票を持っている冒険者だけが観察記録を書く
- 観察記録の内容は「報告メモ」に蓄積される

---

## 報告メモ

- 観察記録票で持ち帰った一次記録を時系列で読み返す場所
- いきもの図鑑を書くための素材置き場
- プレイヤーが編集する完成記録ではない

---

## いきもの図鑑

- プレイヤーが自分で整理・編集する完成記録
- 現在の入力項目：対象名 / 分類 / 遭遇地域 / 外見・特徴 / 備考（旧フィールドは備考に統合済み）
- 右寄せドロワーUIで編集する

---

## データ保存

- `localStorage` に保存
- `quests` / `items` はマスターデータを正として使う（保存データで上書きしない）
- `adventurers` はマスターデータを基本にし、`favorite / memo / history / status` だけ保存データを反映する
- `selectedAdventurerItems` は旧形式（`advId: "itemId"`）から新形式（`advId: ["id1", "id2"]`）へ自動変換する

---

## 既知の課題・注意点

- 戦闘ログは未実装
- 天気予報（将来の天候見通し）は未実装
- NPCフラグ管理・本格会話システムは未実装
- 依頼人・受取人・町人を明示的に管理する仕組みはない

---

## Cursor作業ルール

- 1回1機能
- 最小差分
- 動いている状態を壊さない
- 広範囲の探索や大規模な書き換えはしない
- 関係する箇所だけを修正する
- Shell実行は不要
- 新しいライブラリは追加しない
- React化やフレームワーク導入はしない
- HTML / CSS / JavaScript のローカルMockとして進める
- 修正後は、変更したファイル・変更した関数/変数・確認手順を提示する

---

## 今はやらないこと

- 大規模リファクタリング
- React化 / フレームワーク導入
- DB導入 / ログイン機能 / オンライン同期
- Unity移行
- 画像やBGMの本格導入
- 複数画面の一括作り直し
