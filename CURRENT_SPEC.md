# CURRENT_SPEC.md

## 遠征ギルドログ 現在仕様

このプロジェクトは、HTML / CSS / JavaScript のローカルMockです。プレイヤーはギルドの記録係として、冒険者を依頼へ送り出し、帰還後の報告書ログ・報告メモ・いきもの図鑑を読むことを中心に遊びます。

現在は完成度を磨き込む段階ではなく、面白そうな機能を小さく入れて手触りを確認する段階です。

- Mock version: `v0.1.2`
- 保存キー: `expeditionGuildLogMockV011`
- 基本方針: 1回1機能 / 最小差分 / 動いたらcommit

---

## 基本ループ

1. 依頼掲示板を見る
2. 時間帯・天候を確認する
3. 依頼を選ぶ
4. 冒険者を1〜4人選ぶ
5. 冒険者ごとに支給品を選ぶ
6. 遠征へ送り出す
7. 帰還後の報告書を読む
8. 今回のハイライト・報告メモ・いきもの図鑑を確認する

---

## 現在の主な画面

- ギルド / ホーム
- 依頼掲示板
- 冒険者名簿
- 報告書 / 報告履歴
- 報告メモ
- いきもの図鑑

---

## 現在の依頼一覧

| id | 依頼名 | 分類 | 危険度 | 地域 | 緊張度 | ログの厚み |
|---|---|---|---|---|---|---|
| `quest_herb` | 森の薬草採集 | 探索 | 低 | 薄明の森 | 未設定 | 薄い（汎用フロー） |
| `quest_signpost` | 古い道標の確認 | 探索 | 低 | 古い街道 | 未設定 | 薄い（汎用フロー） |
| `quest_letter` | 届けられなかった手紙 | 生活 | 低 | 雨待ちの街道 | 未設定 | 薄い（汎用フロー） |
| `quest_wedding_support` | 結婚式の手伝い | 生活 | 低 | 町の小さな祝宴会場 | 低(15) | 中（生活専用フロー） |
| `quest_old_house_cleanup` | 廃屋の片付け | 生活 | 低 | 町外れの古い家屋 | 未設定 | 中（生活専用フロー） |
| `quest_field_mystery` | 畑を荒らす「なにか」の追い払い | 戦闘 | 低 | 村はずれの畑 | 中(45) | 厚い（専用関数） |
| `quest_barn_bite` | 納屋に巣食う噛みつく「なにか」の討伐 | 戦闘 | 中 | 古い納屋 | 高(72) | 厚い（専用関数） |
| `quest_old_bridge_repair` | 古い小橋の応急修理 | 保全 | 低 | 村はずれの小川 | 低〜中(30) | 厚い（専用関数） |
| `quest_herb_delivery` | 薬草包みの納品 | 輸送 | 低 | 雨待ちの街道 | 低〜中(28) | 厚い（専用関数） |
| `quest_missing_herbalist` | 帰ってこない薬草採りの確認 | 救助 | 中 | 薄明の森の浅瀬 | 中〜高(58) | 厚い（専用関数） |
| `quest_evening_market_escort` | 夕市帰りの親子の付き添い | 護衛 | 低 | 夕暮れの街道 | 中(42) | 厚い（専用関数） |
| `quest_old_stele_rubbing` | 古い石碑の拓本 | 記録 | 低 | 旧街道脇の石碑 | 低〜中(34) | 厚い（専用関数） |
| `quest_lingering_light` | 夜道に残る灯りの調査 | 調査 | 低 | 村はずれの道 | 高(62) | 厚い（専用関数） |

緊張度の数値は `app.js` の `tensionBase` 実測値。「未設定」は `tensionBase` を持たない依頼（`quest_herb` / `quest_signpost` / `quest_letter` はまだ最古参の汎用フロー `questEventPools` のままで、専用ハイライトも持たない）。

現在は、生活 / 探索 / 戦闘 / 調査 / 保全 / 輸送 / 救助 / 護衛 / 記録の主要ジャンルが一通り入っています。調査・保全・輸送・救助・護衛・記録は各1ジャンル1件のみで、質は高い（全て専用生成関数）ため、増やすなら量の追加が効きます。13件中、戦闘は2件のみで、戦闘偏重ではなく非戦闘系の比重の方が高い状態を保てています。

---

## 冒険者

現在登録済みの冒険者：

| id | 名前 | 通称 | 職業 | 種族/扱い | 執着 | 備考 |
|---|---|---|---|---|---|---|
| `adv_mina` | ミナ・レイフォード | ミナ | 斥候 | 人間 | 贖罪 | 道・手紙・慎重な判断に強い |
| `adv_gadd` | ガッド・オルベイン | 鉄鍋 | 戦士 | 人間 | 収集 | 前衛・力仕事・生活感のあるログに強い |
| `adv_erne` | エルネ・シェルカ | エルネ・シェルカ | 薬草師 | 人間 | 統制 | 記録・観察・手当てに強い |
| `adv_low` | ロウ | ロウ | 見習い盾役 | 人間 | 回帰 | 退路・足場・帰還確認に強い |
| `adv_elsie` | エルシー | なし | ギルド犬 | 犬 | 散歩 | 嗅覚・警戒・追跡・帰還補助。単独出撃不可 |

各冒険者には、以下のログ生成用パラメータがあります。

- `memory`: 記憶力
- `caution`: 慎重さ
- `courage`: 胆力
- `kindness`: 面倒見
- `curiosity`: 好奇心

また、以下を持ちます。

- `traits`: 性格3つ。うち1つは `type: "flaw"` として欠点扱い
- `obsession`: 執着
- `weapon`: 固有武器
- `accessory`: アクセサリー

---

## エルシー仕様

エルシーは `species: "dog"` の特別なギルド犬です。

- `special: true` は内部判定用に残す
- UI上で「特別」バッジは強調しない
- `canSolo: false`
- エルシー単独では出撃不可
- 人間用ログ候補には混ぜない
- エルシーは犬用ログだけに出す
- 人間1人＋エルシーは、完全ソロでも完全複数人でもない中間扱い

エルシーにさせない行動：

- 記録する
- 説明する
- 声をかける
- 読む
- 修理する
- 納品書を確認する
- 拓本を取る
- 会話する

エルシーができる行動：

- 匂いを嗅ぐ
- 耳を立てる
- 吠える
- 低く唸る
- 足元を歩く
- 先に立ち止まる
- 振り返る
- そばにいる
- 伏せて待つ
- 袖口や裾をくわえる

---

## 支給品

現在の支給品：

| id | 名称 | 主な用途 |
|---|---|---|
| `item_bandage` | 包帯 | 負傷対応、仮止め、目印 |
| `item_map` | 古地図 | 街道・旧道・道標・迂回路確認 |
| `item_whistle` | 笛 | 合流、呼びかけ、撤退合図 |
| `item_pot` | 携帯鍋 | 休憩、湯、士気、生活感 |
| `item_oilcase` | 油紙の手紙入れ | 手紙・納品書・記録の雨対策 |
| `item_obs_sheet` | 観察記録票 | 観察メモ、報告メモ、図鑑素材 |
| `item_lantern` | ランタン | 夜道・暗所・足元確認 |

支給品の基本方針：

- 冒険者1人につき最大2スロット
- 支給品ログの主語は原則として所持者
- 依頼に合わない支給品ログは出さない
- `canUseItemInQuest()` で依頼ごとの許可アイテムを管理する
- 所持者でない冒険者が勝手に使ったように見せない
- 支給品の効果は、できれば判断・結果・ハイライトにつながるとよい

---

## 時間帯・天候

- 依頼掲示板の上部に現在の時間帯・天候を表示
- 時間帯は `Date().getHours()` から判定
- Mock検証用に手動選択できる
- 天候は `totalExpeditions` と `archiveSeed` から決定論的に生成
- Mock検証用に手動選択できる
- `startExpedition()` 実行時に、出発時の時間帯・天候を `expedition` に保存
- 報告書本文ログは `expedition.departWeather` を参照する
- 天候キーは `晴れ / 曇り / 小雨 / 風が強い / 霧`
- 油紙の手紙入れの雨対策ログは主に「小雨」時に活きる

---

## 緊張度

現在は依頼に `tensionBase / tensionRange` を持たせ、内部的に `tensionValue` を算出します。

- 表示上は緊張度1〜5
- `minTension / maxTension` を持つログ候補をフィルタできる
- 主な目的は、セリフやログ文が状況に合わない事故を減らすこと

目安：

| 緊張度 | 内容 |
|---|---|
| 1 | 結婚式、手伝い、平和な生活依頼 |
| 2 | 軽い不安、雨、廃屋、修繕 |
| 3 | 追い払い、護衛、探索、足跡確認 |
| 4 | 討伐、救助、夜道、怪異接触 |
| 5 | 死傷、未帰還、帰還不能リスク |

将来的には、ログ内の発見・失敗・負傷・支給品効果で上下させる。

---

## 戦闘システム（隊商護衛の掴み体験・設計確定分）

2026-07-10の設計セッションで、隊商護衛依頼を「掴み」に据えるための戦闘システムの方向性が確定しました。以下は未実装の仕様であり、**数値はすべて叩き台で後で調整する前提**です。

戦闘システムは「勝敗判定機」ではなく「段階的な結末を生成する装置」と位置づけます。出力は勝敗ではなく、緊張度5段階と対応する以下の4段階のどこに着地したかです。

- 軽：無傷で切り抜け（ロストなし）
- 中：負傷（ロストなし、傷は記録に残る）
- 重：離脱・撤退（ロストなし、布石）
- 致命：ロスト（無理をさせた時のみ）

### 撤退判断フェーズ

会敵時に第1判断、ラウンド3開始時に第2判断、それ以降は決着まで見届けます。

```
会敵（エンカウント）
  └─【第1判断】各冒険者が撤退判断
        ├─ 過半数が撤退 → 離脱処理（煙幕あり=続行 / なし=放棄で逃げ帰る）
        └─ 過半数が残る → 交戦へ
              │ ラウンド1〜2
        【第2判断】ラウンド3開始時に再度撤退判断
              ├─ 過半数が撤退 → 離脱処理（それまでの被害を反映）
              └─ 過半数が残る → 決着まで（勝利 or 敗北）
```

- 過半数の撤退判断で離脱成立（4人→3人 / 3人→2人 / 2人→2人）。
- エルシーは頭数に数えず群れの判断に従う。かつパーティ全体の撤退判断をやや早める（危険を先に察知して吠える）。
- 負傷の重さは「どこまで粘ったか」で自動的に決まる（第1で退けば無傷離脱、第2まで粘れば負傷）。

### 煙幕の二重効果

1. 各人の撤退判断に +30% バフ（退きやすくする）。
2. 撤退成立時、戦闘スキップで先へ進める（依頼続行）。ただし効かない敵もいる想定（MVPでは効く固定、敵側の煙幕耐性フラグは将来検討）。
3. 煙幕なしで撤退＝依頼放棄で逃げ帰る。報酬なしだが「被害ゼロ＋そこまでの情報」が実質報酬。

### 敗北時のエルシー有無分岐

決着まで見届けて敗北した場合：

| エルシー | 帰結 | ギルドへの伝わり方 | 次の展開 |
|---|---|---|---|
| あり | 人間は全員行方不明でもエルシーだけは必ず帰還 | エルシーが戻り状況が伝わる | 緊急捜索任務が発生 |
| なし | 全員行方不明に寄る | 帰還時刻超過→「何かあった」 | 時間経過で緊急捜索任務が発生 |

エルシーは4kgの小型犬（パピヨン）で担げないため、役割は「運ぶ」ではなく「囮になって逃げ道を作る」。囮ログ発動、自身は必ず生還します。

### 離脱適性（敗北時の重傷帰還/行方不明の振り分け）

土台は `job` で決めます（`adv.job` / `adv.personality` の既存分岐をそのまま使える想定）。

重傷で帰還できる確率（叩き台）：

| job | 帰還確率 | 根拠 |
|---|---|---|
| 斥候（ミナ） | 80% | 逃走・追跡のプロ |
| 見習い盾役（ロウ） | 65% | 退路・帰還が得意 |
| 薬草師（エルネ） | 60% | 自他の手当てで生存寄り |
| 戦士（ガッド） | 35% | 殿を務めて逃げ遅れる |

補正（すべて叩き台）：

- personality: 慎重 +10% / 豪胆（勇猛） −10%
- エルシー同伴：パーティ全員 +15%（囮ログ発動）
- エルシー自身：常に帰還（100%）

### 戦闘内部値（2026-07-11〜12の設計セッションで確定）

前述の未決事項（戦況値の算出式、撤退しきい値、第1/第2判断の扱い）はすべて設計判断が完了しました。数値はすべて叩き台です。

**戦況値の算出式**

- パーティの `combat`（育成stats）合算に、`weapon.power`（攻撃）/ `weapon.guard`（防御）を加算する。
- 装備選びが戦況値に直接効くことで、支給品（包帯=守り／煙幕=回避）の意味付けと噛み合う。

**撤退しきい値**

- job基礎値（上記の離脱適性と同じjob軸）＋ personality補正。
- personality補正はハードコード分岐ではなく、personality→補正値のテーブル参照方式にする（今後personalityが増える前提）。
- 補正の叩き台：慎重 +10% / 豪胆 −10% / 我慢強い −10%（豪胆と同扱い）。

**第1判断・第2判断の個人スコア（加算型）**

- 個人撤退スコア ＝ 戦況スコア（味方HP計と敵HPの比較由来）＋ 状況補正の合計。
- これを各自のしきい値と比較し、超えた者が「撤退したい」に票を入れ、過半数で離脱成立。
- 状況補正の例：煙幕所持 +30%（退きやすく）／効果的なアイテム所持（退きにくく）／敵HPが閾値（例：20%）を切ったら「ここで決める」方向の補正。
- 「効果的なアイテム」の定義は依頼データ側の新規フィールドに持たせる（既存 `canUseItemInQuest()` とは別系統）。

**個人HP**

- 冒険者は個人ごとにHPを持つ。初期値はjob別基礎値（叩き台：戦士120 / 見習い盾役110 / 斥候90 / 薬草師80）。
- エルシーはHP管理の対象外（被弾せず、常に帰還する仕様と整合）。

**敵データ**

- `data-enemies.js` を新設し、敵をマスターデータ化する。最小スキーマは `id / name / hp / threat` の4項目のみ。依頼側は enemyId で参照する。
- 既存の data-* 分離パターンの踏襲であり、いきもの図鑑（第2層）との将来連携も見込む。煙幕耐性・敵ギミックは将来の拡張枠。

**被ダメージ配分**

- 前衛集中型の重み分散。叩き台：前衛60%、残り40%を他の人間メンバーで等分。エルシーは0%。
- 前衛はjob順（戦士＞見習い盾役＞斥候＞薬草師）で決定し、同職は `courage` の高い方。
- 「殿を務めて逃げ遅れる戦士」という離脱適性の物語とHPの減り方が一致する。

**ヘイト値**

- 数値としてのヘイトシステムは今は作らない（将来拡張候補）。前衛選定ルールが実質の簡易ヘイト順であり、「前衛＝注目役」としてログに反映する。
- エルシーの「気を引く」は敗北時の囮ログの枠で表現する。

### 実装時に詰める残項目

- 戦況スコアの具体式（味方HP計と敵HPをどう1数値に落とすか）
- 各しきい値の絶対値（job基礎値の具体数字と、撤退スコアの単位合わせ）

---

## 支給品の段階開放（隊商護衛）

隊商護衛の「掴み体験」向けに、支給品の学習曲線を設計しました（未実装）。

- 隊商護衛達成までは支給品をつけられません（できることを初めは狭める）。
- 初期に選べるのは包帯（守り）と煙幕（回避）の2択のみ。この2択が戦闘3分岐（強行突破/戦線離脱回避/失敗）と直接対応し、支給品選択が戦術の意思表示になります。
- 隊商護衛達成後、その商人から支給品を変更できるよう開放されます。最初の開放候補は携帯食/携帯鍋（守り/回避/継戦維持の三竦みになる）。
- MVPの掴みでは2択のままとし、3つ目を最初から見せて選択を濁らせません。

---

## ロスト仕様（隊商護衛）

- 隊商護衛達成までロスト不可。
- 達成後は「予兆→緊急捜索→最後のチャンス」の3段階を踏んで初めてロストが発生します。
- キャラロストはエルシーを対象外とします（エルシーは常に帰還）。
- 最後のチャンスでは最善策が明示されますが、あえて不適切編成で挑んだ場合のみ即ロスト＋信頼値大幅減。依頼自体は辛勝で前進します。
- 理不尽ロストは無し、段階を踏む、無理をさせた時だけ、という原則です。

---

## 信頼値システム（構想）

受付嬢・冒険者への信頼値という軸が発生する想定です。MVPでは裏で保持するだけでも可とし、完成形の第3層（愛着の湧いた冒険者・受付嬢を守る動機）への伏線として位置づけます。詳細な保持場所・上下条件・UI露出の要否は未設計です。

---

## 報告書

報告書に含まれる情報：

- 依頼名
- 地域
- 分類
- 編成
- 支給品
- 出発時の時間帯・天候
- 緊張度
- 結果
- 遠征ログ
- 観察記録票がある場合の観察メモ
- 今回のハイライト

`report.logs` は現在、主に以下を持ちます。

- `kind`
- `text`

将来的に検討中：

- `phase`
- `importance`
- `tensionLevel`

---

## 報告書ログ順序フェーズ案

現時点では設計メモであり、まだ `phase` 実装はしない。

共通フェーズ案：

1. `departure`: 依頼内容・出発
2. `arrival`: 現地到着
3. `situation`: 状況確認・最初の違和感
4. `action`: 作業 / 移動 / 探索 / 交戦
5. `choice`: 冒険者の判断
6. `support`: 支給品・個性の反映
7. `result`: 結果
8. `return`: 帰還
9. `afterglow`: 余韻
10. `highlight`: 今回のハイライト

方針：

- 毎回すべてのフェーズを使わない
- 生活・輸送・保全では不穏さを抑える
- 救助・戦闘では結果を早く漏らさない
- 調査・記録では未解決や判読不能を自然に残す
- エルシーログは主作業の邪魔をしない位置に置く
- ハイライトは本文と矛盾させない

---

## 今回のハイライト

遠征終了時に、今回の印象的な1文を表示する機能。

方針：

- 依頼内容と結果に合う
- 本文ログと矛盾しない
- 低緊張依頼に重すぎる文を出しすぎない
- 支給品・武器・アクセサリー・天候・執着などを将来的に反映する

---

## 観察記録票 / 報告メモ / いきもの図鑑

### 観察記録票

- 観察対象がある依頼で意味を持つ
- 観察記録票を持っている冒険者だけが観察メモを書く
- 観察メモは報告メモへ蓄積される

### 報告メモ

- 観察記録票で持ち帰った一次記録を時系列で読み返す場所
- いきもの図鑑を書くための素材置き場
- プレイヤーが編集する完成記録ではない

### いきもの図鑑

- プレイヤーが自分で整理・編集する完成記録
- 現在の入力項目：対象名 / 分類 / 遭遇地域 / 外見・特徴 / 備考
- 右寄せドロワーUIで編集する

---

## データ保存

- `localStorage` に保存
- `quests` / `items` はマスターデータを正として使う
- `adventurers` はマスターデータを基本にし、`favorite / memo / history / status` など保存対象を反映する
- `selectedAdventurerItems` は旧形式（`advId: "itemId"`）から新形式（`advId: ["id1", "id2"]`）へ自動変換する
- `reports` / `reportMemos` / `beastLog` を保存する

---

## 既知の課題・注意点

- ログ量はまだ少ない
- 掛け合いログはまだ少ない
- エルシー専用ログの頻度・文脈は後で調整
- 執着ハイライトの出現確認は未完
- 報告書フェーズ構造はまだ実装しない
- 依頼チェーン / 地域進行度は未実装
- 戦闘の交戦フェーズは今後拡張したい
- 報告書の読書演出は未実装
- 天気予報 / 天候見通しは未実装

---

## Cursor / AI作業ルール

詳しいルールは `AI_GUIDELINES.md` を参照。

要点：

- 1回1機能
- 最小差分
- 大規模リファクタリング禁止
- 既存構造をなるべく維持
- Shell実行不要
- README更新不要
- 関係ないUI変更禁止
- 変更したファイルと確認手順を必ず提示
- 遠征開始から帰還結果表示までの基本導線を壊さない

---

## 今はやらないこと

- Claude Code本格導入
- Unity MCP導入
- unity-cli-loop導入
- Unity移行
- TDD環境の本格整備
- クリーンアーキテクチャの厳密導入
- 大規模リファクタリング
- React化 / フレームワーク導入
- DB導入 / ログイン機能 / オンライン同期
- report.logs の大改造
- phaseプロパティの本実装
- ハイライト全文解析アルゴリズム
