# SESSION_STATE.md

> **このファイルが正本。** Notion 側の `01_projects/遠征ギルドログ/SESSION_STATE（写し・閲覧用）` は写しであり、正本ではない。
>
> （2026-07-27 決定。**向きは「誰が書くか」で決まる**＝この文書は Claude Code が生成して Claude Code が読むため repo が正本。2026-08-01 に CURRENT_SPEC も同じ側になったので、これは例外ではなく**もう一方の原則**。
> Notion 側の写しはスマホから現在地を見るための閲覧用で、session-end スキルが毎回同じ内容で上書きする。手で編集しないこと。）

セッションをまたぐ「いまどこまで来ていて、次に何をやるか」のスナップショット。
毎回このファイルを最新化してから作業を終える。確定した決定の理由は `docs/DECISION_LOG.md` を参照。

※ ★ **仕様の正本は repo の `docs/CURRENT_SPEC.md`**（2026-08-01 に向きを戻した。Notion 側は session-end が毎回生成する生成物）。設計判断の正本は Notion の `DECISION_LOG`。
※ 運用規約は Notion `02_operations/ドキュメント運用規約` を参照。

---

> 最終更新: 2026-09-21（JST・EX-140 を反映。★ **序盤の流れを 酒場 → 掃除 → 6件 に作り直した**（酒場と掃除は**一度きり**・掃除は**20秒**・廃屋は達成数2件をやめて掃除へ付け替え・6枠目は**森の薬草採集1時間**）。★ **掃除の常駐エルシーはギルドにいる回だけ**出す形にし（同時遠征で嘘になっていた）、名簿に漏れていた内部キーも日本語へ。以下は 2026-09-20 までの要約：EX-126〜139 を反映。**実機のフリーズを直し、同時遠征を入れた**：原因は TDZ で、起動の2文を app.js の末尾へ移して構造的に消した（EX-130）。最初のクエストの初回強制完遂（2026-08-06 の裁定が未実装だった）を実装し（EX-131）、所要時間は全依頼で**実時間だけ**を出す形に、危険度の呼び名も一本化（EX-134）。掲示板の**観察対象は遭遇後だけ**出す（EX-135）。診断版を消し、**配信のキャッシュ避けを自動で付けた**——版は配信元の ETag をそのまま使うので手で書き換える箇所が無い（EX-136）。★ **同時遠征**は調査（EX-137）→ 乱数シードの独立化＋基準値の取り直し → 報告書 id の衝突を塞ぐ → 実装（EX-138）の直列で通し、**本数の上限は EX-139 で撤回**した（可能だが推奨しない行動は縛らない）。前の版の要約は下の各「現在の進捗」を参照。以下は 2026-09-18 までの要約：EX-090〜125 を反映。**命名システムが一通り通った**：生態目録のキーを id にして SOAP 構造へ作り直し（S 証言＝自動／O 確かめられたこと＝12軸から選んで自由記述／A 推測／P 次に確かめたいこと）、枠は**読了ハンコ**で現れる。命名は **O の軸の種類が12軸中7つ**で解禁し、**確定印**で確定して以後は編集できない。答え合わせは実装しない裁定。★ **確定した名前の参照化を二段で通した**——第一段＝題名・掲示板・生態目録（描画のたびに引く）、第二段＝**これから生成される報告書**の本文・観察文・交戦ログ・敵の短縮名（生成の出口で置き換えて保存）。⚠ **報告書の見出しは仮称のまま**に戻した（命名前の報告書で見出しだけ確定名になると、同じ紙の上で呼び名が割れるため）。★ **あらすじの「正体はまだ不明」は命名後も残す**（名づけは同定ではない）。★ **「いきもの図鑑」を「生態目録」に改名**し（図が無いのに図鑑と名のるのは主題に反する／作中に既にある言葉／外から名付けられた名前）、**内部名とセーブのキーも `ecologyRecord` へ揃えた**（旧キーから読んで落とす。id 接頭辞 `bl_`→`er_`。`schemaVersion` は据え置き）。**冒険者は生態目録に載せない**。**納屋の敵に短縮名「なにか」**を足して交戦記録の「相手」を解消（40/40本・208行）。タイトル案「西端ギルド記録員■■■■著：未踏地域の生態目録について」を記録（■■■■ に意味を与えないことが決定そのもの）。名前の記号でカードが壊れる件を塞いだ（`escapeJsArg`）。Notion 向けの変換を1本に寄せ、デバッグ速度の記述を現行UI（等倍／60倍／3600倍）に合わせた。SESSION_STATE の写しを生成物にした。ブラウザ動線8項目を実機で通した。未達には未達のハイライトを返し、観察記録の段を「好奇心 → 記憶」の順へ。調査依頼の文面を本置き。掲示板を6枠＋クールタイムに／救助を捜索へ統合／観察記録の既定を対象の種別で引く形へ＋検査を1本／初の調査依頼「森の際の見慣れない草」を追加。包帯の重みを −10 へ／夜道 v2 を作り直し／主成長を時間帯で分ける／夜道 v1 を退避つきで削除／**体験版の範囲の材料出しと、喪失を起こさないという確定**）

## 現在の進捗（2026-07-30 まで）
- 戦闘内部値の設計論点: 全クローズ（〜07-12）
- 戦闘シーンの見せ方: 案B「交戦記録ブロック型」に決定（07-14）
- 状態語: 4段階に確定（健在/手負い/深手/戦闘不能。閾値70%/35%/HP0）（07-14）
- 戦闘シーンの見せ方: 案B破棄 → **案D「戦闘ドラマログ型」**に改訂（07-14）
- ✅ スライス1: simulateBattle が `events`（交戦イベント列）を返す（07-14 完了・検証済み）
- ✅ スライス2: 隊商護衛の交戦を events から1行1アクションのドラマログで描画（07-14 完了・検証済み）
- ✅ スライス3: 実機の文章崩れを監査 → 真因はブラウザのページ翻訳と判明。コード起因ではない。`index.html` に翻訳抑止（translate="no" ＋ notranslate メタ）を追加（07-14 完了・検証済み）
- ✅ スライス3.5: 戦闘ログの時系列崩れ修正。トドメ行を描画層で最後尾へ（エンジン・判定は不変）。`scripts/merge-remote.sh` も追加（07-14 完了・検証済み）
- ✅ スライス4: ダメージ表記を「Nダメージ」形式に、状態変化を色分け（開始・深手・戦闘不能=赤 #ff6b5b、手負い=橙 #ffa94d。文字色のみ）（07-14 完了・検証済み）
- ✅ スライス5: 回復モデル化＝supportを戦闘に初接続（回復役=support最大の生存人間、包帯1消費で15回復、エルシーは運び手。回復行・戻り行は緑 #92ba7d）（07-22 完了・検証済み）
- ✅ スライス6: 戦闘数値体系の全面再設計＝stat 255スケール移行（√型与ダメ・survival由来HP・敵HP280/240・stage=累積被ダメ基準 lwr0.18・性格値は成長対象外・旧セーブ自動移行。全時期3〜5T決着をシミュ確認）（07-23 完了・検証済み）
- ✅ スライス7: 敵threat再調整＋分散拡大（野盗40/納屋38・分散0.75-1.25。深手「時々」復活・包帯差が深手率に出る。勝率80-90%と撤退はthreatの射程外＝繰延、根本原因は味方DPS非減衰）（07-23 完了・検証済み）
- ✅ スライス8: 損耗DPS低下を導入（状態語連動：手負い80%/深手50%・深手の弱撃描写・「！」は損耗後基準。既存分布は無傷）。**「敗北＝全滅ではなく任務失敗」を確定**（全滅は構造的に起きない：前衛集中/maxRounds8/撤退R3固定）（07-24 完了・検証済み）
- ✅ スライス9: 状況トリガー臨時撤退判断（動揺スコア・半数成立・野盗HP320/T38改訂・bail「荷を置いて退く」＋捜索チェーン接続）。**繰延2項目を解消：包帯1で成功93.4%（85-95帯）・失敗は全て撤退で全滅ゼロ**（07-24 完了・検証済み）
- ✅ 総点検フェーズ1: 読み取り専用監査（数値照合約40件・却下案記録漏れ4件・デッドコード実測・未確定99件・依存16系統・正本矛盾18件→Notion監査結果に全記入）（07-24 完了）
- ✅ 総点検フェーズ2A: 軽（浅手）の復活＝lwr0.25＋深手ガードで軽27.7%・煙幕S1「失敗を部分成功に変える保険」・エルシーE2「深い失敗を浅い撤退に変える犬」（煙幕/エルシー同行の失敗率0%化・弱編成exploitは部分成功100%に格下げ・great_unhurt→great_light改名）（07-25 完了・検証済み）
- ✅ 総点検フェーズ2B: 文書の是正と記録欠落の補完（CURRENT_SPEC戦闘節を確定値で全面更新＝正本化・DECISION_LOGに却下案4件を遡及記録・コアコンセプト2/4/6/8/9/14/15/18/19/20章に改訂注記・調査タスクを完了済みへ・2Aページ裁定一本化・NEXT_TASKS冒頭注記。コード無変更）（07-25 完了）
- ✅ スライス10: 成長システム実装（18章2段成長式×生還補正×成否補正・混合判定・エルシーも種別+微成長・性格値不変検証済み。混合運用でcombat 30→120回146・150到達≈125回。タグ v0.1.3-growth）（07-25 完了・検証済み）
- ✅ 棚卸し: 体験版スコープ確定のための実装状況調査（A〜K 11項目を✅/⚠️/❌分類・行番号付きでNotionに記入。読み取り専用）（07-26 完了）
- ✅ 体験版①: 掃除とバグ修正（observations系統削除・あだ名リロードバグ修正・リロード帰還=案Bカード・schemaVersion=2導入・ps1英語化。ブラウザ18項目PASS）（07-26 完了・検証済み）
- ✅ 体験版②: 時間実装（`DEMO_DURATION_MS` 廃止・帯 近/短/中/長で全17依頼に所要時間・時間スケールは `REAL_MINUTES_PER_GAME_DAY=60` の1定数・`daysPassed` 復活・体験版モード等倍/60倍/3600倍＝比較時に倍率を掛ける方式・即帰還は加速中のみ表示。ブラウザ23項目PASS。タグ v0.1.4-time）（07-26 完了・検証済み）
- ✅ 書類: **仕様の正本を Notion の CURRENT_SPEC へ移行**（`3aaa8a5dfd45819294a9c359178bcc9a` 新設・ローカルは写し）。配下の旧仕様ページ16件を降格＋思想側5件に状態注記（★「降格宣言」と「状態注記」を別用語に分離／★ラベルで除外せず中を見る＝「方針」を名乗る2件が旧statを抱えていた）。CLAUDE.md の `00_shared`→`02_operations` 修正（07-27 完了）
- ✅ 体験版②-3: 依頼一覧の所要時間表示と依頼の表示制御（カードに所要時間の行＝地域の直下／`unlockedBy` を新設し `hidden`＝チェーン専用と並存／解放判定は成否を問わず1回行けば／解放状態は `state.reports` から導出＝schemaVersion据え置き／初期公開4件・護衛まで最短4件＝実5時間・側枝8件は進行を塞がない。ブラウザ24項目PASS）（07-27 完了・検証済み）
- ✅ ドキュメント運用: **正本を Notion に統一**（`docs/` は写し）。`CLAUDE.md` に「ドキュメントの正本」「Notion ページID」「ビルド・テスト」の3節を追加、`.claude/skills/session-end/` を設置（07-26 完了）
- ✅ 書類: **一覧の正本をコードへ寄せた**。CURRENT_SPEC から「現在の依頼一覧」表と「解放ツリー」全件表を**削除**し、規則の記述＋生成スクリプト `scripts/gen-quest-table.js` に置き換え（★一覧＝事実＝コードが正本／規則＝判断＝文書が正本。★生成物には生成日時と「正本ではない」の2行をスクリプトが自動で付ける）。**「17件へ更新」案は却下**（同じ腐り方を繰り返すため）。ゲーム本体のコードは無変更（07-27 完了・実行確認済み）
- ✅ 参照パスの追従: `CLAUDE.md` / `docs/DECISION_LOG.md` / `NEXT_TASKS.md` / 本ファイルの `00_shared` → `02_operations`（運用系の移動に追従）。`CLAUDE.md` に CURRENT_SPEC のページID を登録（07-27 完了）
- ✅ 運用: **SESSION_STATE を Notion でも読めるようにした**。★**この文書だけ repo が正本**（生成する主体と読む主体がどちらも Claude Code のため）。Notion 側に写しページを新設し、session-end スキルに⑤-b を追加して**同じ操作で両方が更新される**ようにした（写しは全置換／先頭3行はスキルが自動付与／書き込み後の読み直しを⑥に追加）。マスター（`02_operations`）を先に直してから repo へ配布（07-27 完了・検証済み）
- ✅ 受け渡し: **CLAUDE.md に「チャットへの受け渡しブロック」節を追加**（停止時・完了時の雛形をコピペできる整形済みの形で／規則3つ＝このブロックは正本ではない・「詳細はページ参照」で済ませない・はみ出し／未対応の欄を空にしない）。マスターは `02_operations/ドキュメント運用規約` の「Code → チャット の受け渡し：貼り付けブロック」節（同日新設）。★**構造（項目の数と順序）がマスター、整形が配布先の責任**という切り分けをマスターに明記。配布状況は表に変更し**遠征のみ配布済み**（07-28 完了）
- ✅ 整理: **所要時間の帯を `data-time.js` へ移設**（★データをロジック側に置かない）。`app.js` は `window.masterDurationBands` を参照するだけに。生成スクリプトの**抜き出し方式を廃止**（`app.js` の書式変更で静かに壊れる経路が消えた）。移動先は A/B で確認して**新規ファイル案（A）を採用**＝`data-quests.js` は「依頼一覧の正本」のままに保つ。**帯の値は1つも変えていない**（移設前後で帯オブジェクトと生成表の差分ゼロを機械確認・ブラウザ14項目PASS）（07-28 完了・検証済み）
- ✅ 第3段階: **負傷の持続と回復クールダウン**（★負傷の重さは stage ではなく**帰還時の個人HP率**＝既存の状態語の閾値を流用／回復時間は `data-time.js` が正本・**ゲーム内換算は仕様に書かない**／**重症だけ本人が断る・ボタンは塞がず状態バッジを出す**／負傷は戦闘の開始HPに影響させない／`schemaVersion` 3・**移行関数は配布ビルド前に必ず入れる**と発動条件を固定）。ブラウザ17項目PASS・戦闘の分布は不変（07-29 完了・検証済み）
- ✅ 測定: **軽0%の測り直し。★前提が崩れた＝軽は 0.00% ではなく 26.0〜27.2%**（現HEAD・隊商護衛・基準4人・包帯1）。監査の 0% は HEAD `49bd00a` 時点の値で**フェーズ2Aで既に解消済み**＝回帰は現存しない。裁定は **A：戦闘の数値は触らない**（離脱の長さは第3段階で決める）。★**閾値の上限は「1−深手発生率」**という構造を発見（`lightWoundRatio` を上げても軽は約41%で頭打ち）。`scripts/measure-battle.js` を新設し、`CLAUDE.md` に結末分布の必須確認を追加。**数値は1つも変えていない**（07-29 完了・検証済み）
- ✅ 測定＋H1-a: **前衛1人に負傷が偏る原因の特定**（★H1 選出が決定的・H2 配分が前衛集中の両方が成立／H3 個体が脆いは不成立＝**負傷は個体ではなくポジションで決まる**／★**4人編成という推奨の形が、後衛の頭割りでちょうど後衛を安全地帯に入れていた**）。打ち手 H1-a（前衛を毎ラウンド HP率降順で選び直す）を単独で投入＝交代 0回→3.3回/戦・負傷は4人に分散・軽は 27.7%→30.7%。★**ただし深手が 59.6%→0% になり、重症・撤退・煙幕・エルシーの分岐が連鎖して止まった＝負傷の分散と劇的な分岐が同じ仕組みに乗っていた**。しきい値70%版も試したが H1-a より悪く revert。★**しきい値を掃引して、深手と分散に両立点が存在しないことを確認**（値では解けない＝この枝を永久に閉じた）。**深手は将来クリティカルで「累積から事件へ」変えて作る**（実装は敵の種類と一緒に別タスク）。数値は1つも変えていない・`scripts/measure-front-bias.js` を新設（07-29 完了・検証済み）
- ✅ 棚卸し: **全依頼の経路を調べた**（`scripts/inventory-quest-routes.js` を新設・17件×200回）。★**戦闘を通るのも負傷が付くのも失敗が出るのも隊商護衛1件だけ**／`quest_barn_bite` は `enemyId` を持つのに `simulateBattle` 0%＝データとロジックが食い違っている／**進行不可は現HEADで再現しない**（3方針×300ターン。最初の「進行不可」はこちらの計測ミスで、3600倍だと1分依頼が実16.7msで終わり1回の再描画中に完了していた）。監査節3の未再測定2件も測り、ロスト発火は生きている・膠着度は0%でなく重6.2〜6.5%と確認。**移行方針は B（非戦闘依頼用の共通経路を別に作る）で裁定済み・実施は未着手**（07-29 完了）
- ✅ 設計確定: **クリティカル＝深手（発生率5%・倍率2倍・上限なし）**。★ **HP0 は死ではなく行方不明→捜索チェーン行きなので、「2連続で即死しない」条件はもともと満たされていた＝上限は不要**（上限を入れると自爆成長を許可してしまう）。着手前の即死確認で **ミナとエルネが条件を超過**（実測最大 116.9%/126.6%）したため一度停止し、A（1.5倍）→B（上限40%）→**C（2倍・上限なし）**と裁定が差し替わった。★**もろさの本体はクリティカルではなく「エルネの maxHp 79 が前衛シェアに対して小さい」こと**（中盤帯 threat 56 ではクリティカルなしでも 78/79 に届く）。**同日に実装まで完了**（commit da24e4e。深手 0%→53.2%・撤退・煙幕・エルシー・包帯の価値がすべて戻り、負傷も4人に分散。手当てはクリティカルの深手からも持ち直させる／0ダメージの被弾ではクリティカル判定をしない）。★**軽だけが 30.7%→19.4% に動いたので、そこだけ裁定待ちで停止**（07-29 実装・検証済み）
- ✅ 整理: **撤退と捜索チェーンの設計**をアイデアメモに独立させた（`3aca8a5dfd45818fac80dc588b9f3664`）。確定＝撤退のトリガーは接敵と重傷者の2つ・撤退は失敗しうる・エルシーがいれば必ず撤退できるが2連続が限界で「本人が嫌がる」形式・HP0 は行方不明→3日で死亡判断→遺体捜索→遺品・時間は実時間のタイマーのみ出す。未確定＝**全滅時の帰結**（ゲームオーバーか受付嬢か）と**外部機関への依頼**（強すぎて自滅回収が最適になる懸念で保留）（07-29）

- ✅ 軽の帯: **`lightWoundRatio` 0.25→0.26 で軽を 24.8% に戻した**（07-29 の判断待ちを裁定Aで解決。commit `3a2f756`）。深手 53.1%・分岐・分散・戦闘不能0件はすべて不変。★**0.26 は暫定値**で、閾値の中央値付近に軽が急変する領域があるので**敵の種類が増えたら再調整が要る**（07-30 完了・検証済み）
- ✅ 測定: **guard の式と値を測った**（`scripts/measure-guard.js` を新設。ゲーム本体は無変更）。★**式は線形で急峻ではない。0ダメ67%の原因は「絶対値の減算を、位置で4.5倍違う取り分にぶつけている」こと**（前衛と後衛で取り分が4.5倍違う）。★**小さいクリティカル（「6ダメージ！」）は guard の問題ではなかった**＝guard 0 でも後衛ヒットの66.3%が1-5帯で、原因は `frontDamageShare` と後衛の頭割り（07-30 完了）
- ✅ guard/power のスケール統一: **どちらも 0〜50 で持ち、`BATTLE_TUNING.guardScale` / `powerScale`（1/10）を掛けて使う**（commit `bceaa31` / `87614a1`）。★**実効値は据え置きで、同一シードの突き合わせ（guard 3000本・power 6000本）で結果が完全一致**＝バランスは1つも動かしていない。`schemaVersion` 据え置き（guard も power もセーブに乗っていない）。★**guard も power も成長させない**＝装備が決めるものは育つ数値と別に扱う。あわせて**どこからも読まれていない装備値2件**（武器の `control`・エルシーのハーネスの `capacity`）を列挙（07-30 完了・検証済み）
- ✅ 敵を1種追加: **一撃が重い敵（大熊 HP260/threat46）**（commit `bbee1b8`。`scripts/measure-enemy.js` を新設）。★**どの依頼にも割り当てていない**（既存の依頼の敵を差し替えると基準値が全部動くため、測定スクリプトの中だけで別条件として使う）。★**guard の設計仮説（連続攻撃に強く、一撃が重い敵には弱い）が実測で確認された**＝ロウのダメ0率 48.1%→21.5%・硬さ首位から陥落・guard が吸う割合 33.8%→28.3%。★**小さいクリティカルは敵を変えても減らない**（表示10以下が 71.5%→73.3%）＝配分側の問題であることが確定（07-30 完了・検証済み）
- ✅ **撤退判断を4段階に作り直した**（commit `291752b`。`scripts/measure-retreat.js` を新設）。★**作り直しの根拠が実測で出た**＝旧実装の撤退の **78〜84% が「敵HP20%以下」での撤退**（あと一撃で倒せるのに逃げる）／**接敵の撤退は12,000戦闘中0件で構造的に発火していなかった**（段階1は存在していなかった）／**相手の「残り」は見ていたが「強さ」を見ていなかった**（threat も最大HPも式になく、野盗と大熊の接敵スコアが同一）。裁定は 論点1=A（予想ラウンド数）／論点2=**B**（動揺のまま残し、直後に段階4を別判定で足す＝2026-07-24 の却下を覆さない）／論点3=A（医療品は残り個数）／論点4=A（4イベント）。あわせて**撤退失敗**（エルシーがいれば必ず成功）と**新結末 `avoid`**（挑まずに帰る。負傷・報酬・捜索チェーンなし）を実装。**軽25.7%／深手52.9%（N=6,000）で基準値は維持**（07-30 完了・検証済み）
- ✅ 実態調査: **同時遠征は実装されていない（上限1本）ことを実機で確認**（Playwright。遠征中は「遠征開始」が disabled）。★**エルシーの機会費用の理由を訂正**＝旧「同時遠征があるので付ければ他に付けられない」は成立せず、正しくは**パーティ枠4のうちエルシーも1枠を使う**（連れて行けば人間が1人減る）。**結論（2連続制限を入れない・疲労を持たない）は変わらず、むしろ毎回効くので強くなる**。★**タブを2つ開くと見かけ上2本走るバグを発見**（保存は後から書いた方だけが残り、先の遠征は報告書も出ずに消える／同じ冒険者を二重に派遣できる）＝別タスクに切ると裁定済み（07-30 完了）


## 現在の進捗（2026-07-31 追記）
- ✅ **撤退の歪み2件を裁定どおり修正**（commit `2525a83` → `67bf12b`）。★ **一般則「判断に入るのは『勝てるか』に関わるものだけ。『逃げやすいか』は撤退の成否にだけ効く」**＝煙幕とエルシーを判断から外し、成立を**半数以上**へ。包帯は「勝つ手段」なので判断に残す。あわせて forecast の「持たない」を**総HPと前衛の残り時間の短い方**に修正。軽 24.1〜24.9%／深手 53〜54% は維持、敵HP20%以下での撤退は 78〜84% → **0.4〜0.9%**（07-31 完了・検証済み）
- ✅ **stat 体系の棚卸し**（読み取り専用）。exploration / investigation / negotiation は**判定入力ゼロ**・死蔵6件＋`personalityTags`＋到達不能分岐を列挙。★ **255 スケールに根拠はない**（作者の「えいや」）と確定し、CURRENT_SPEC に明記（07-31 完了）
- ✅ **性格値を `stats` から `tendencies` へ分離**（1〜5・育たない・UIに数値を出さない。commit `6a1a180`）。★ **一般則「性質の違うものを同じ入れ物に入れると、片方の規約がもう片方に伝染する」**（`00_shared` の分離と同型で2回目）。**同一シード5,100件で不一致0**。`schemaVersion` は据え置きで旧セーブは育成6種だけ取り込む（07-31 完了・検証済み）
- ✅ **非戦闘依頼の経路移行**（`simulateFieldwork` を新設。commit `bb5e78d` / `889545b` / `a1b6b5c`）。★ **exploration / investigation / negotiation が初めて判定入力になった**（`GROWTH_STAT_BY_CATEGORY` から導く＝伸びる stat ＝ 使う stat）。天候と疲労を別々に持ち、撤退判断は入れず結末の格下げで表す。工程ログは**動いた工程だけ**書く。畑と納屋を `simulateBattle` へ接続し、畑用に `enemy_field_pest`（HP80/threat16）を新設。非戦闘は重症0%・負傷は4人に均等・行数は +0.1〜+0.7行。**夜道の灯りだけ据え置き**（時間帯の扱いが後回しのため）（07-31 完了・検証済み）
- ✅ **小さいダメージのクリティカルを判定しない**（`critMinHpRatio` 0.05。commit `045bc3b` / `1d8caa4`）。★ **深手 53%→18.3% は下がった値ではなく本来の値**（旧53%の **64.9%** が素ダメージ4未満＝後衛のかすり傷だった）。致命の最小表示 2→24。**深手の分散はむしろ改善**（ロウだけ半分だったのが4人ほぼ均等）。★ **軽 20〜30帯と深手 50〜70帯を、根拠が汚染された帯として取り下げ**（07-31 完了・検証済み）
- ✅ **軽の帯の両端を決めた**（commit `b53f4e9` → `bcceedc` / `d56f1fe` / `ed8c981`）。下限＝**合計被ダメが maxHp の5%未満なら無傷**／上限＝`lightWoundRatio` **0.30**。★ **同じ 5% でもクリティカルは1発あたり、負傷は累積**。★ 一度「1発ごと」で実装して同日中に直した（4%の被弾を8回受けた者まで無傷になり、無傷82.7%・回復待ち17.3%まで振れた）。**報告書と名簿の食い違いは、直すべき表示ではなく実装が間違っているという表示だった**（直したら本当の食い違いは0%）（07-31 完了・検証済み）
- ✅ **低危険帯の設計**（調査＋論点3のみ実装。commit `52b5ae9` / `bc9d62c`）。★ **納屋と野盗は同格ではなかった**（回復待ち 69.0% 対 91.3%）＝チャット側が threat だけ見た推測誤り。★ **本当の断層は畑→納屋の16倍**（4.2% → 69.0%）。畑の軽症は**敵の値では達成できない**と実証（前衛交代で1人あたりの累積が伸びない）ので議題から外した。報告書に**「防げた瞬間」**（最も効いた1〜2個だけ）と**「使わなかった支給品の状態」**（封を切られないまま戻ってきた／持たせたときだけ）を追加。★ **失敗のときだけ理由が読める状態を解消**（07-31 完了・検証済み）
- ✅ **Drive ざらい 第2束**（冒険者パラメータとログドラマ設計）（07-31）

## 現在の進捗（2026-08-01 追記）
- ✅ **EX-047：結末の候補と結末文をデータ側へ移した**（4段階。commit `5d96ddb` / `dc90308` / `3001769` / `7d36a85`）。`data-quests.js` の `outcomes`（完遂／部分／未達の3段階）と新設の `data-outcomes.js`、例外3件は `outcomeOverride`。★ **`GROWTH_TIER_BY_RESULT` が依頼データから作られるようになり、登録漏れという事故そのものが構造的に消えた**（移行時に2件の登録漏れを発見して修正＝交戦回避が完全成功扱い／確認のみが完全成功扱い）。**17依頼 × 8編成 × 6支給品 × 4時間帯 × 7天候 × 3シード＝68,544件で不一致0**（段階1〜3。段階4だけ意図的に成長の段階が変わる）。app.js は531行減り、依頼IDのハードコード6行が消えた（08-01 完了・検証済み）
- ✅ **セーブが毎回初期化される回帰を修正**（commit `90a700e`・**宣言順の1箇所だけ**）。`let state = loadState()` が `GROWTH_ELIGIBLE_STAT_KEYS` を宣言前に読んでいた（`const` の TDZ）ため、**保存データがあると必ず例外→初期化**。07-31 の `6a1a180` からの回帰で、**丸一日壊れていた**。確認手順の項目7が `file://` と HTTP の両方で通ることを確認（08-01 完了・検証済み）
- ✅ **CURRENT_SPEC の向きを repo へ戻し、Notion 側を生成物にした**（commit `8382fde` / `8085a75` / `a9654b3`）。`scripts/gen-notion-spec.js` を新設し、**宣言3行はスクリプトが自動で付ける**。session-end に ③-b と ⑤-c を追加して**必ずセットで行う**形にした。あわせて `CLAUDE.md` ほかの「Notion が正本」を**「誰が書くか」の3分類**へ書き換え（08-01 完了）
- ✅ **タスク番号制（EX）を遡及適用**（OP-001 の遠征分）。48件に **EX-001〜EX-049**、番号↔UUID の対応表と次番号（現在 **EX-050**）を Notion `_tasks` に設置。`CLAUDE.md` に「タスク番号（EX）」節と「Unity バージョン」節（Unity 6・URP 2D）を追加（08-01 完了）
- ✅ **依頼のデータ構造の棚卸し**（読み取り専用）。夜道の灯りが共通経路の例外である理由と、支給品ごとの判定場所を列挙。★ **判定場所は「支給品 × 経路」で見ないと正しくない**（同じ包帯でも戦闘と工程で別の場所が見ている）（08-01 完了）
- ✅ **Drive ざらい 第2束の続き**（オープニング導入案 v0.1／依頼ジャンル現状整理）（08-01）
- ✅ **EX-049：体験版の範囲を決める作業を開始**（`3afa8a5d-fd45-810a-8c4e-f920e97189e5`。調査のみ・コード無変更）。★ **本文の見立てを3件覆した**＝(1) category は10種で「捜索」が抜けていた (2) **古参3依頼は初期公開に入っていない**（初期公開は結婚式・廃屋・夕市・夜道） (3) **専用ハイライトを持たないのは3件ではなく9件**。掲示板は全件リストで6枚制限・一日一回枠・クールタイムはすべて未実装、金・在庫・購入・店は語としてコードに0件、`openingSeen` 相当は `state.player.interviewDone` が既に同じ形で動いている（08-01 完了）
- ✅ **所要時間の実態を測った**（EX-049 の追加調査）。★ **「1件75分」は 301÷4 の計算で、護衛本体の180分を前提4件に割り付けたものだった**。実際は前提4件＝121分・平均30.3分で、**5時間のうち60%は護衛1件**。初期公開4件は全部「近」（1/10/10/10分）で、**所要時間が進行につれて伸びる形は既に `durationBand` で実現されている**（深さ別平均 7.8 → 30.0 → 67.5 → 150.0分）（08-01 完了）

## 現在の進捗（2026-08-03 追記）
- ✅ ★ **受付嬢の名前を「アルメナ」に確定**（由来はローマの出産・記録の女神カルメンタ。一次案のカルミナから音を変えた）。★ **これでオープニングの前提が外れた**（名前が決まらないと「？？？」から変わる演出が実装できず、08-01 から止まっていた）。**あだ名は段階を分ける**＝冒険者のあだ名変更を先に通し、そのあとアルメナ側のイベントが出る／**彼女から言い出す**／**断れる**／**過去ログは当時の呼び方のまま残す**。★ **未同定対象の命名と同じ仕組みなので実装コストはほぼゼロ**（08-03 確定・実装は未着手）
- ✅ ★ **体験版の位置づけを確定**：**身内向けでアルファ未満。公開配布ではない。** 将来の配信先は **Steam か DLsite**（DLsite の Steam 配信支援を使う方向）。**仮イラストは NovelAI で代替してよいが、DLsite の AI フラグに露出ペナルティがあるので配信時は外部コミッションへ差し替える前提**。**体験版に入れる絵は受付嬢とギルド内の2枚程度**（08-03 確定）
- ✅ **体験版の範囲を詰める作業を継続**（EX-049 `3afa8a5d-fd45-810a-8c4e-f920e97189e5`）。★ **指標は総時間ではなく1件あたり**を再確認（「4任務で5時間」＝1件75分が問題という読みは、実測では護衛本体の180分を前提4件に割り付けた計算だったと 08-01 に判明済み）（08-03）
- ✅ **Drive ざらい：オープニング導入案／依頼ジャンル現状整理**（08-03）
- ✅ **frontend-design スキル導入タスク（`3b1a8a5dfd4581638cd6e06226192456`）への所感を返した**（遠征分・**導入は未着手**）。読み取りのみで、コードも `CLAUDE.md` も触っていない。★ 採番（OP-xxx）は `02_operations` が読み取り専用のため**チャット側の作業**（08-03）
- ✅ **EX-050：古参3依頼（薬草採集・道標・手紙）の現状を測った**（`3b1a8a5dfd4581b6aec4c1198b9b8e7e`。測定のみ・**コード無変更**）。★ **タスク本文の前提が2つとも外れた**（初期公開ではない／行数は最下位ではない）。★ **薄いのは専用ハイライトではなく工程ログ**で、原因は**力量が高すぎて滞りが起きないこと**（薬草採集・道標は滞りゼロ87.8%＝畑の無傷97%と同じ形。手紙だけ逆で32.5%）。**編成・支給品・天候はすべて成否に届いている**＝07-31 の経路移行は効いていた。設計判断3点で停止中（下の判断待ち）（08-03 測定完了・実装は未着手）
- ✅ **Drive ざらい：ゲームUI段階的解放設計／遠征ゲームの依頼分類設計**（08-03。下の「Drive から出てきた設計」を参照）

## 現在の進捗（2026-08-04 追記）
- ✅ **EX-050：古参3依頼の報告書を厚くする**（裁定 A/A/A どおり実装。commit `b5f5e9a`）。★ **工程経路の「効いた瞬間」＝誰の力量が支えたか**を定義し、一つも滞らなかった回だけ1行書くようにした（`fieldworkCapability` が最大値の持ち主も返す。**新しい値は持たない**）。**専用ハイライトは作らず**、代わりに**支給品のハイライトを「実際に効いたときだけ」に絞った**。★ **狙いは行数ではなく「うまくいった回にも理由が読める」こと**で、**滞りゼロの回に理由が読める割合は 100%**（工程経路の12依頼すべて）。**事実と文章の矛盾は 62/64件 → 0件**。**結末の分布は全依頼・全条件で完全に一致**（判定は1つも動いていない）（08-04 完了・検証済み）
- ✅ **EX-051：進行中タスクの棚卸し**（純粋な調査＋裁定の反映。コードは1行も変えていない）。11件を判定し、**完了5件（EX-031/033/038/039/046）＋廃止2件（EX-037/041）を完了済みへ移動**。★ **チャット側の見立てが2件外れていた**——EX-037 は「EX-038 に吸収して廃止」が**EX-038 側にしか書かれておらず**状態は「未着手（優先）」のまま／★★ **EX-032 の「シミュ/描画分離は合格」は誤りで、分離は未達成**。EX-041 と EX-044 は重複（**2026-07-27 の B-1 と同じ形の再発**）で EX-044 に一本化。**記録漏れ1件（デバッグの標準速度＝5秒固定）を DECISION_LOG と CLAUDE.md へ補完**（08-04 完了）
- ✅ **EX-052：隊商護衛の報告書を直す**（commit `4cf2049` / `afe65c0` / `b5d94fb`）。★ **ハイライトの嘘 272/400 → 0件**（ランタンは本文に一行も出ないのに夜出発200件すべてで書いていた）。★ **変化のない被弾をラウンド単位で1行に畳んだ**（EX-046「防げた瞬間」と同型の適用。被弾行の重複 1,152組 → 94組）。★ **斥候の並打ちが1文だけだったのを他の職と同じ3文に揃えた**（設計ではなく漏れ）。報告書あたりの重複発生 100% → 63.75%、行数 44.49 → 38.27、**17依頼すべてで結果分布は完全一致**。★ **完了条件「重複が他依頼並み」は差し替えた**（攻撃行は毎ラウンド出るので、最も長い戦闘が他依頼並みになることはない＝指標が依頼の性質を無視していた）（08-04 完了・検証済み）
- ✅ **時系列ログの並び順を昇順に統一**（commit `aa0b928`）。`docs/DECISION_LOG.md` の44エントリを反転し、冒頭の規約行を「古いものが上・末尾に追記する」へ。`.claude/skills/session-end/SKILL.md` の修正履歴も昇順に。★ **マスター（Notion）が昇順なので、写すときの順序変換という毎回の工程が消えた**（変換の注記は工程を消さない＝人が忘れた瞬間にずれる）。★ **以後、ログ追記の検証は「最終行＝今回の追記」で判定してよい。** 反転は順序だけで**本文は1文字も変えていない**（非空行1048行が完全一致）（08-04 完了・検証済み）
- ✅ **Drive ざらい 完了（残り4文書）**——企画メモ v0.1／世界観設計／生活系ギルド依頼の設計／Gem共通知識ベース v0.1（129KB）。結果は Notion `Driveざらい台帳（2026-08-04）` `3b2a8a5d-fd45-8174-98ca-f35af2a65832` に A〜E で分類済み。★ **今後 Drive の旧文書を読む前に、必ず台帳を先に見ること**（マージ3段階・グリッドインベントリのように、当時「採用寄り」で現行と乖離した案の蒸し返しを防ぐため）。repo には**決定2件だけ収載**した（収益モデル＝無料体験版＋買い切りアンロック／失った冒険者は同一人物として再生成しない。commit `894741e`）（08-04 完了）
- ✅ **Notion 側 CURRENT_SPEC の再生成（⑤-c）を実施**。2回ぶん（EX-052 分＋ロスト仕様の1行）溜まっていたのを解消した。生成日時 2026-08-04 00:37 → **2026-08-05 13:24 UTC**。★ **以後も CURRENT_SPEC を触ったセッションでは ⑤-c をその場で当てる（溜めない）**（08-04 裁定）
- ✅ **日付表記の注記を repo へ同期**（commit `fa5f3d9`）。★ **2026-08-04 付の記録のうち4件は、実際には 2026-08-05（JST）実施**（原因はチャット側が現在時刻を取り直さなかったこと。Claude Code 側の時刻は正確だった）。**日付は据え置き、注記で補う**（振り分け直すと推測が混じるため）。★ **完了報告に「実施日時（JST）」を1行含める運用を追加**（CLAUDE.md の貼り付けブロック節。チャット側が時刻を持たないので、報告の日時が時系列の基準になる）（08-05 完了）
- ✅ **EX-053：最初のクエスト「隣の酒場に買い出し」**（commit `cbe80ac`）。記録係の就任祝いの酒樽を隣の酒場へ買いに行く20秒のクエスト。`near_20s` を新設（実時間はゲーム内日数から導出する流儀のまま）。★ **初期公開はこの1件だけ**にし、結婚式・夕市・夜道＋廃屋を深さ1へ。★ **樽の担ぎ手は `fieldworkCapability` の持ち主（`fw.lead`）で決まり、`tendencies` で言い回しが変わる**（新しい値も新しい判定も持たない。5人それぞれ別の文が出ることを実測）。金額は出さず「代金はギルドの財布から出た」とだけ書く。酒場は画面外の場所。**既存17依頼は結果分布・行数とも完全一致**（08-05 完了・検証済み）
- ★ **CURRENT_SPEC の冒険者表の id が2件間違っていた**（`adv_erne`→`adv_elne` / `adv_low`→`adv_row`）。実装を見て直した。**測定ハーネスをこの表から書いて落ちたのが発見のきっかけ。**
- ✅ **報告書 11.59行の内訳を測った**（コード無変更）。裁定は **短縮しない**（遠征と読書は非同期で、長さが次の行動を止めない）。★ **差の本体は定型行**——夜道 4.50行は専用分岐で定型行が0行、**本編の量（3.50行）は両者で同じ**。内訳は `docs/DECISION_LOG.md` 2026-08-06 に収載（08-06 完了）
- ✅ **EX-054 作業1：工程ごとの参照育成値を宣言できるようにした**（commit `9c325b1`）。★ **宣言は判定工程（`quest.fieldworkSteps`／phase）側に持たせ、`workEvents` は触らない**（描写の抽選が乱数列を消費するため、そちらに置くと不一致0を満たせない）。★ **成長側は `hiddenTags.fieldwork.stats` だけを読む**（依頼データの直読みはしない＝対応表を1つに保つ）。宣言のない工程はジャンル表のまま。**全18クエスト・1,080件で不一致0**。**宣言を持つ依頼はまだ無い＝仕組みだけが入った状態**（08-06 完了・検証済み）
- ✅ **EX-054 作業2・3＝宣言を最初のクエストに当て、廃屋の解禁を達成数にした**（commit `38beb68`）。**店主と話を通す＝`negotiation` ／ 樽を担ぐ＝`survival`**（★ **体力に対応する育成値は現状 survival の1つだけ**。`maxHp` の元でもある）。**担ぎ手はガッド→ロウに変わった**（承知の上の裁定）。★ **廃屋は `unlockedAfterCount: 2`＝行った依頼の種類数が2件で開く**（どの依頼かは問わない＝3回目に選べる。`unlockedBy` とは別条件で、両方あるときは AND）。**既存17クエストは不一致0**、掲示板は 0件＝1／1件＝4／2件＝6 で初期公開1件も維持。結末は 出直し 8.5%→3.25%・寄り道 39.3%→30.0%、行数 11.59→11.53（08-06 完了・検証済み）
- ★ **エルシー単独では樽の行が出ない**（工程エンジンを通らないため）。**EX-054 の変更前からの挙動**で、HEAD `725394c` でも同じことを確認した。→ **`canSolo: false` により到達不能な経路で、バグではないと判定済み**（08-06・チャット側）
- ✅ **⑤-b にも条件付き差分更新を認める改訂を写しへ同期し、DECISION_LOG の写しに漏れていた3件を正本から写した**（commit `eaff0a5` ほか）。★ **⑤-b と ⑤-c で扱いを分ける根拠がない**——全置換のほうが事故率が高いという判断は対象ページが変わっても同じ。あわせて**Notion の番号付きリストの自動採番は既知の変換差分として差分箇所に数えない**ことを明記。写した3件は **初回強制完遂／エルシーの疲労値／性格の統合（`personality` 廃止）**（08-08 完了）
- ✅ **【裏設定】受付嬢と記録員バッジの深掘り**（`3a3a8a5d-fd45-8173-ab8f-df6604a9b180`。**チャット側の作業。コード・データは無変更で、repo 側への反映も不要と裁定済み**）。★ **「動機は掘らないと決めた」**——未採用案5件を理由つきで記録した。★ **repo 側で覚えておくのは「掘らない」という決定があること自体**で、中身は裏設定ページが正本（08-08 完了・チャット側）
- ✅ **EX-062：行方不明→死亡を実時間7日にする影響調査**（`3b4a8a5d-fd45-81c7-87db-c655a26fb482`。調査のみ・コード無変更）。★★ **「行方不明→死亡」のタイマーは実装されていない。** HP0 は戦闘の中だけの `downed` で、帰還時に**HP率0＝「重症」**に落ち、60分（実時間）で回復する。`state` に行方不明という状態も「3日」という値も無い。`GROWTH_SURVIVAL_MULT.missing` は受け皿でどこからも参照されない。★ **`searchChain` は時刻を一つも持たず期限が無い**（放置しても消えないし進まない）。★ 7日にするなら **state への状態追加／`getDemoSpeed()` の倍率の扱い（60倍だと7日が2.8時間）／編成枠の塞ぎ方／期限を行方不明者と捜索チェーンのどちらに持たせるか／オフライン経過／「日」の表記が2つになる** の6点を決める必要がある。★ **見立ての検証**：クールタイムは「消化数ベースだから無関係」ではなく**未実装だから無関係**／回復は独立だが **`getDemoSpeed()` の倍率は掛かる**（08-06 完了）
- ✅ **EX-061：判定用語彙を確定して `data-vocab.js` に定義**（`3b4a8a5d-fd45-81de-9338-eb2d9c41ac3e` は EX-060。commit `284ecf0`）。**行為11語**（運ぶ／察する／伝える／前に立つ／攻める／進む／手当て／記録／留める／整える／火と食）と**対象5語・任意**（重い／壊れ物／紙物／生き物／人）。★ **`tags` は表示用のまま据え置き、判定には使わないと決着**（2026-08-03 からの未決を解消）。★ **行為が主で対象は任意**にしたことで、書ける工程が 63%→77% に上がり対象側の問題が全部消えた（EX-060 の実測）。全18依頼の101工程・支給品8品・冒険者5人に付与し、**11語・5語とも未使用ゼロ**。⚠️ **判定には接続していない**（読んでいるコードは無い）。全18依頼で不一致0（08-06 完了・検証済み）
- ✅ **EX-060：⑤-c の規約改訂を写しへ同期**（commit `51d1ef4`）。★ **3条件（同一生成元／差分が特定できている／読み直して検証し報告に明記）を満たす場合に限り、全置換でなく差分の外科的更新でよい**。例外の追認ではなく規約の改訂なので、条件を満たす差分更新は「はみ出し」に書かない。マスターとの差分はこの2点だけだった（08-06 完了）
- ✅ **EX-058／EX-059：判定用語彙の材料出しと、行為×対象への割り当て**（調査のみ・コード無変更）。tags は6か所が持ち130語・延べ195。★ **4軸に無い軸が2つ実在**し、社会的文脈は6.2%と最も薄かった。行為×対象で割ると、対象必須では書けるのが63%どまりだった（08-06 完了）
- ✅ **EX-055：tags の棚卸し**（`3b4a8a5d-fd45-81f9-9b6b-c141c7680115`。調査のみ・コード無変更）。tags を持つのは6か所で、**指示の4つに加えて特性（37語）と記録係の気質（12語）も持っており、どちらも死蔵**。重複を除いて130語・延べ195。4軸へ分類すると 要求スキル38.5%／対象物15.4%／環境10.8%／**社会的文脈6.2%（最も薄い）**、別枠が 複数軸4.6%・軸外24.6%。★ **4軸に無い軸が2つ実在する**（行動特性/性格10語・切迫度/反復性4語）。精度の問題は 同義語9系統・`category` と同名9語・**依頼59語中43語が1回だけ（73%）**・実装名が8語。**軸を変えるかは裁定待ち**（08-06 完了）
- ✅ **EX-057：エルシーを探索特化に＋読了ハンコ**（`3b4a8a5d-fd45-819c-8431-f9bc48b02a73`。commit `08ba6bd` / `9a0973f`）。育成値は **戦闘0／交渉0／支援0／調査10（据え置き）／探索32／生存30**。★ **「戦闘0で与ダメが消える」はチャット側の誤りで、`simulateBattle` は冒頭で人間だけを取り出しているためエルシーは元から与ダメも forecast も持っていない**（効くのは `hasElsie` の旗＝撤退の成否と結末だけ）。★ **基準4人（犬なし）は全18依頼で結末分布・行数とも不一致0**（力量はパーティ内の最大値なので犬なしには効かない）。犬を連れた編成では8依頼が動き、**support 20→0 で保全・救助の小編成は力量が下がる**（犬は支える側から探す側へ移った）。検査 `check-elsie-actions.js` は 10,800件で違反0件。読了ハンコは `report.readStampAt` を持つだけで **`schemaVersion` は 3 のまま**・未読件数バッジなし・8項目 PASS（08-06 完了・検証済み）
- ★ **名簿の「傾向」に255スケールの値が出る件は、ローカルの現行 HEAD では再現しない**（「傾向」も「記憶」「胆力」も `app.js` に0件）。**表示の削除は 2026-07-31 の commit `6a1a180`**で、デプロイ済みビルドがそれより古いだけ
- ✅ **EX-056：工程の担い手から犬を外す宣言（`humanOnly`）**（`3b4a8a5d-fd45-81bc-a62c-febae589b588`。commit `e85d57d`）。★ **樽だけの話ではなく9依頼に同じ形で存在していた**（`エルシーが道と目印を先に読み…`／`道具と手当てを先回りで整え…` が実際に出ていた）。**力量の計算は変えず、外すのは担い手＝名前が文に出る側だけ。** 工程ごとの宣言が優先し、工程を宣言していない依頼は依頼ごとに同じ鍵で宣言する（対応表は増やさない）。**検査 `scripts/check-elsie-actions.js` を新設**（全18依頼×16編成×各40件＝10,800件で犬の担い手 0件・違反 0件）。**11,520件で結末・行数とも不一致0**、文面が変わったのは宣言を付けた10依頼の 844件だけ（08-06 完了・検証済み）

## 現在の進捗（2026-08-19 追記）

- ✅ **EX-057 完結：工程数と宣言の切り離し → 担い手の工程単位化 → 文面の階層化**（commit `1c6b689` / `94b56d2` / `26fa909`）。★ **停止理由だった2点が解消**——`fieldworkPhases` があればその固定数・無ければ乱数2〜4とし、`fieldworkSteps` は宣言だけを持つ（数は決めない）。担い手は全工程で控え、**値が最大の工程の担い手**が「効いた瞬間」の主語になる。文面は**階層方式の初適用**（工程固有 ＞ 種族固有 ＞ 基層）で、探索に犬固有3文＋基層1文、樽担ぎに工程固有1文。★ **人間の抽選プールは据え置き**（基層を足すとプールの数が変わり同一シードの抽選結果が動くため、基層が抽選に乗るのは現状犬だけ）。**薬草納品の `fieldworkHumanOnly` を外した**（においで薬草を見分けるのは犬の領分）。2,880件で切り離しは不一致0、工程単位化で動いたのは樽103件のみ、文面追加で動いたのは樽103件＋薬草納品80件のみ（08-18 完了・検証済み）
- ✅ **EX-071：樽の二重行を解消**（commit `aa29aad`）。樽の行（`tendencies` 分岐）が出た回は工程固有文を出さない。★ **汎用の衝突検知は作らない**（2例目が出てから）。二重行0件。★ **現行の樽では樽の行が毎回出るため、工程固有文は実質常時抑制**になる（事実として記録）
- ✅ **EX-070：行方不明の時計と状態を実装**（`3c0a8a5d-fd45-81c9-ab7d-e7b4a78efad9`。commit `1e5801c`）。★ **冒険者を失いうる最初の経路。** 発生＝敗北（`defeat`/`stalemate`）で `downed` のままの者だけ・個人単位／エルシー同行時は発生しない／致命は当面対象外／対象は戦闘経路3件すべて。時計は**判明起点・遠征がある間だけ進む・demo 倍率なし**で、猶予（行動時間24時間相当）と段階3つの閾値は `data-time.js` の `masterMissingClock`。状態は `status: "行方不明"`＝既存判定で編成不可・負傷は持たない・`savedKeys` に `missing` を追加（`schemaVersion` 据え置き）。期限切れで「死亡」へ遷移（遷移まで。追悼画面は作らない）。★ **実測の発生率は 護衛×エルネ+ロウ 0.45%／他は0%**——納屋は敗北/膠着19.9%だが膠着が大半で downed を残す敗北がほぼ出ない。2,880件で不一致0（08-18 完了・検証済み）
- ✅ **EX-063：判定用語彙の行為を13語に**（commit `d4d37a8`）。〈採る〉〈離れる〉を追加。★ **〈退く〉ではなく〈離れる〉**（撤退は荷物を置いてくることであり逃走ではない）。付与4件（薬草採集の摘む工程／エルネ＋／煙幕／エルシー＋）で**未使用ゼロ・未定義ゼロ**（工程102・行為13・対象5）。⚠️ **判定には未接続のまま**。不一致0（08-18 完了・検証済み）
- ✅ **EX-064：クエスト「ギルドの掃除」＝受付嬢が書く定型報告書**（commit `0cf388d` 器 ／ `3a7091d` 文面）。★ **本作で唯一、報告書の書き手が受付嬢になる例外。** `fixedReport` / `reportAuthor` の旗で分岐し、**`app.js` は依頼IDを見ない**。抽選を1つも引かないので**シード・時間帯・天候を変えても同一**。記録係も名を連ね、エルシーは常駐扱い。**成長なし・支給品なし**。既存18依頼は不一致0（08-18 完了・検証済み）
- ✅ **EX-067：作業前の origin 同期確認を手順化**（commit `f194bfb`）。session-end に ⓪ を新設し、CLAUDE.md にも同じ規則を入れた。★ **報告に HEAD と origin の一致を1行書く**（一致していたことも書く）。EX-066 でコンテナが 2026-07-26 のクローンで起動していた事故が根拠
- ✅ **EX-066：却下案索引を生成物として作った**（commit `4135932`）＋ **EX-069：表を年月で割る形に**（commit `7c8891d`）。`docs/REJECTED_INDEX.md` は `docs/DECISION_LOG.md` からの生成物で、session-end ⑤-d で毎回作り直す。★ **理由は要約せず原文の先頭を切り出す**
- ✅ **EX-065：記録員バッジのUI表示**（`3b7a8a5d-fd45-81be-aba0-da9f02eb8502`。commit `4f29200`）。就任前は非表示・就任と同時に出現、押すと名前と就任日だけ。★ **数字も未読件数も出さない。** 就任日 `state.player.appointedAt` を新設（旧セーブは最古の報告書の作成日時で補完・`schemaVersion` 据え置き）。「光る」状態は再演出用に形だけ持たせて未使用
- ✅ **EX-068：短編ナラティブ調査の写しを `docs/research/` に置いた**（commit `2467181`）。Notion が正本（`39ba8a5dfd4581e6a1ccdceb3611470c`）。★ **正本側の文字化け8語を報告 → チャット側が修正 → 2026-08-19 に写しを取り直した**（文字化け一覧の節も削除）

- ✅ **EX-051 以来2回目のタスク棚卸し（session-end ⑦）**。`_tasks/進行中` 15件を判定し、**完了なのに進行中に残っていたページが10件**あった（EX-054/056/057/058/059/060/062/065/066/070。対応表は EX-057 以外すべて既に「完了」＝**表とページの置き場がずれていた**）。★ **EX-048「結末51種を汎用化するか」は EX-047 で決着済み**（DECISION_LOG 2026-08-01「`category` から導く形は実測で否定された」）。★ **移動と廃止は裁定後に行う**ので、今回動かしたのは指示のあった EX-055 だけ

## 参照する種ページ（2026-08-06 追加）

★ **チャット側が今日作った「種」の2ページ。** 設計の芽で、タスクではない。**探しにくいのでここから辿れるようにしておく。**

- **（種）分類するというプレイヤー動詞｜亜人と「人」の境界** `3b4a8a5d-fd45-8106-824f-c7730d1538d8`
  - ★ 判定用語彙の対象語〈人〉の定義に直結する（EX-061 で〈生き物〉から〈人〉を分けた判断の背景）
- **（種）命名と断定の罰｜反ミームの記入規則** `3b4a8a5d-fd45-8180-baae-c81870f52ab8`
  - ★ EX-062（行方不明→死亡を実時間7日にする）の根拠として指定されたページ

## DECISION_LOG の写しに入っていなかった決定（2026-08-06 発生・2026-08-08 に解消）

★ **2026-08-08 に正本から写して解消した。** 記録として残す。★ **同じ漏れを防ぐには、チャット側が正本へ書いた日に写しへも配ること**（この3件は2日ぶん滞留した）。

- **初回強制完遂**（最初のクエストの扱い）
- **エルシーの疲労値**
- **性格の統合（`personality` 廃止）** ※実装は未着手。★ **実装するときは `findByTrait` 系と `PLAYER_PERSONALITIES` に効く**

⚠️ ★ **写しの同日内の順序は正本と一致していない。** 正本は決定ごとに1エントリだが、**写しは作業単位でまとめた粒度**（例：写しの「樽を担ぐは survival ／ 廃屋の解禁は達成数2件」は正本では2エントリ）なので、1対1に並べ直せない。**今回の3件は正本での相対順（強制完遂 → 疲労値 → 性格の統合）を保って 08-06 の末尾に置いた。**

## 現在の進捗（2026-09-12 追記）

- ✅ **EX-072：行方不明の段階文言を本置き**（commit は 09-11 分）。段階2「消息を絶ってから、しばらく経つ」＋「{名前}に焦りが見える」／段階3「だいぶ経つ」＋「{名前}の表情は暗い」。★ **{名前}＝最後に同じ遠征に出ていた者**（報告書の参加者から導く。新しい値は持たない／複数なら参加者順の先頭／該当者なしは受付嬢アルメナ＝仮置き）。表示はピルではなくカード内の行（430px で名前欄が潰れたため）
- ✅ **EX-073：probe 廃止＋PR 運用の決着**。★ **old＝new の probe は存在しない文字列でも成功を返す**＝照合していない。当たり確認は `notion-fetch` の読み直しだけ。`notion-search` も使わない。old≠new の「No matches」は「消えたことの確認」にだけ使ってよい。PR #1 をマージし（`53ebb85`）、session-end に **④-b** を新設
- ✅ **EX-074：判定用語彙を担い手の選出に接続（第一段）**。フィルタ方式・行為のみ・向きは見ない・語を持つ者が0人なら絞らない。3,040件で不一致0。★ **犬が「できない行為」の担い手になる穴を1件検出**（石碑の拓本。`fieldworkHumanOnly` で解消）
- ✅ **EX-075：〈留める〉を器用さに限定し、エルネとミナへ付与**。基準4人のフォールバックは0に。担い手が変わった工程は0で、**フィルタが効くようになったのは228工程**
- ✅ **EX-076：`phasesMax` の由来調査**（調査のみ）。★ **4に裁定の記録も実装根拠も無い**（導入は `bb5e78d`・2026-07-31）。ただし絶対件数の閾値により難度に強く効く
- ✅ **EX-077：滞りの閾値を比率化**（commit `b0164af`）。部分 `ceil(0.25n)` ／未達 `max(2, ceil(0.5n))`。**現行の工程数 2〜4 では全依頼で恒等**（3,040件・11,200件で不一致0）。純比率は却下（2026-08-01 の未達帯の裁定に触れる／酒場の「部分」が消える）
- ✅ **EX-077 作業3：癒着は3つあり、切れたのは1つだけと判明**。★ **一度 CURRENT_SPEC に「残る癒着は疲労の側」と書いて同日中に訂正した**（commit `c4d7f67`。一律4工程を測っていなかったため段差が見えていなかった）
- ✅ **EX-078：`steps[phase-1]` を均等間引きへ**（commit `1b5e2a3`）。宣言と語彙の両方に同じ写像。**届く行 70→79**、3,040件・11,200件とも不一致0、犬の禁止行動チェック違反0件。★ **間引きは総量を増やさない**（53.00行→53.00行）ので、代わりに各依頼の行2が落ちた（12行）
- ✅ **EX-079：結婚式の「厨房を手伝う」を先頭へ**（commit `dffec98`）。唯一の〈火と食〉が **0.00→1.00本**。★ **その依頼にしか無い語は先頭か末尾に置く**（先頭と末尾は工程数によらず必ず読まれる）。到達不能行にしか出ない語は0に戻った
- ✅ **セルフレビューの試行（EX-075【2】）を3回実施**。停止報告をサブエージェントに叩かせてから出す運用で、**3回とも出す前に直すべき誤りを検出**（1回目＝前提の事実誤り3件／2回目＝論点の取り違え／3回目＝★ **CURRENT_SPEC に書いた診断そのものの誤り**）。**コストに見合っている**
- ✅ **EX-080：担い手と本文のズレを調査**（読み取り専用）。★ **「誰がやったか」は4つの独立した選択器が別々に決めている**（工程の担い手／本文の作業行の主語／滞り・手当ての行／支給品の行）。担い手が主語になる行は「効いた瞬間」と樽の2つだけ。結婚式4人編成の一致率 10.7%・酒場の「効いた瞬間」103件は全て抑制・道具の行1,060件は全て無人称。**裁定は C（前提が違う＝作業行と工程は別物。合わせにいかない）**
- ✅ **EX-081：第二段（支給品の使用優先）の設計案**（実装なし）。★ **薄いのは接続の設計ではなく語彙の分布**——〈察する〉36行に対し〈火と食〉〈採る〉は各1行。語で絞ると笛の発動が 50.3%→9.5%、鍋は14依頼中13で発動不能。文面だけ接続しても書き分けられるのは666件中35件（5.3%）。**裁定は A（いま接続しない）。再開の条件＝語彙の分布を厚くしたとき**
- ✅ **EX-082：支給品の二重記述を直した**（commit `46225c4`）。原因は独立した3つ（依頼固有ログが事実を見ない／条件が揃っていない／天候の渡し忘れ）。★ **抽選を済ませてから落とす形**にして乱数の消費を変えず、11,400件で結末・あらすじ・滞り件数は不一致0、行数が変わったのは**嘘の行が消えた41件だけ**
- ✅ **EX-083：`weatherLoad` のキー食い違いを直した**（commit `cae20d4`）。「強風」→「風が強い」。★ **バランス調整ではなく故障**——表の7が一度も効かず、**風が強い日は既定値4（曇りと同じ）で動いていた**。完遂 60.5%→52.9%・未達 12.0%→16.3%（他4天候は完全に不変）
- ✅ **EX-084：表の掃除と登録漏れ**（commit `a3d810c`）。未実装の天候2件を表から削除（★ **消した値は記録：雨上がり3／雨11**）、死蔵の `questEventPools.weather` 配列4件を削除、捜索チェーン2件の許可リストを追加（煙幕・観察記録票は外した）。対応表11件を機械照合し、**名前の食い違いは `weatherLoad` の1件だけ**と確認
- ✅ **EX-085：許可リストの既定を全禁止へ**（commit `90e6940`）。★ **全19依頼が許可リストを持った今だけ挙動不変で切り替えられる窓**だった。11,400件で不一致0。許可0件は `quest_guild_cleanup` のみ（意図どおり）。`CLAUDE.md` に「書き忘れると支給品の行が一切出ない」と結果まで書いた
- ✅ **セルフレビューを正式運用にした**（企画横断）。★ **5回まわして5回とも、出す前に直すべき誤りが出た**（毎回3〜6件）。論点の取り違えと CURRENT_SPEC の診断の誤りも捕まえている。`.claude/skills/session-end/SKILL.md` の停止時の節に追加（マスターはチャット側で反映）
- ✅ **EX-051／2026-08-19 に続く3回目のタスク棚卸し（session-end ⑦）**。`_tasks/進行中` は **4件**（EX-006／EX-032／EX-049／酒樽の保留）。**判定は4件とも「未着手（意図的）」で、重複も前提の崩れも無し**。逆方向（完了済みに入っているが完了時の作業が残っていないか）も見て、EX-048 は 2026-09-12 に完了済みへ移動済み・DECISION_LOG に根拠あり（2026-08-01 の EX-047 エントリ）で残件なし。
  - ★ **ただし EX-032（バランス検証ハーネス）は「放置」ではなく「毎回 手で作り直している」状態**だと分かった。**今セッションの scratchpad だけで使い捨てスクリプトが43本**（★ 当初42本と書いたのは数え間違い。内訳＝Playwright を起動 34本／Node だけで集計 9本、うち `generateReport` を呼ぶものが27本）。`ex078-cmp.js` → `ex082-cmp.js` ＋ `ex082-diff.js` は**同じ形を書き直している**。★ **`scripts/` の .js は11本**（★ 当初「12本すべて DevTools 貼り付け前提」と書いたのは誤り）で、**DevTools 貼り付け・依存ゼロが8本／Node から実行する `gen-*.js` が3本**。Node から回す形は既にあり、**11本に共通するのは「外部依存ゼロ」のほう**。**EX-032 の未達項目の記述は現状と合っているが、「誰も困っていない」わけではない。**
- ✅ **EX-086：検証ハーネスの共通化（裁定A で実装済み。commit `3611d2a`）**。`tools/sweep.js` に `sweep`／`compare`／`tally`／`scan` の4本。★ **置き場は外部依存の有無で分かれる**（`scripts/` の11本は外部依存ゼロ＝クローンしただけで流せる。線引きは `CLAUDE.md` に明記）。★ `lines` と `fieldwork` は **opt-in**（本文つき出力は実測17MB）。代表3本を書き直して**旧版と全件一致**（比較 11,400件で結末・あらすじ・行数・滞り件数・本文とも不一致0／分布は風が強い 完遂 52.9% が EX-083 の記録と一致／走査は17条件の内訳まで不一致0）。実行行数は **13／14／29**（見積もり 14／15／45 に対し、走査は `scan` が Node 側で正規表現を扱えるぶん見積もりより16行短かった）。★ **EX-032 は閉じない**——未達項目2つ（`scripts/` の DevTools 前提／CLAUDE.md への実行時間の追記）はどちらも残る
- （旧）EX-086 の設計案・停止時の記録：★ 共通なのは**軸と集計の形**で、違うのは probe の中身だけ。案A＝`tools/sweep.js` を新設し `sweep`／`compare`／`tally`／`scan` の4本に切る（推奨）／案B＝`scripts/` に置く。★ **論点は「外部依存（playwright）を repo のどこに置くか」の一点**——現行 `scripts/` の11本は全部が外部依存ゼロ。★ 見積もりはパターンで大きく違う（比較 51→14行／分布 47→15行／**走査 70→45行どまり**）。★ **EX-032 の未達項目2つ（`scripts/` のハーネスの DevTools 前提／CLAUDE.md への実行時間の追記）はどちらも解消しない**
- ✅ **EX-088【1】【3】：〈察する〉から〈確かめる〉を割った**（commit `5c8c1f5` / `6b17d8d`）。行為14語。8行を移し、**ロウ（門番見習い＝柵と段差を見て回るのが元の仕事）とエルネ（既に壊れ物の語を2つ持つ）**に付けた。★ **11,400件で結末・あらすじ・行数・滞り件数・本文とも不一致0**（語は力量に効かず担い手だけを絞るため＝`app.js:7169`）。担い手が変わったのは**道標150件（ミナ→ロウ）**のみ。犬の禁止行動チェック違反0件。〈確かめる〉の到達は**8行中5行**。`data-vocab.js` 冒頭の古い注記（「判定に接続していない」）も直した
- ✅ **EX-089：語彙表を5行以下へ圧縮**（commit `dd7bc8f` / `5ab7d75`）。死に行21行を抜いて **102→81行**、工程エンジンを通る14依頼の未到達行は **21→0**。★ **11,400件で本文・担い手まで不一致0**（死に行だけを抜いたので並びが不変）。★ **削除ではなく退避**（`data-vocab.js:268` の `window.masterVocabRetired` に文・語・旧行番号を残した）。⚠ 並びが不変なのは現行の `phasesMax: 4` の下でだけ（酒場は3・4工程で崩れる。今は到達しないだけ）
- ✅ ★ **語彙を厚くする作業はここで打ち止め**（2026-09-12）。**到達しうる行は63で、圧縮しても増えない。** 工程エンジンを通らない4依頼（畑・納屋・護衛・夜道）の18行は語彙側の手では救えず、**〈離れる〉0行・〈攻める〉到達0も同じ理由**。★ **ここから先は「戦闘依頼を工程エンジンに載せる」という大きな話**で、**着手条件＝戦闘の作り直しをするとき**として未解決に残した。★ **したがって EX-081 の第二段は再開条件を満たさないまま保留が続く。これでよい**（薄い機能を据え付けるより保留の方が正しい）

- ✅ **EX-090：包帯の判断への重み（−20）の調査**（読み取り専用）。`tools/sweep.js` で 288,000遠征。★ **効くのは36セル中5セルだけ**で、本体は「納屋と護衛でミナ+ガッドが包帯を持つと段階1を通り抜ける」の一点。反転の境界は**護衛 −18／納屋 −1**、包帯そのものの実力は**護衛 1.6→6.0%／納屋 47.9→64.4%**。★ **「裁定記録が無い」のは値の大きさだけ**で、効き方（残り個数・2026-07-30）と適用範囲（判断に残す・2026-07-31）は裁定済みだった（セルフレビューで自分の誤りを検出）
- ✅ **EX-091：重みを −20 → −10**（commit `4b62724`／`app.js:6448`）。同一シードの対で 72,000組を比較し、**変わったのは 4,020件（5.58%）＝EX-090 の予測と完全一致**。護衛×ミナ+ガッドの反転は消え（包帯0/1/2 とも 100% 引き返す）、納屋×ミナ+ガッドの反転は残った（勝利 64.4%）。結末分布は**60セル中56セルが完全一致**。★ 2026-07-31 の「直りきらなかった2点」の2番目はこれで閉じた
- ⚠️ **測り方の注意**：`Math.random` で新旧を別々に引くと n=1,500 でも ±1.5pt の揺れが出て本当の差と見分けられない（実際に一度そう測り、24セル中22セルが「差あり」に見えた）。★ **シードを揃えた対で測る。**
- ✅ **EX-092：夜道 v2 を実装**（commit `57f7f36` / `e1afed6`）。★ **`battleAmbush`（段階1を経ない遭遇）と `battleWeakenedBy`（ランタン＝敵の弱体化）を依頼データの宣言として新設**。`withdraw_first` が出なくなるので**観察記録が全結末で残る**（v1 からの後退の解消＝採用の主眼）。敵は「硬い」ではなく**「重い」方向**（HP650/threat80）——threat を 70 に下げると膠着 2.1%・行方不明 41件が出る。結末分布はランタンなし＝勝利 0.0%／撤退 82.3%／膠着 0.0%／敗北 17.8%、ランタンあり＝勝利 100.0%／深手 8.1%。v1 は `quest_lingering_light_v1` ＋ `hidden` で退避し、**id は v2 が継いだ**
- ✅ **昼夜のハイライトが結末を見ていなかったのを直した**（畑・納屋の中止／失敗で「追い払いは成功した」「無事に戻ってきた」が出ていた。97,200件で 977件が変化、他は不一致0）
- ✅ **EX-093：主成長を時間帯で分け、再出現を入れた**（commit `e9e66fc` / `f06b30a`）。報告書が宣言する鍵 **`hiddenTags.growthStats`**（夜＝combat/survival ／ 昼＝investigation）。再出現は**倒すまで戻る・消化数1〜3**。★ **初版は `state.reports` の向き（unshift＝新しいものが先頭）を取り違えて逆に動いていた**（挑戦直後にすぐ戻り、以後3件周期で点滅）。★ **この故障は sweep では構造的に検出できない**——sweep は毎回 `state.reports` を空にし、掲示板を描かない
- ✅ **EX-094：過去の「本文まで不一致0」を測り直した**（commit `f2e8fa5` / `0cb0d64` / `40c3378`）。EX-084／085／088【1】／089 を各45,600件で再現し、**空振りは無かった**。★ ただし **EX-085 は変更行が到達不能で掃き出しが証拠になっていない**（主張自体は正しい）。**EX-086 は前後比較として再検証不能**（ゲーム側を1行も変えていないコミット）。★ `compare` に警告3つ（**`unchecked`** / `constant` / 両側判定）を追加——**`fields` に入れ忘れた項目は鍵に取り込まれ、差が `missing` に落ちて `diff` は0のまま**だった
- ✅ **EX-095：掲示板に「待機中」の1行**（commit `dc94cd4` / `22e19e4`）。「{依頼名}の件は、まだ次の話が来ていない。」。**解決済みと再出現は出さない**（前者は結末文が言っている／後者は黙って現れるのが設計7の質感）。★ **`state` は1つも増やしていない**。実機の DOM で8状態を確認、既存依頼は48,000件で不一致0
- ✅ **EX-096：夜道 v1 を掲示板から削除**（commit `7f7b81e` / `510716a` / `80571b0` / `09b649a`）。★ **消さずに退避**（`masterQuestsRetired` / `masterOutcomesRetired`）。依頼 **20→19件**・hidden **3→2件**・掲示板候補は17件のまま。★ **旧セーブは壊れない**——v1 時代の報告書は `questId` が `quest_lingering_light` で、その id は v2 が継いでいる（削除前後の画面出力が完全一致・石碑の解放も保持）。⚠️ **`GROWTH_TIER_BY_RESULT` から2件消える**（60→58）が、生きた依頼はその結末名を出さない。★ **この差は画面にも報告書にも出ないので、どちらの検証でも拾えない**——グローバル表を直接読んで確かめた
- ✅ **EX-097：依頼が見つからない遠征を中断する防御**（commit `8494e81` / `6c139dc`）。`checkExpeditionCompletion` が所要時間を待たずに畳む。**冒険者は待機中へ／暦は進めない／報告書は作らない**（死んだ id を解放条件と報告メモに混ぜない）／行方不明の時計はその遠征ぶんだけ積んで止める／`console.warn` 1行。★ **これは一度きりの事故ではなく構造的な穴**（依頼の入れ替えは今後も起きる）
- ✅ **対比較の項目が 6→10 に育った**（2026-09-13〜14）。結末 / あらすじ / 行数 / 本文（指紋） / 観察記録 / 成長段 ＋ **ハイライト / 履歴行 / 冒険者の履歴行 / hiddenTags**。★ **4回とも壊れていたのは実装ではなく検証のほうで、全部「不一致0」の顔をして出てきた**（参照比較 → `keepLines` の付け忘れ → `constant` → **`unchecked`**）。⚠️ **まだ見ていない**：報告書に後から行を足す3つと `checkExpeditionCompletion` の経路は `sweep` の範囲外
- ✅ **EX-098：写しと生成物の扱い3件**。①チャット側が正本へ直接書いた節を**却下欄の無い形式のまま**写しへ取り込む（索引の「項が無いエントリ」が 19→20件に増えるのは正しい反映）／②**Notion で消えるコードスパンは生成側で「」へ**（`scripts/gen-notion-spec.js`。実害は `+` と `-` の2件だけ）／③**写しは全置換しない**代わりに「言い換えが蓄積している／一字一句は正本を見る」を写しの冒頭に常時出す。★ **①③は `session-end` の ② と ⑤-b に規約として追加**（マスターは未反映）
- ✅ **EX-099：夜道 v2 に `usedItemIds` は接続しない**（裁定のみ・コード変更なし）。★ **ランタンは既に3経路で報告書に出ている**（敵の弱体化で結末が変わる／夜の固定文 360/360件／昼の古地図の行 240件）。接続の価値が「もう一度書く」だけなら**樽の二重行（EX-071）を自分から作ることになる**。実測1,440件でも接続だけでは1文字も変わらない
- ✅ **`session-end` の写しをマスターと再同期**（差分5件）。★ **写しに来ていなかった 2026-09-09 の「C：前提が違う」**（A/B に畳むと畳んだ側が枠を決める、への構造的な対処）を含む。セルフレビューの**渡す観点5つ**も、写しでは短縮されていて列挙が落ちていた
- ✅ **EX-100：EX-049 の材料出し（調査5件・コード無変更）**。所要時間は**近6件／短8件／中5件／長0件**で、未使用の帯は `near_5m` / `long_5h` / `long_7h`。掲示板は**枠数の上限が無く**、済んだ依頼も並び続けて**最大17件**（★ **未消化の最大は7件**——当初5件と報告したのは**1つの消化順で観測しただけ**で上界ではなかった。到達可能な1,332状態を全列挙して 2026-09-15 に訂正）。クールタイムは**依然として未実装**で、EX-093 の `reappearAfterCount` は「倒すまで戻る」＝別物（持つのは夜道 v2 の1件）。行方不明は **3,360件中2件（0.06%）**・死亡は実質到達不能。ジャンルは9種だが**掲示板に出るのは8種**（捜索2件が hidden）。★ **2026-09-15・EX-103 で「帰ってこない薬草採りの確認」を 救助→捜索 へ付け替えたので、いまは 捜索3件・救助0件＝全体も8種**（救助を1件新設して9に戻す）。あわせて `02_operations/スキル：session-end` のページIDを `CLAUDE.md` に登録し、写しの規約に「**写しを作るときに短縮しない**」を追加
- ✅ **EX-101：体験版の範囲のうち3件を確定**（裁定3・4・5／コード変更なし）。★ **体験版で行方不明・死亡は起こさない**（不足ではなく仕様。**ただし経路は残す**＝隊商護衛 × 2人編成の 0.45%）／**未使用の帯3つは削らない**（本編の長時間遠征で使う。★ 線は「使っていないか」ではなく「宣言されているか」）／**所要時間は現状のまま**（総時間は指標にしない）。詳細は `docs/DECISION_LOG.md` の 2026-09-15 のエントリ

- ✅ **EX-102：掲示板を6枠にし、消化した依頼にクールタイムを入れた**（commit `78141f0` / `c661a2c`）。**未消化＝解放の浅い順**／余り枠＝クールタイム明けの既消化を**久しく行っていない順**／**緊急依頼は枠の外**（カードは最大7枚）／選択の掃除を枠あふれへ拡張。値は `data-quests.js` の `masterBoardRules`。★ 未消化7件の局面では**いちばん深い「街道の外れを行く隊商の護衛」が落ちる**
- ✅ **EX-103：「帰ってこない薬草採りの確認」を 救助 → 捜索 へ付け替えた**（commit `893111d`）。★ **`generateReport` の出力は 27,360組・11項目で不一致0**（`absent`／`constant`／`unchecked` も空）。★ **変わったのは `generateReport` の外側の3つだけ**＝(1) 成長 stat 探索+支援 → 探索+調査（裁定どおり） (2) **在席ログと掛け合いが「探索」の文へフォールバック**（`捜索` のキーが表に無い。⚠️ **救助の文のほうが捜索らしい**ので、移すかは文面側の判断） (3) **エルシーの行は partial（痕跡確認）のときだけ 200回中57回変化**し、「負傷者のそばを離れず」が 22→0回
  - ~~★ **これでジャンルは8種**（救助0件）。救助を1件新設して9に戻す＝**判断待ち**~~ → ★ **2026-09-15・EX-104 で「救助」を「捜索」へ統合**（救助の新設は中止）。ジャンルは9種で、うち調査が EX-105/106 で1件増えた

- ✅ **EX-104：「救助」を「捜索」へ統合した**（commit `741e5e4` / `945ec39`）。★ **この世界に「急を要する救出」は成立しない**（知られる前に死ぬ／救助は常に捜索の結果にしかならない）。`GROWTH_STAT_BY_CATEGORY` の救助行・掛け合い／在席ログのキー・`defaultNewQuestRoleNote`・`FIELDWORK_TUNING.mapCategories` を捜索へ寄せ、**EX-103 で出た「探索の文へフォールバック」も解消**。★ **「9ジャンル一通り」は体験版の達成条件にしない**（9に設計上の根拠が無い）
- ✅ **EX-105：観察記録の既定を「対象の種別」で引く形にし、沈黙して落ちる穴を検査で塞いだ**（commit `c9b9df4` / `da94634` / `64df85f` / `19e97b9`）。裁定は **D＋E**。
  - **D**：依頼データに `observationKind`（図鑑の分類語）を1語足し、`OBSERVATION_KIND_NOTES`（獣／植物／既定）から引く。**専用分岐 → 種別既定 → 受け皿**の3段
  - ★ **本丸は `observationNotes: null` の直書き**。5か所を生成呼び出しへ直し、残る2か所（定型報告書・夜道の昼）は**意図と理由を明記**。さらに **`finalizeQuestReport` の既定 `null` も外した**（教会巡回・隊商護衛・捜索チェーンの3分岐が引数を渡しておらず、同じ穴が別の形で残っていた）
  - **E**：`scripts/check-observation-targets.js`（外部依存ゼロ）。①分岐も種別も無い ②種別が表に無い ③理由なしの `null` 直書き の3つを検出。**陽性対照で3種類とも検出することを確認**、現状は **0件**
  - 実測：★ **変わったのは納屋だけ**（観察記録360件・本文108件・行数44件・ハイライト286件が変化／他18依頼は11項目とも不一致0）。犬の禁止行動 違反0件
  - `CLAUDE.md` の「新ジャンル追加の最小差分ポイント」に **6番目「生成を呼ぶ・`observationKind` を付ける・検査を0件にする」**を追加
- ✅ **EX-106：初の「調査」依頼「森の際の見慣れない草」を追加**（commit `7fec3b9` / `af4a008`）。仮称 **「灰かぶりのような『なにか』」**／結末＝群生確認・標本確保・採取見送り（**未達は宣言しない**）／親＝森の薬草採集の**直後**に挿入／`observationKind: "植物"`／`fieldworkHumanOnly: true`（石碑の前例）。★ **依頼は全20件・掲示板の候補は18件**。追加後も**既存19依頼は 27,360組・11項目で不一致0**
  - ★ **命名システムの穴（仮称が図鑑の命名に接続していない）は今回対象外**。記録に残すだけ

- ✅ **EX-107：番号の指す対象を確定**（コード変更なし・コメントと文書のみ）。★ **EX-105＝観察記録の既定と穴塞ぎ／EX-106＝調査依頼の追加**。**作業の発生順に合わせる**（観察記録の穴が先に見つかり、その裁定を待って調査依頼を実装した）。番号は動かさず、**指す対象だけ**を確定。`_tasks` 対応表・`NEXT_TASKS.md`・`CLAUDE.md`・`CURRENT_SPEC`・`app.js` ほかのコメントを揃えた
  - ★ **調査依頼の文面は仮置きのまま**。必要な箇所の一覧を下の「受け渡し待ち」に出して停止

- ✅ **EX-108：調査依頼の文面を本置き**（commit `0fc49dc`）。あらすじ／道イベント4種×2文／結末3種×4文に加え、
  ★ **観察記録は種別の既定ではなく専用分岐**（`generateAshGrassNote`。固有名「灰かぶり」を文に出すため）、
  ★ **ハイライトも専用2文**（汎用の調査文が「書面の情報より少し違っていた」＝前情報がある前提で噛み合わないため）。
  **既存19依頼は 9,120組・10項目で不一致0**（警告3種も空）／観察記録は票を持たせた240回すべてで出る／道イベントの8文も全部出現
  - ⚠️ ★ **好奇心の段は現行5人では到達不能**（記憶→好奇心→既定の順。ミナが記憶5・好奇心4）。★ **到達不能は計11文**（灰かぶり2／畑2／夜道2／種別既定5。**森喰い兎は好奇心の段を持たない**）。直し方は (a) 順序 (b) この分岐だけ (c) 冒険者の値 の3つで、**どれも未裁定**
  - ⚠️ ★ **未達（引き返し）の昼の回にも専用ハイライトが返る**（実測 60件中6件）。事実の嘘ではないが語調が噛み合わない。**夜と戦闘は EX-092 で結末を見る形に直してあり、ここだけ揃っていない**。未達用の文が要るので文面側の裁定待ち
  - ⚠️ **未達の回（実測 16.7%）は共通の「引き返し」に落ちる**（`fail` を宣言していないため。裁定どおりの挙動）

- ✅ **EX-109：未達のハイライトと到達不能11文**（commit `2ddd2a0` / `e69a35c` / `c9289b8`）。
  ★ **裁定1＝未達用の2文を足した**（`GROWTH_TIER_BY_RESULT` が `fail` かで分ける。実測：引き返し194件中89件＝45.9%で返る）。
  ★ **裁定2＝(a) 順序が使えた**——好奇心の段を記憶より先に見る形に4箇所を直した。
  **記憶と好奇心はどちらも `pickOne` を1回だけ引くので乱数の消費は変わらない**。実測でも 9,600組・10項目のうち
  **動いたのは観察記録の文だけ**（480件。すべてミナが書いた回）。**(b) の案を出す必要はなかった**。
  ★ **裁定3＝アクセサリー・執着の行は既存の型のまま足せた**（文面の追加は不要）。★ **裁定4＝観察文6文を名前入りに直した**。
  - ★ **到達不能だった11文のうち8文が出るようになった**（灰かぶり2／畑2／夜道2／種別「獣」2）。掃引で実在を確認
  - ⚠️ ★ **残る3文は順序の問題ではない。** 種別「植物」と「受け皿」の表は**今どの依頼からも引かれていない**
    （唯一の植物依頼＝灰かぶりが専用分岐を持ち、種別の付け忘れも0件）。**表ごと未使用**で記憶・既定を含む計9文が出ない。
    ★ **これは穴ではなく備え**——専用分岐を持たない植物依頼を足せば、その時点で出る
  - ⚠️ **ブラウザ動線8項目は未実施**（EX-107 で「次に実機を触るときにまとめて」と裁定済み。EX-108 で報告書1本は実機で読んである）

- ✅ **EX-110：SESSION_STATE の写しを生成物にした**（commit `74f9865`）。`node scripts/gen-notion-session-state.js` の
  出力で**毎回まるごと置き換える**。変換は CURRENT_SPEC と共用（`gen-notion-spec.js` の `convert` を切り出した。
  ★ **spec 側の出力は生成日時の行を除いて1バイトも変わっていない**＝1,340行を機械比較）。
  - ★ **差分更新で溜まった言い換えは53箇所あった**（2026-09-16 実測）。**生成して置き換えれば一度に消える**
  - ★ **2026-09-14・EX-098 の「明示する」は歯止めにならなかった**——明示した分だけ溜まり続けた
  - ⚠️ **転記事故は生成では消えない**（貼るのは AI のまま）。⑥ で生成出力と読み直しを機械で突き合わせる
  - ★ **裁定2：種別「植物」「受け皿」の9文は未使用のまま残す**（消さない）。CURRENT_SPEC に「到達不能とは理由が違う」と明記
- ✅ **EX-110：ブラウザ動線8項目を実機で通した（8項目とも PASS）**。EX-106〜109 の変更を通しで確認
  （面接→ホーム→掲示板1件→遠征→即帰還→リザルト→報告書→名簿→リロード→赤エラー0）。
  - ★ **交戦ログの「Nダメージ」も現物で確認**（畑を荒らす「なにか」：「ミナが致命の一撃を受けた！（ミナに26ダメージ！）」）
  - ★ **成長も現物で確認**（ミナ 戦闘 12→15.6 ほか）。リロード後も報告書5件・記録係「カズ」が残る
  - ⚠️ **3600倍だと即帰還ボタンを目視できない**（8分の依頼が1フレームで終わる）。**60倍で確認した**
    ——2026-07-29 の「3600倍が偽の不具合を作る」と同じ形が、ボタンの有無でも起きる

- ✅ **EX-111：Notion 向けの変換を1本に寄せ、デバッグ速度の記述を現物に合わせた**（commit `b5a7014`）。
  `scripts/gen-rejected-index.js` の独自変換を捨て、`gen-notion-spec.js` の `convertInline` を require する形に。
  **生成3本（spec ／ session-state ／ rejected-index）が同じ「インラインの」変換を通る。**
  ⚠️ ★ **ブロックの変換（表のタグ化・見出しの繰り上げ）は索引だけ自前のまま**（コードブロックを出すようになったら片方に無い状態になる）。
  - ★ **一本化しても出力は変わらなかった**（3本を生成日時の行を除いて機械比較し**差分0行**）。
    ★ **今の内容に落ちていた記号が無かっただけ**で、これから書く文には効く（`` `+` `` のような1文字スパンは「+」になる）
  - ★ **`CLAUDE.md` 3.6 は「標準は60倍。不具合を見たら等倍でも確認」に書き直した**。
    ⚠️ **旧記述の「5秒固定／10秒固定」は現行の画面に無い**（速度は 等倍／60倍／3600倍 の3つだけ）
  - ⚠️ ★ **3600倍では「Mock用」ボタンを目視できない**（EX-110 で実際に取りこぼした）。
    2026-07-29 の「3600倍が偽の不具合を作る」と**同じ失敗モードがボタンの有無という形で再発しかけた**

- ✅ **EX-112：ブロック変換の扱いを決めた**（記録のみ・コードは1行も触っていない。commit は下の docs コミット）。
  ★ **いまは直さない。** 索引の `toNotion`（表のタグ化・見出しの繰り上げ）は自前のまま残す。
  ★ **着手条件＝索引がコードブロックを出すようになったとき、または表・見出しの形を変えるとき。**
  - ★ 理由：**今は壊れていない**（索引の出力は表と見出しだけで、spec 側と重なる要素が無い）。
    **壊れていないものを一本化しない**のは EX-086 の「性質でディレクトリを分ける」と同じ判断
  - ⚠️ ただし **EX-111 で塞いだ穴（インライン変換が2系統）と同じ構造がブロック側に残る**ので、
    「2系統である」事実と「いつ効くか」を DECISION_LOG に残した
  - ※ 写しが1行ぶん先行している件は**次の全面置き換えで揃う**。`~~~~` は既知の変換差分として記録済み

- ✅ **EX-113：命名システムの現状調査**（★ 調査のみ・コードもデータも1行も変更していない）。
  ★ **穴は3つで、どれも「今ある形のままでは命名を載せられない」形**：
  - ★ **図鑑のキーが「名前」そのもの**（`state.beastLog[対象名]`）。**仮称→正式名に変えると別キーになり、実機で再現した**：
    (A) 仮称で保存してから改名すると**2件に増える**（内容が同じ重複。報告書から開くと**古い仮称の側**が開く）／
    (B) 仮称で保存せずに改名すると図鑑は1件だが、**報告書から開くと空の新規フォームが出る**（そのまま保存すると結局2件）
  - ★ **「観察記録票の記入項目」は実装に存在しない。** 観察記録票は**支給品**で、書くのは冒険者（自動生成の散文）。
    ★ **図鑑まわりでプレイヤーが書ける欄は5つ**（名前／分類／遭遇地域／外見・特徴／備考。`docs/CURRENT_SPEC.md` の図鑑節に既出）。
    ※ 解禁条件の母数に何を使うかは裁定事項——ここは「今ある欄はこれだけ」という事実まで
  - ★ **答え合わせに使える「隠した語」は無い。** 報告書は散文で、`hiddenTags` が持つのは戦闘・成長・分岐の値だけ
    （`target` は仮称そのもの）。依頼データにも正体を示す語は無い
  - ⚠️ ★ **仮称の表記が揃っていない**：納屋は**同じ `data-quests.js` の中で割れている**——
    title と summary が「**噛**みつく」(U+565B)、`observationTarget` が「**嚙**みつく」(U+5699)。敵データ（`data-enemies.js`）も 噛。
    ★ **掲示板に出る題名と、図鑑のキーになる対象名が別字**。名前をキーにしている以上、この差はそのまま同定の失敗になる
  - ⚠️ ★ **同じ名前に改名すると、既にある別のエントリを黙って上書きする**（`saveBeastLogEntry` は無条件代入）。
    ★ **図鑑にエントリを消すUIが無い**ので、重複も上書きも**ゲーム内では取り消せない**（消えるのはセーブ全消しだけ）
  - ★ **図鑑を開く経路は3つ**（報告書詳細／報告メモの「図鑑を編集」／図鑑カードの「編集」）。**報告メモ経由でも空の新規フォームが出る**
  - ★ **掲示板カードの「観察対象」欄は `masterQuests` を直に出す**（`state.quests` は毎ロード作り直し）ので、
    **プレイヤーが付けた名前は掲示板には出ない**。どこまで命名後の名前を及ぼすかは裁定事項
  - ★ **`beastLog` は今どこからも読まれていない**（解放条件にも記録密度にも効かない）。「一定数埋まったら解禁」は読む側の配線が全部新規
  - ★ **分類の選択肢に「人」が無い**（現行11種）。分類の種（Notion）は「分類欄に『人』を追加する」と書いている
  - ⚠️ **セーブの扱いは片側だけ見ると重く出る。** 確かに `STATE_SCHEMA_VERSION` を上げると移行せず初期化されるが、
    ★ **`docs/CURRENT_SPEC.md` に「配布ビルドを作る前に必ず個別移行関数方式へ切り替える」が 2026-07-29 に確定済み**
    （身内向けが「配布」に入るかは未確定）。さらに **schemaVersion を上げずに移し替えた前例が3つある**
    （旧アイテム形式の正規化／**図鑑の旧フィールドを備考へ統合**／読了ハンコの追加）。**＝キーを変える＝セーブを捨てる、ではない**
  - ★ **本文・履歴に仮称が残ることは穴ではない**——**2026-08-03 に「過去ログは当時の呼び方のまま残す」が確定**し、
    **「遡って書き換える」は却下済み**（`docs/DECISION_LOG.md`）。穴なのは**図鑑エントリが二重化・上書きされる側だけ**

- ✅ **EX-115：図鑑の項目の現状調査**（★ 調査のみ・コードもデータも1行も変更していない）。
  - ★ **5欄の実体**：名前＝`input`（1行・文字数制限なし）／分類＝`select`（**11種**）／遭遇地域＝`input`／外見・特徴＝`textarea`（rows=2・**改行は保存される**）／備考＝`textarea`。
    **検証は「名前が空でなければ保存」だけ**で、他は全部空でも保存できる（実機で確認）
  - ⚠️ ★ **保存はオブジェクトを毎回作り直す**ので、**5欄＋旧5欄しか残らない**。
    **知らないキーは保存で黙って落ちる**（実機で確認）＝項目を足すなら `saveBeastLogEntry` の書き換えが必須
  - ★ **旧5欄の痕跡が残っている**（行動／危険性／有効な対処／効かなかった対処／次に確認したいこと）。
    読み込み時に**備考へ統合**して表示し、保存時に空へ戻す（2026-07-06 の初回コミット時点で既にこの形＝**この repo より前の版にあった欄**）
  - ★ **2026-07-26 に削除された observations 系統の項目は git から復元できる**（`31c2d19`）：
    `id / name / category / testimony（証言）/ facts（確認された事実）/ inference（推定）/ next（次に調べること）`
  - ★ **観察文は全部で51文、うち到達可能は47文**（兎18のうち**4文は死蔵**＝`REVIVE_MID_MEMORY_NOTE = false` で止めた分岐。
    到達可能は 兎14／畑6／夜道6／灰かぶり6／種別既定15。**兎14文は `docs/CURRENT_SPEC.md` の記述とも一致する**）。
    ⚠️ **初出の「50文・兎17」は誤り**——文字列の抽出が `adv.personality === "慎重"` などの二重引用符とペアがずれて1文落としていた（セルフレビューで発覚し、字句で数え直した）。
  - ★ **到達可能47文が触れている軸**（1文が複数に跨る）：
    **動き・行動24／生息環境20／持ち物への被害14／大きさ13／形・質感11／対処と次に確認10／色10／痕跡8／匂い7／危険性6／食性5／天候2／数2／音2／時間1**。
    ⚠️ ★ **初出の「食性14」は誤り**——「袋」で当たっていただけで、中身は**荷物を狙う行動の話**だった（餌・薬草・食べ物・空腹に触れるのは**5文**）。
    ★ **「持ち物への被害」と「天候」は初出の表に無かった軸**。**薄いのは 時間1／音2／数2／天候2**
  - ★ **Notion の元設計（UI画面設計／観察記録システムとUI設計 `39aa8a5dfd458198bf08f3084136d6ce`）は未実装の構想**と冒頭に明記。
    テンプレートは3種（通常の魔物／怪異／「それ」専用）で、**段階選択は危険度・記録確度・認識汚染度の3つ**、他は自由記述。
    ★ **同じページに「26の観察項目」の一覧がある**（基礎メタデータ7／対象の特定と外形5＝名称・**仮称**・分類・個体数・外見的特徴／
    生態的相互作用9＝行動・痕跡・鳴き声/音・匂い・食性・巣/棲み処・移動経路・他生物との関係・人間への影響／メタ認知と課題5＝標本・**確度**・不明点・推測・次回調査事項）。
    ★ **「仮称」と「確度」は EX-113 の穴（改名で二重になる）と直結する**
  - ⚠️ ★ **欄を1つ足すには3〜4か所を同時に触る**：`beastLogEditorHtml`（欄）／`saveBeastLogEntry`（読み取りキー）／
    **`beastLogCardHtml`（カードの表示。忘れると「保存されるのに図鑑に出ない」欄が沈黙して通る）**／旧欄と同じ扱いにするなら `openBeastLogEditor` の統合ブロック
  - ★ **`state.beastLog` を読むのは図鑑・報告メモの5か所だけ**（進行にも判定にも効かない表示専用）。項目を増やしても他へ波及しない
  - ⚠️ ★ **名前に `'` を入れるとカードの「編集」が壊れる**（onclick 属性に値を埋めているため。`\` は別キーの新規になる）。**自由入力の命名を載せると今日でも踏む**
  - ※ 細かい実体の訂正：**`rows=2` は HTML に書かれていない**（DOM の既定値。高さは CSS の `min-height: 72px`）。
    **改行が画面で見えるのは備考だけ**（`white-space: pre-wrap` が備考にしか付いていない）。名前が空白だけのときは**何も出ずに閉じもしない**

- ✅ **2026-09-17：命名と図鑑の裁定5件を記録**（★ チャット側が出した裁定。**実装はまだ入っていない**）。
  - ★ **図鑑は初遭遇で「枠」だけ現れる**（仮称＋遭遇地域のみ。**分類は入れない＝分類はプレイヤーの仕事**）
  - ★ **初遭遇の判定＝プレイヤーが報告書を読んだ時点**（読了ハンコ）。帰還と同時にしない
  - ★ **読了ハンコを押すまで報告書を閉じられない＋未読が机に見える**（A＋C）。**既読の読み返しは自由**
  - ★ **命名の解禁条件は「記入項目を実装する」**——2026-08-06 の裁定は**記入項目が無くて成立していなかった**。
    **条件を差し替えず、足りないほうを作る**
  - ★ **図鑑を SOAP 構造へ**（証言／確かめられたこと／推測／次に確かめたいこと。入力は軸を選んで自由記述）
  - ★ **削除済みの observations 系統が SOAP そのものだった**（`testimony/facts/inference/next`）。
    **消した理由は構造ではなく自動生成だったこと**（`31c2d19`）＝**構造は正しかった**
  - ★ **出どころは Notion「UI画面設計／観察記録システムとUI設計」**（Drive 由来・SOAP と MNAR の調査。冒頭に「未実装の構想」と明記）

- ⚠️ **2026-09-17：却下案索引の Notion 側に87行の欠落が見つかり、貼り直した**（生成物なので repo は無傷）。
  - ★ **原因は「大きい表を1回で投入すると行が黙って列に畳まれる」**——82行の表で**1行目が後続69行ぶんのセルを吸い込み279列**になり、**セルが1個消えた**。同じ作業で2回発生（149行の表は無事＝**サイズ順ではなく断続的**）
  - ★ **対策は小さく刻むこと**：ヘッダ＋数行の表を作り、`update_content` で直前の行を anchor に 20〜25行ずつ足す（`insert_content` は表の外に別ブロックを作るので使わない）
  - ★★ **検証は行数だけでは足りない。** 行数（315行）は合っているのに列が崩れていた——**列数まで数える**
  - ★ **単独のバックスラッシュが書き込みで食われる**ことも実測（写しで1文字欠けていた）。**これは変換差分ではなく直す対象**
  - session-end の写し（`.claude/skills/session-end/SKILL.md`）の ⑤-d・⑥・既知の落とし穴に反映済み。**マスターはチャット側**

- ✅ **EX-116：名前の記号でカードが壊れる件を塞いだ**（commit `6246dbc` / `26b7c78`）。
  `escapeJsArg` を新設し、**JS のエスケープ → HTML のエスケープ**の順で onclick の文字列に値を埋める。
  - ★ **修正前は実際にコード実行に至る形だった**（`');alert(1);//` という名前で成立）。`'` は「編集」が死んで**その記録を二度と開けない**、`\` は**黙って別キー**になっていた
  - ★ **塞ぎ残しなし**：`on〜="` は全35箇所、値を埋めているのは17箇所。通していない4つは**定数のみ**（気質キー／時間帯／天候／速度）。属性値（`title` `value` など）と本文は元から `escapeHtml` 済み
  - ★ **振る舞いは不変**：データ全件1,524文字列で `escapeHtml` と出力差0／報告書は 300件・6項目で不一致0（`node --check` と観察記録の検査も通過）
  - ⚠️ **図鑑に削除の導線は無い**ので、削除の検証は該当なし。**改行入りの名前は1行入力が改行を落とすので作れない**
  - ⚠️ ★ **保存データを手で編集して改行入りの名前を入れた場合は、編集画面が改行を落として別キーになる**（`<input>` の仕様）。**図鑑のキーを id 化する理由がもう1つ増えた**（裁定待ち）

## 現在の進捗（2026-09-17 追記）

- ✅ **EX-113：命名システムの現状調査**（調査のみ・commit `2ef6589`）。★ **図鑑のキーが対象名そのもの**なので、仮称→正式名で**二重になる／空の新規が開く**を実機で再現。★ **「観察記録票の記入項目」は実装に無い**（観察記録票は支給品で、本文は自動生成）。★ **答え合わせに使える「隠した語」も無い**
- ✅ **EX-115：図鑑の項目の現状調査**（調査のみ・commit `291792d`）。5欄の実体（input2／select11種／textarea2）と、★ **知らないキーは保存で黙って落ちる**ことを確認。削除済み observations 系統の項目（証言／確認された事実／推定／次に調べること）＝**SOAP そのもの**を git から復元。観察文は **51文・到達可能47文**
- ✅ **EX-116：名前の記号でカードが壊れる件を塞いだ**（commit `6246dbc` / `26b7c78` / `17834c7`）。`escapeJsArg` を新設し、★ **JS → HTML の順**でエスケープして onclick に値を埋める。塞ぎ残しなし（値を埋めている17箇所すべて。残る4つは定数のみ）。★ 修正前は `'` で編集が落ち、`');alert(1);//` は**実際にコード実行に至る形**だった
- ✅ **EX-117：図鑑を id キー＋SOAP 構造へ作り直した**（commit `0d826cd` / `8b1cbe1` / `e3dc190`）。
  - ★ **キーは id**。名前は表示用の値で、仮称（`target`）は不変、命名で入るのは `name` の側。**旧セーブは読み込み時に移し替え**（`schemaVersion` 据え置き）。★ **改名で別ページが生えなくなったので削除UIは作らない**
  - ★ **SOAP**：S＝観察記録票で溜まった文（自動）／O＝**12軸**から選んで自由記述／A／P。**備考は廃止**し、外見・特徴は O へ。移行してきた旧記述は**軸へ自動で振らず**「移行前の記述」欄に置く
  - ★ **枠は読了ハンコで現れる**（開封では現れない。読了済みの古いセーブには読み込み時に立てる＝冪等）。枠に入っているのは**仮称と遭遇地域だけ**
  - ★ **同じ軸に複数件を許した**（回を重ねた観察を上書きさせないため）。⚠ `observationNotes` の **`null`（対象がいない）と空配列（記録票なし）を分けた**——分けないと枠の可否が判定できなかった
  - 検証：400組・6項目で**結末・あらすじ・行数・本文の指紋・成長段が不一致0**（差は意図した obs の45件のみ）
- ✅ **EX-118：命名の解禁と確定印**（commit `8a797a4` / `b0c41f4`）。
  - ★ **解禁＝O の軸の種類が12軸中7つ以上**。**件数では数えない**（同じ軸に3件書いても1軸）。「移行前の記述」は数えない。⚠ **7は暫定**
  - ★ **確定印で確定し、以後は名前を編集できない**（読了ハンコとは別の判子）。確定前は下書き（`nameDraft`）として何度でも書き直せる。★ **確定印の前に確認を1回出す**（取り返しがつかないため）
  - ★ **解禁と確定済みの判定は画面と関数の両方で見る**（コンソールから直接呼んでも通らない）
  - ★ **答え合わせは実装しない**（隠した語が無く、作ると全依頼の文面に照合対象を埋め込む作業になる）。2026-08-06 の「めちゃくちゃなことを書いてOKにしない」は**保留**
- ✅ **チャット側の裁定を DECISION_LOG に記録**（2026-09-16〜17 の計3エントリ）。図鑑は初遭遇で枠だけ／初遭遇＝読了／読了まで閉じられない＋未読が机に見える／解禁条件は記入項目を実装する／SOAP へ作り直す／**5欄の統合と備考の廃止**／**12軸の目的は通常と反ミームで逆向き**（同じ軸を使い、2系統に分けない。反ミーム専用の雛形の余地は残す）／**名前は題名まで及ぼす・参照化は段階分け**／キーは id
- ✅ **Notion 向けの既知の変換差分をもう1つ見つけた**：★ **太字が行をまたぐと Notion 側で太字が落ちる**（`**A\nB**`。本文の文字は欠けない）。★ **書く側で避けられる**ので、`**` は1行に収めること。session-end の写しに追記済み

## 現在の進捗（2026-09-18 追記）

- ✅ **EX-119：名前の参照化 第一段**（`425b3cb` / `0991e57`）。確定した名前が**これから表示される**題名・掲示板（題名／観察対象欄／待機中の帯）・遠征中・編成画面に及ぶ。★ **依頼データは書き換えず、描画のたびに引く**ので、保存済みの本文・メモ・履歴は当時のまま。★ 仮称を題名に含む依頼は**3件**（畑／夜道／納屋）。⚠ 納屋だけ題名の「噛」と対象の「嚙」で字が違うので**表示だけの別名**で吸収した
- ✅ **EX-120：報告書の見出しを仮称に戻した**（`a295be7` / `d269232`。4箇所）。★ **理由：命名前の報告書で見出しだけ確定名にすると、同じ紙の上で呼び名が割れる。** 第二段でも揃わない（第二段が及ぶのは**これから生成される報告書**で、過去の報告書は当時のまま）。★ 確定名が及ぶのは**「今の状態」を出す画面だけ**
- ✅ **EX-121：名前の参照化 第二段**（`be85839` / `88b1336`）。**命名後に生成される報告書**が本文・観察文・交戦ログ・観察記録票まで確定名で揃う（生成の出口で置き換えて保存）。★ **命名前の報告書は1文字も変わらない。** ★ 置き換えるのは**本文が実際に使っている呼び名**（畑・納屋は「なにか」、夜道は「灯り」）——⚠ 仮称そのものは本文にほとんど出ない（夜道の「残る灯り」は全70行を洗って**0回**）
- ✅ **EX-122：あらすじの「正体はまだ不明」は命名後も残す**（`399b229`）。★ **名づけは同定ではない。** 答え合わせを実装していないこととも整合する。CURRENT_SPEC の申し送りを「裁定済み・残す」に書き換えた
- ✅ **EX-123：「いきもの図鑑」→「生態目録」に改名**（`44b28a2` / `5a14c46`）。画面の文字列8箇所＋表示されるデータタグ1件＋コメント。★ **報告書 240本を走査して生成文に「図鑑」「目録」は0件**＝改名は描画側だけに及び、**過去の報告書は1文字も変わらない**（旧ビルドのセーブで JSON 一致を確認）。★ **冒険者は生態目録に載せない**（名簿と二重になる／冒険者は名乗っているので命名の対象外）
- ✅ **EX-124：納屋の敵に短縮名を足した**（`f08e66f` / `fc461a1`）。`shortName: "「なにか」"`。★ 交戦記録の「相手」が消え、本文と同じ呼び名で**一枚として揃う**（実測：納屋 40/40 本・208行 → 0行）。★ 漢字を使わない形なので噛／嚙の表記ゆれを踏まない。検証：`Math.random` を固定して測った**結末分布は完全一致**、400組は `body` の20件（＝納屋ぶん）だけが動いた
- ✅ **EX-125：内部名とセーブのキーを生態目録へ揃えた**（`f0454cd` / `b971a59`）。`beastLog`→`ecologyRecord` ／ `bl_*`→`er_*` ／ `BEAST_LOG_*`→`ECOLOGY_RECORD_*` ／ ルート名 `beastlog`→`ecology` ／ eyebrow も `Ecology Record`。★ **旧キーから読んで落とし、id 接頭辞も `bl_`→`er_` へ振り替える**（`schemaVersion` 据え置き）。検証：旧セーブ4件が中身そのままで4件移行／EX-117 前の名前キー形式も2件→2件／400組・6項目で不一致0／動線8項目／インライン `onclick` 26個すべて定義済み
- ✅ **タイトル案を記録**（チャット側が種ページへ書いたものの写し）。「西端ギルド記録員■■■■著：未踏地域の生態目録について」。★ **■■■■ に意味を与えないことが決定そのもの**
- ⚠️ **Notion の CURRENT_SPEC に、位置・入れ子だけのずれが3箇所たまっていた**（EX-119 でローカルの並びを patch しやすい形に入れ替えたときの取り残し2件＋差分更新で入れた行の入れ子1件）。★ **`update_content` は検索置換なのでブロックの移動・入れ子変更ができない**ので、2026-09-18 の session-end で**全置換**して解消した
- ⚠️ **全角数字の打ち間違いが2回続いた**（EX-124 の「５」・EX-125 の「３」）。★ **`old_str` が当たらず「No matches」で弾かれるので害は無い**が、既知の注意として session-end の写しに1行残した


## 現在の進捗（2026-09-20 追記）

- ✅ **EX-128／EX-129：実機のフリーズの原因特定**（`524b629` / `0027ded`）。ヘッドレスでは 916,110 試行しても再現せず、**診断版（`?diag=1` の `diag.js`）を配信して実機で1回踏んでもらって確定**した。原因は **TDZ**——`FIELDWORK_TUNING` の宣言より前に走るトップレベルの `render()` が、遠征中のまま時間が過ぎたセーブで完了処理に入って `ReferenceError` を投げ、**app.js の評価がそこで止まる**（以降の定数が永久に未定義・`setInterval` だけ生き残って毎秒同じ例外）
- ✅ **EX-130：起動の2文を app.js の末尾へ**（`4ed2b87` / `8ea5544`）。★ **位置の問題は位置で直す。** 宣言を全部評価してから初回描画する形になり、TDZ が構造的に消えた。⚠ **背面復帰（完了判定が毎秒の再描画にぶら下がっている件）は着手条件つきの未解決**
- ✅ **EX-131：最初のクエストの初回強制完遂**（`0e411b6` / `0a2ceab`）。2026-08-06 の裁定が**未実装のまま残っていた**もの。`firstRunNeverFails` が立つ回のうち**その依頼を一度も行っていないとき**だけ未達を完遂へ。★ 負荷の数値は触っていない
- ✅ **EX-133：最初のクエストを 20秒→10秒**（`3de8bb2` / `06ef365`）＋掲示板カードの項目の調査
- ✅ **EX-134：所要時間は全依頼で実時間だけ／危険度の呼び名を一本化**（`ea2f3d8` ほか）。`realDurationOnly` の旗は削除。★ `danger` が何を表す値なのかは**着手条件つきの未解決**
- ✅ **EX-135：掲示板の観察対象は遭遇後だけ**（`1429bff` / `13594cd`）。判定は `state.reports` の `observationNotes`（EX-117 の3値）から導く＝**state を増やしていない**。★ 「なし」の依頼は遭遇が起きないので**遭遇後も出ない**
- ✅ **EX-136：診断版を消し、配信のキャッシュ避けを自動で付けた**（`b3555bb` / `9bf4561`）。★ **版は配信元の ETag／Last-Modified をそのまま使う**ので手で書き換える箇所が無い。画面左上に配信された `app.js` の版を出す
- ✅ **EX-137：同時遠征の影響範囲調査**（`946bf93`・調査のみ）。★ **前提の訂正——同時遠征は設計として存在し、実装が追いついていなかった。** 1本前提だったのは12種
- ✅ **EX-138：同時遠征（直列3段）**（`86c265c` ほか6コミット）。① **報告書の乱数を出発時の種だけから作る**（検証の前提そのもの。分布は動かず、**CURRENT_SPEC の基準値は 2026-07-31 の値が現物と食い違っていたので測り直して差し替え**）② **報告書 id に乱数の接尾辞**（200本生成で旧は19個しか別でなかった）③ **同時遠征**（裁定 a=A／b=A）
- ✅ **EX-139：同時遠征の本数の上限を外した**（`83f20bd` / `26ef096`）。★ **可能だが推奨しない行動は縛らない**（`00_shared` の「ゲーム設計の根幹」1件目）。実質の上限は人数で、現行5人なら**最大4本**
- ⚠️ **欠番**：EX-132 は採番されていない（指示が EX-131 から EX-133 へ飛んだ。EX-114 と同じ扱い）
- ✅ **4回目のタスク棚卸し（session-end ⑦。前回は 2026-09-12）**。`_tasks/進行中` は **5件**。★ **EX-128（最初のクエストで遠征が完了しないフリーズ）は完了している**——原因は TDZ で、EX-129 で特定し EX-130 で修正済み（DECISION_LOG 2026-09-18／commit `4ed2b87`）。**進行中に残っているのは置き場のずれ**で、移動は裁定後に行う。残る4件（EX-006／EX-032／EX-049／酒樽の保留）は**未着手（意図的）**で、重複なし。
  - ★ **EX-032 は前提が一部変わっている**——骨格は 2026-09-12・EX-086 で `tools/sweep.js` に入り（commit `3611d2a`）、「**使い捨ては scratchpad に書いて捨て、残すのは骨格だけ**」と裁定済み。**残っているのはシミュ/描画分離**（`app.js:73` が DOM を触るため Node 単体で読めない）。
  - ⚠️ **逆方向**（完了済みに完了時の作業が残っていないか）：完了済みの最新は **EX-070** で、EX-071 以降はタスクページ自体を作らない運用（2026-08-04 のゆるめ1）。**完了済み側に残件なし**。★ **番号→UUID 対応表との突き合わせは今回していない**。

## 現在の進捗（2026-09-22 追記）

- ✅ **EX-142【3】：冒険者選択と名簿で性格を3つとも出すようにした**（commit は下記）。★ **全員 `traits` 3つ**（2026-08-06 の裁定が未実装だった。それまでは人間が気質1つで、**気質を持たないエルシーだけ `traits` に落ちていた**）。★ **欠点の印は点線の枠と色**——詳細画面が既に使っていた形を広げただけで、**数字にも通知の見た目にもしていない**。欠点は必ず最後で、読み方は詳細に1行。★ **気質は画面から外したが、判定には今までどおり効く**。
  - ⚠️ **欠点の語彙の向き（潔癖症＝整える＋ など）は未着手**／**性格の統合（`personality` 廃止）も確定済み・未着手のまま**。
- ✅ **EX-142：配信の運用を戻し、受付嬢が名乗るようにした**（commit `a83bbdc` ほか・PR #78 をマージ）。★ **完了したらその場で main へマージして配信する**（session-end まで溜めない。溜めた結果 EX-140・141 が実機に出ておらず、画面左上の版が EX-139 のままだった）。★ **session-end は記録の反映に専念する**（④-b の「main が現HEADを含む」の確認はそのまま残す）。
- ✅ **掃除の定型報告書①が受付嬢の最初の名乗りになった**——「記録は僭越ながら、**私、アルメナが**つけております。」。★ 新しい演出を足さず、**既にある1行を1語だけ変えて成立**した（掃除は序盤の2件目で必ず読まれ、彼女が書く唯一の報告書）。
- ⚠️ **運用の変更は `CLAUDE.md` に書いた。`.claude/skills/session-end/SKILL.md` のマスター（Notion `02_operations/スキル：session-end`）にも反映が要る**（写しだけ直すと次の配布で消えるので触っていない）。
- ⚠️ **画面左上の版の文字列はこの環境からは読めない**（GitHub Pages への接続が egress ポリシーで 403）。配信の確認は `pages build and deployment` の run の成否で行い、**版が変わったかは作者の実機で見てもらう**。

## 現在の進捗（2026-09-21 追記）

- ✅ **EX-141：セーブを2層にし、手動セーブ「記録を綴じる」を入れた**（commit `75fb960`）。★ **綴じた記録は別キーに2冊**（画面に出るのは最新の1冊で、綴じるたびに前の1冊が裏へ下がる。**戻ると表の1冊を使い切り、裏が表に出る**＝沈みかけた盤面で綴じても詰まない）。★ **時計は「残り時間」で持ち、戻すときに現在時刻から組み直す**——そのまま戻す方式は実測で壊れていた（10分後に戻すと依頼が即完了し、重症の残りも減った）。★ 最初の綴じは**最初の報告書を読んだ直後**に受付嬢が促す。**Mock初期化は綴じた記録も消す**／**戻すときも `loadState` を通す**。
  - ⚠️ **戻す入口はいま Mock 用のボタンだけ**（加速中だけ出る）。**本番の入口＝運営不能の判定は金の実装とセット**。
  - ★ **未解決：戻すことは記録を消すこと**（下の未確定項目へ）。

- ✅ **チャット側が正本へ直接書いた記録を写しへ取り込んだ**（2026-09-21・session-end ②）。**3件**——①**体験版の序盤の流れ：祝い→掃除→6件解禁**（★ 酒場は就任祝いなので**2回目は文面が嘘になる**、という理由はこちら側にしか無かった。実装は EX-140【3】で完了済み）／②**支給品は隊商護衛のあとに解禁する**（観察記録票も含む。★ **未実装**で、下の未確定項目へ入れた）／③**辺境教会の周辺は「忌み地」**（正本は `00_shared`「共有世界設定」。⚠️ **そのページIDは `CLAUDE.md` に未登録**／「忘れられし地」とは別の場所／天使様企画への波及は未確認）。
- ✅ **EX-140：序盤の流れを作り直し、報告書の嘘を2件消した**（commit `f56433a` ／ `cd0ef1c`）。★ **酒場 → 掃除 → 6件**（酒場と掃除は**一度きり**・掃除は**20秒**・廃屋は達成数2件をやめて掃除へ付け替え・6枠目は**森の薬草採集1時間**）。★ **掃除の常駐エルシーはギルドにいる回だけ**出す（同時遠征が入って「別の遠征に出ている最中でも陽だまりで眠った」と書かれる形になっていた）。★ 名簿に漏れていた内部キー `carry_support_item` を日本語へ（**全画面を走査して漏れは他に0件**）。★ 掃除の定型報告書⑥は「着任したばかりで机にかじりつきの」へ（**2件目に出る依頼なので「いつも」が成立しない**）。
  - ★ **2026-07-27 却下案7（一度に6件＝掲示板が混む）を撤回した**——実測で、枠6に対するあふれは**現行 0→1→1→1→3→4→5→6→7 に対し、新しい流れは 0→0→0→1→2→3→4→5→6** と序盤がむしろ減る。**却下理由がこの形では成立しない。**
  - ★ **2026-08-06 却下案（廃屋を別の特定クエストに繋ぎ直す）も撤回**——却下理由「順路が塞がる」は、**掃除が全員必ず通る地点**なので成立しない。

## 解決済みの判断待ち（2026-09-21・EX-141 → **点0=A／点1=C／点2=A／点3=A／点4=A／点5=A／点6=A・小さな5点は推奨どおり**で裁定され、同日に実装済み・commit `75fb960`）

> 作業：EX-141（指示ブロックで実施・タスクページなし）。★ **調査と案出しのみ。コードは1行も変えていない。**
> 裁定済み：オートセーブは現行のまま／手動セーブを新設し「記録係が記録を綴じる」行為として物語化する／いつでも綴じられる／
> 面接後の最初の仕事＝最初の手動セーブ／★ **やり直しの代償は「綴じ忘れていれば遠くまで戻る」**。
> 測定条件：2026-09-21・HEAD `c84eec3`（origin と一致）。★ **2026-09-21 に全点が裁定され、同日に実装・検証まで完了した**（commit `75fb960`）。

### 実測（案の土台）
- 保存は**1キーに state 丸ごと**（`expeditionGuildLogMockV011`）。サイズは面接直後 23KB／報告書36通で 129KB（1通あたり約2.9KB）。**localStorage の上限は実測 約9MB**＝枠の数の制約にならない。
- ★ **state の「絶対時刻の錨」は5つ**：`expedition.startTime`／`worldState.expeditionAnchorStart`／`injury.injuredAt`／`missing.anchorStart`（`revealedAt`・`deadAt` も同じ入れ物）／★ **`missing.occurredAt`**（`app.js:806`。現在どこからも読んでいない死蔵だが、同じ入れ物にあるので使われた瞬間にずれる）。
- ★ **そのまま戻すと「やり直し」にならない**（実測）：遠征2本（10分・1時間）＋重症（残り30分）で綴じ、10分後にそのまま戻すと、**10分の依頼は即完了して報告書が1通増え、重症の残りは20分**になった。経過を打ち消して戻すと、遠征2本・重症30分がそのまま戻った。
- 金・借金・運営不能は**コードにもドキュメントにも1件も無い**（`data-quests.js:55` の「代金はギルドの財布から出る」だけ）。

### 未裁定（案を出して停止した点）
- ★ **点0：今回どこまで作るか**（綴じるだけ／Mock用の復元トリガまで置いて実機で通す）。★ **決める範囲そのものが論点**なので最初に置いた。
- 点1 枠の数（1つ／3つ／★見た目は1冊で裏に1冊を自動で残す）。
- 点2 綴じる操作の置き場（ホームのカード／topbar 常設／読了ハンコの直後）。
- 点3 最初の綴じをどこに置くか（面接直後／★最初の報告書を読んだ直後）と、必要な文面**7箇所**。
- 点4 運営不能時の画面（日時＋状況1行／日時だけ）。★ 点0 次第で保留。
- 点5 保存の持ち方（別キー／同じキーの中の `boundSave`）。
- 点6 時計の持ち方（絶対時刻をずらす／★残り時間で持つ／遠征は綴じた記録に含めない）。
- あわせて5点（★戻したとき消える報告書の扱い／`Mock初期化` は綴じた記録も消すか／戻すときも `loadState` と同じ移行を通すか／最後に綴じた日時の表示先／解放の巻き戻し）。

### ★ 主題に触れる点（見落とすと後から戻せない）
- ★ **戻す＝綴じたあとに書かれた報告書を捨てること。** 同日の EX-140【1】で「**報告書が本体**なので嘘を1行でも残さない」と決めたばかりで、**黙って消す形にすると主題と正面からぶつかる**。
- ★ **2026-07-27 の見直し条件が初めて発動する**——「`state.reports` から導出しているので、報告書を削除できるようにすると**解放が巻き戻る**」（`docs/DECISION_LOG.md:407`）。丸ごと戻すぶんには整合は崩れないが、**プレイヤーから見ると掲示板が巻き戻る**（開いていた6件が閉じる／一度きりの酒場・掃除が戻りうる）。
- ⚠️ **1枠＋上書きだけにすると「詰む」形が作れる**（沈みかけた盤面で綴じると、唯一の戻り先がその盤面に置き換わる）。裁定が定めた代償は「遠くまで戻る」であって「詰む」ではない。

## 解決済みの判断待ち（2026-09-21・EX-140【3】→ **裁定1＝A ／ 裁定2＝B** で裁定され、同日に実装済み・commit `f56433a`）

> 作業：EX-140（指示ブロックで実施・タスクページなし）。**【4】（掃除の定型報告書⑥の差し替え）は実装済み**（commit `cd0ef1c`）。
> ★ **2026-09-21 に裁定** → 裁定1＝**A（6枠目は森の薬草採集・探索1時間）** ／ 裁定2＝**B（廃屋は掃除へ付け替え）**。どちらも実装済み。
> ★ **【1】【2】は同じ日に再送されて実装済み**（EX-140 の最初のブロックが届いていなかった。EX-132 と同じ形だった）。
> 測定条件：2026-09-21・HEAD `cd0ef1c`（origin と一致）。★ **「一度きり」を実装した前提**で数えている（未実装だと酒場が掲示板に戻るので数が変わる）。

### 作者の裁定で決まっている分（【3】差し替え）
- 「隣の酒場に買い出し」＝**一度きり**（10秒のまま）／「ギルドの掃除」＝**一度きり・20秒**（現在1分）。
- **掃除を終えると掲示板に6件が解禁される。** 流れは 酒場 → 掃除 → 6件（外へ出す → ギルドの中を知る → 世界が開く）。

### 裁定された点（案を出して停止していた）
- ★ **6件の中身。** 5件は案A・案Bで同じで、**違うのは6枠目の1件だけ**だった。
  - 共通5件：廃屋の片付け（生活1分）／結婚式の手伝い（生活10分）／夕市帰りの親子の付き添い（護衛10分）／夜道に残る灯りの調査（戦闘・危険高・10分）／古い小橋の応急修理（保全30分）
  - **裁定＝A（森の薬草採集・探索1時間）** ／ 却下＝B（古い道標の確認・探索30分）
- ★ **廃屋の解放条件** → **裁定＝B**（`unlockedBy: "quest_guild_cleanup"` へ付け替え）。

### この変更に含まれていたこと（「据え置き」ではない）
- ★ **結婚式・夕市・夜道の3件は、親を `quest_tavern_errand` → `quest_guild_cleanup` へ付け替える必要があった。** 付け替えないと酒場クリアの時点で掃除と並んで出るので、**掃除を飛ばせてしまい流れが強制されない**。
- ★ **酒場クリア後の掲示板が「掃除1件だけ」になる**（従来は4件）。**1件だけの板が2回続く**のは意図どおり。
- ★ **廃屋の達成数2件は触らなくても開く**（酒場＋掃除でちょうど2件）。ただし 2026-08-06 が条件で表そうとした「どれを先にやってもよい」は、流れが一本化されるので**意味を失う**（だから裁定2で付け替えた）。
- ⚠️ **夜道は勝つと二度と出ない**（`reappearAfterCount` ＋ `battleOutcome === "victory"`）。6件のうち1件は恒久的に消えうる枠。
- ⚠️ **酒場の `firstRunNeverFails`（初回だけ未達を出さない）は、一度きりにすると「2回目以降」の分岐が到達不能になる。**
- ★ **枠あふれは新しい問題ではない**（実測）。現行も酒場クリア直後から1件あふれており（0→1→1→1→3→4→5→6→7）、案A は序盤のあふれがむしろ減る（0→0→0→1→2→3→4→5→6）。**EX-139 の「枠6は当面そのまま」を前倒しで踏む話ではない。**
- 掃除の20秒は**帯の新設が不要**（`near_20s` が EX-133 で潰さず残っている）。

## 解決済みの判断待ち（2026-09-20・EX-138-3 → **a=A ／ b=A** で裁定され、同日に実装済み・commit d190dae）

> 作業：EX-138（同時遠征・上限2本）／指示ブロックで実施（タスクページなし）。
> ★ **指示どおり「着手時に案を出して停止」した2件。** どちらも実装の形が変わるので、裁定前に書き始めない。
> ★ **2026-09-20 に裁定** → a＝**そのまま報告書の本数で数える** ／ b＝**チェーン系は同時1本まで**。どちらも実装済み。

### 【要裁定】EX-138-3-a：同時遠征でのクールタイムの数え方

- **論点**：クールタイムと再出現は「その回より新しい**報告書の本数**」で測っているので、2本が同時に帰ると一度に2進み、**待ち時間が実質半分になる**（実測：他3本が同時に帰ると `since` が一度に3進み、必要数1〜3を1回の波で満たす）。
- **A（推奨）**：**そのまま報告書の本数で数える。** 同時に出すほど掲示板が速く回るのを、同時遠征の利益として受け入れる（上限2本なので最大2倍）。
- **B**：**同時に帰った報告書を1と数える（波で測る）。** 待ち時間の意味を「行った回数」ではなく「掲示板を見直した回数」に保つ。
- **トレードオフ**：A＝実装0行・`state` も増えない／ただし2本出すと掲示板の回転が2倍。B＝待ち時間の意味が保たれる／ただし**報告書に波の通し番号という鍵が1つ増え**、旧セーブの報告書は波番号を持たない（＝1本ずつ扱いになる）。
- **却下済み**：**実時間・暦で測る案**（2026-09-13・EX-093 と 2026-09-15・EX-102 で「消化数で測る」と確定済み。EX-049 の確定事項「クールタイムは消化数1〜3」）。

### 【要裁定】EX-138-3-b：捜索チェーンの扱い

- **論点**：`state.searchChain` は単数。**護衛2本が同時に失敗すると2本目の起動が黙って捨てられ**（`!state.searchChain` のガード）、**捜索2本を同時に出すと stage が二重に進み `attachmentScore` が二重に引かれる**。
- **A（推奨）**：**チェーンに関わる依頼（隊商護衛・捜索2件）は同時に1本までとする。** 裁定2（同じ依頼の二重出撃を禁止）の仕組みをそのまま広げるだけで、`state` は増えない。
- **B**：**チェーンを配列にして、複数の隊商喪失を並行して追えるようにする。**
- **トレードオフ**：A＝実装が小さく、報告書から「どの隊商の話か」が一意に読める／ただし「護衛2本を同時に出す」遊び方は塞がる。B＝物語は厚くなる／ただし**緊急依頼の帯・stage・`sourceReportId`・ペナルティの数え方まで複数化が波及する**。
- **却下済み**：なし（この論点は今回が初出）。

## 未確定項目（着手条件つきの未解決・2026-09-21 時点）

- ★ **背面復帰**：完了判定が毎秒の再描画にぶら下がっている。**着手条件＝「背面から戻っても完了しない」が実際に観測されたとき**（2026-09-18・EX-130）。
- ★ **`danger` が何を表す値なのか**：**着手条件＝体験版の難度を詰めるとき、または「危険度を見ても判断できない」が実際に観測されたとき**（2026-09-18・EX-134）。
- ★ **掲示板の枠6**：当面そのまま。**着手条件＝冒険者の加入が実装されたとき**（7〜8人で最大7本になり、枠が窮屈になりうる。2026-09-20・EX-139）。
- ★ **`diag.js` を作り直すか**：EX-136 で削除済み。**着手条件＝実機だけで起きる不具合をまた追うとき**（作り方は EX-129／EX-130 の記録にある）。⚠️ 恒久で残すなら**赤帯がボタンのタップを奪う件**の手当てが要る。
- ★ **戻すことは記録を消すこと**（2026-09-21・EX-141）：「**報告書が本体**」（EX-140【1】）と緊張する。いまは「黙って消さない（戻す前に通数を見せる）」で止めている。**将来案＝戻っても読んだ報告書を「綴じられなかった記録」として残す。着手条件＝運営不能の経路を金の実装とセットで繋ぐとき。**
- ★ **支給品を隊商護衛のあとに解禁する**（2026-09-21・チャット側の裁定。**未実装**）：観察記録票も含めて、隊商護衛のクリア後に解禁する。体験版にもお金の概念を入れる。**着手条件＝金と在庫の実装に手を付けるとき**（保留中の「酒樽の代金を実際に減らす」と同じ塊）。★ 本編では図書館のイベント後に解禁条件を変える予定。
- ★ **タブを2つ開くと保存が後勝ちになる**：2026-07-30 に記録して以来**未対応**。同時遠征で**失われる状態が増えた**ので、扱いを決めるなら今後。

## 次にやること（次のセッションの最初の一手）

※ 中長期の積み残しは下の「次にやること」（旧来の一覧）を参照。

0-z. ★ **実機で同時遠征を触る**（2026-09-20・EX-139 のあと）。配信は反映済みなので、**画面左上の版が変わっていること**を確かめてから、2本以上を同時に出して進捗カードと帰還の行列の手触りを見る。
   ⚠️ **EX-130 の実機確認（タブを閉じて10分以上あけてから開く）も未実施のまま。** 同じ回で一緒に見られる。

0-a. ~~★ **ブラウザ動線8項目をまとめて通す**~~ → ★ **2026-09-16・EX-110 で実施（8項目とも PASS）**。次に実機を触るのは、また画面まわりを変えたとき

0. ~~★ **名前の参照化 第二段**~~ → ★ **2026-09-17・EX-121 で完了**（第一段は EX-119）。
   ★ **命名前の報告書は全部仮称、命名後の報告書は全部確定名。どちらも一枚の中では揃っている**——これが整合の条件。過去に生成済みの文字列は書き換えない。
   ★ 項目数が決まったら `ECOLOGY_RECORD_NAMING_AXES_REQUIRED` と CURRENT_SPEC を直す。⚠ **7軸は暫定**で、**反ミームに必要な項目数が決まったときに確定する**。
   ※ 第一段・第二段の内容は CURRENT_SPEC の「生態目録」節を参照。

1. ★ **戦闘依頼を工程エンジンに載せるか**（着手条件＝戦闘の作り直しをするとき）。★ **夜道まわりの未実装は 2026-09-14 に無くなった。**
   - ~~夜道 v2 の作り直し~~ → ★ **2026-09-13〜14 に完了**（EX-092〜095）
   - ~~包帯の判断への重み（−20）の裁定~~ → ★ **2026-09-13 に解決**（裁定A で EX-091 として −10 に）
   - ~~夜道 v1 を削除するかの判断~~ → ★ **2026-09-14 に完了**（EX-096。退避つきで削除）
   - ~~夜道の `usedItemIds` を接続するか~~ → ★ **2026-09-14 に「接続しない」と裁定**（EX-099）。
     ランタンは既に3経路で出ており、**接続の価値が「もう一度書く」だけなら樽の二重行（EX-071）を自分から作ることになる**。
     実測でも接続だけでは 1,440件で1文字も変わらない
   - ★ **依頼を消すときの手順が1つ増えた**（EX-097）：掲示板から消すだけでなく、**その id を指すセーブが残りうる**ことを前提にする（防御は入れたが画面には何も出さない）
2. ~~語彙の分布を厚くする~~ → ★ **2026-09-12 に打ち止め**（上の進捗）。第二段は保留のまま続ける。次に効くのは語彙側ではなく「戦闘依頼を工程エンジンに載せる」で、**着手条件＝戦闘の作り直しをするとき**
3. ★ **DECISION_LOG の「未解決」2件の着手条件は変わらず「工程数を動かすとき」**（丸めの段差／遠征あたり1回きりの支給品）
4. ★ **これからの検証は `tools/sweep.js` の上に書く**（EX-086）。同じ骨格を書き直さない。`lines` は要るときだけ true にする

## 解決済みの受け渡し（2026-09-15・EX-107 → EX-108 で文面が届いて実装済み）

> ★ **裁定は #4＝B（専用分岐）／#5＝ハイライトも足す**。#1〜#3・#7 は届いた文面をそのまま実装した（commit `0fc49dc`）。
> 以下は依頼したときの一覧（記録として残す）。

> 2026-09-15（JST）／EX-107（タスクページなし・指示ブロックで実施）。作業時点の HEAD は `3360edb` で origin と一致。
> ★ **文面はチャット側が書く**。ここは「どこに何文いるか」の一覧。**実装は文面が届いてから。**

### 依頼主が知っていること／知らないこと（文面の前提）

- **知っている**：場所（森の際の湿った窪地）／**見たことのない草が出ている**こと／村人が自分で見たこと。
- **知らない**：名前・種類・毒かどうか・いつから生えているか・どこまで広がっているか。★ **前情報がない**のが主眼で、
  依頼書に書けるのは「見たことがない」までしかない。⚠️ **依頼主は植物に詳しくない村人**なので、専門的な言い方をさせない。
- ★ **ギルド側も知らない**。だから仮称「灰かぶりのような『なにか』」で呼ぶ（**正式名が付くのは体験版の後**）。

### 文面が必要な箇所（現状はすべて仮置き）

| # | 場所 | 何文 | 現状 |
|---|---|---|---|
| 1 | 掲示板のあらすじ `data-quests.js` の `summary` | **1〜2文** | 仮置き1件（依頼名・地域・`tags` 4語・仮称もここ） |
| 2 | 道イベントの文 `app.js` の `roadEventText` | **新規4種 × 各2文＝8文** | 仮置き。★ 6種のうち「湿った足跡」「倒木」は既存の文を流用（書き直し不要） |
| 3 | 結末3種 `data-outcomes.js` | **3結末 × 各4文＝12文**（`summary`／`line`／`after`／`history`） | 仮置き。★ `line` には `{一行}`／`{薬草師}`／`{慎重}` の名札が入る |
| 4 | 観察記録の文 | **6文**（`memory` 2／`curiosity` 2／`base` 2） | ★ **いまは「植物」の種別既定を共有**している。専用にするなら分岐を1本足す（下記） |
| 5 | 今回のハイライト | **2〜3文**（足すなら） | ★ **専用分岐が無い**。⚠️ 汎用の調査文が「**書面の情報より少し違っていた**」＝前情報がある前提で、この依頼と噛み合わない |
| 6 | 冒険者ごとの履歴行 | **不要** | 調査カテゴリの既存4種（`defaultNewQuestRoleNote`）を流用。依頼ごとの履歴行は #3 の `history` |
| 7 | 工程の語彙 `data-vocab.js` の5行 | **不要**（文は出ない） | ★ `label` は報告書に出ず、担い手の選出にだけ効く。★ ただし**語の割り当ての妥当性は見てほしい**（進む／確かめる／察する／採る／記録） |

### #4 の選択（文面を書く前に決めること）

- **A：植物の種別既定6文を書き直す** — ★ **他の植物依頼にも効く**（いまは1件だけ）。この対象の固有名は出せない。
- **B：この対象の専用分岐を1本足す** — 森喰い兎・残る灯りと同型。**文数は自由**だが、
  ★ **`pickOne` を必ず1回だけ引く**という不変条件は守る（守らないと既存の乱数列が動く）。
- ※ A と B は排他ではない（B を足しても既定は他の植物依頼で使う）。

## 解決済みの判断待ち（2026-09-15・EX-105：観察記録の既定 → 裁定 **D＋E** で実装済み）

> 2026-09-15（JST）／EX-105（タスクページなし・指示ブロックで実施）。作業時点の HEAD は `2f0d48d` で origin と一致。
> ★ **裁定は D（種別で引く）＋ E（検査を1本足す）。A/B/C は却下**（C は 2026-08-01 の却下に当たる）。
> 実装も EX-105（`c9b9df4` ほか）。★ **番号の指す対象は EX-107 で確定**（EX-105＝観察記録／EX-106＝調査依頼）。**残っていた穴2つのうち「専用分岐の `null` 直書き」もここで塞いだ**（観察記録票を持たせないと出ない件は仕様どおり据え置き）。

### ★ 既定を直しても残る穴が2つ（先に知っておく）

1. ★★ **専用分岐を持つ依頼は `observationNotes: null` を直書きしている（7か所）。** 観察記録の生成を呼ぶのは **4経路だけ**。
   → **新依頼が専用分岐を持つなら、既定をどう直しても観察記録は1行も出ない。**
   ⚠️ `CLAUDE.md` の「新しい依頼ジャンルを追加するときの最小差分ポイント」にも**生成を呼べとは書かれていない**。
2. ★ **観察記録票を持たせないと1行も出ない**（所持者0人の時点で打ち切り）。**既定の見直しでは解決しない。**
   ⚠️ しかも生成側は許可リストを見ないので、**許可リストに入れ忘れても観察記録だけは出る**（消えるのは支給品の行のほう）。

### ★ 事実の訂正（当初の見立てが2つ違っていた）

- ⚠️ ★ **既存3分岐は形が揃っていない。** 畑＝memory / curiosity / 既定 の3段 × 各2文／
  夜道＝**4段**で caution 段と既定段は**1文固定**／兎＝**curiosity を読まず**、personality・background・job で分岐し、
  **冒険者名を文に入れない地の文**。→ **「既存と同じ形に揃える」という言い方はできない。**
- ★ **同じ穴は納屋1件だが条件つき。** 納屋は撤退段階1（`withdraw_first`）のとき観察記録を捨てるので、
  既定文が出るのは**「観察記録票を持たせた かつ 段階1以外」の回だけ**。

### 案

| | 中身 | 効き／欠点 |
|---|---|---|
| **D（推奨）** | **既定を「対象の種別」で引く**。依頼データに1語足す（例 `observationKind: "植物"`） | ★ **図鑑の分類語（獣／鳥／虫／植物／菌類／水棲／魔物／怪異／人工物）は既にコードにある。** 1語だけなので**条件式のデータ化にはならず**、C の却下理由に当たらない。★ **今回の対象が植物である以上これが決定的**——獣向けの文（跳ねた／逃げた／足跡）は草に当たらない |
| A | 既定を**対象名を差し込む汎用の観察文**にする | ⚠️ **獣・怪異・植物を1組の文で賄う**ことになり、**「何も言っていない文」に落ちやすい**（＝いまの既定と同じ失敗） |
| B | 既定の**1文だけを差し替える**（「初見のため、形と場所だけを書き留めた」等） | 最小差分。★ 本当の欠点は「全員同じ」ではなく**冒険者の性格を読まない＝人による差が出ない**こと |
| C | 観察記録の文を**依頼データ側へ宣言**する | ⚠️ ★ **2026-08-01 に却下済み**（下記）。再提案するなら何が違うかを言う必要がある |
| **E（併用）** | **検査を1本足す**（`scripts/` に外部依存ゼロ。犬の禁止行動の検査と同型） | 「`observationTarget` を持つのに分岐が無い依頼」と「生成を呼ばない専用分岐」を列挙する。★ **どの案を採っても、検査が無ければ次の依頼で同じ穴に落ちる** |

### どの案でも共通のコスト

- ★ **納屋の観察記録は必ず変わる**（A/B/C/D どれでも既定を通るため）。**要測定**——
  `tools/sweep.js` の既定の比較項目に観察記録が入っているので、「納屋だけ差が出て、兎・畑・夜道は不一致0」を機械的に示せる。
- ★ **既存セーブの報告メモは書き換わらない**（観察記録は**文字列ごと** `state.reportMemos` に保存済み）。
  → **メモ一覧に新旧の文が混在する。** ※ 2026-07-30 の「過去ログは当時のまま」とは整合する。
- ★ **これは振る舞いが変わる変更**なので**直列（一度に1つ）**。**新依頼の追加と同時に走らせない**
  （`CLAUDE.md` の「どちらも測定を伴い、基準値が動く」に当たる）。

### 却下済み

- ★ **案C は 2026-08-01 に却下済み**——「**依頼固有の34行を全部データに出す**：条件式を JSON に持ち込むことになり文章が書きにくくなる」。
  ⚠️ **EX-047 が移したのは結末の候補と結末名だけで、同じエントリが「描写はコードに残す」と明記している。**
  観察記録文は性格・職・前職の条件式に依存する**描写**なので、そのまま却下側に当たる。
- ★ **2026-07-26「自動追記型の観察記録（observations 系統）を一式削除」**（理由②＝コアコンセプト30章「記録係が自分で書く」と
  自動追記は合わない）。★ **抵触はしない**（今の観察記録は削除した系統とは別物だと CURRENT_SPEC が明記）が、
  **既定を厚くする＝自動生成の文を増やす方向**なので触れておく。
- ⚠️ ★ **「記録できなかったが既定なのは、対象が少なかった頃の形」は記録上の裏付けがない。**
  DECISION_LOG を全文検索しても経緯のエントリは無く、導入コミットも特定できなかった。**推定なので根拠には使わない。**

## 解決済み：CURRENT_SPEC（Notion 側の生成物）のずれ一覧（2026-09-15・EX-103 で調査 → 同日の session-end で全置換して解消）

> 生成物（`node scripts/gen-notion-spec.js` の出力）と Notion ページを、**既知の変換差分を正規化したうえで**突き合わせた。
> ★ **2026-09-15 の session-end（⑤-c）で Notion 側を全置換したので、下の9件のずれは解消済み**（EX-104 の裁定4）。以下は記録として残す。

- 差分ブロックは **28**。うち**19 は既知の変換差分で、ずれではない**：
  - `{名前}` などの波かっこが Notion 側で `\{ \}` とエスケープされる（6件）
  - **生成物の複数行が Notion 側で1行に畳まれている**（10件）
  - 箇条書きの先頭 `- ` が落ちている（3件）
- ★ **本当のずれは 9件**。うち**内容が古いものが3件**で、これが実害：
  1. ★ **夜道の節が v1 時代の記述のまま**（「共通経路に乗せない（2026-07-31 裁定）」。生成物は EX-092 の作り直し後の版）
  2. ★ **Notion にだけ「※ 以下は v1 時代の記述です」という手書きの注記がある**（生成物には無い＝人が手で足した）
  3. **`usedItemIds` の記述**が EX-099 の裁定（接続しない）より前の書き方
  - 残り6件は言い換え：「挙動」→「振る舞い」／「tags とは別の表」→「表示用のタグとは別の表」／
    「tags を判定に使うかは未決」の取り消し線の有無／空白の有無／生成日時が1回ぶん古い／再出現の宣言名が短縮されている
- ⚠️ ★ **2026-09-15 に自分が入れた差分更新（掲示板の節）も、生成物と完全には一致していない**（手順1の文が違う）。
  **差分更新は「生成物と同じ文字列」を作れない**という実例。
- ★ **判断材料**：全置換は**約54,000字を手で貼る**工程で、規約自身が「転記が壊す」と記録している（2026-08-01 に3件の転記事故）。
  一方で差分更新を重ねると上の9件のようなずれが積む。**どちらを取るかは裁定が要る。**

## 解決済みの判断待ち（2026-09-13〜14 分は DECISION_LOG へ移した）
★ **判断が済んだ節はここから消す**（session-end の取り決め。残すと複数の文書が現行を名乗る）。
2026-09-13〜14 に決着した3件——**包帯の重み（EX-091）／夜道 v2 の遭遇（EX-092）／夜道 v2 の昼ルート（EX-093）**——は、
設計時の記録も含めて `docs/DECISION_LOG.md` の 2026-09-13・2026-09-14 のエントリに入っている。

## 解決済みの判断待ち（2026-09-12・EX-088【2】→ 裁定A で EX-089 として実装済み・commit dd7bc8f）
★ **裁定A（死に行をそのまま削る）で決着。** B（内容で選んで残す）は却下——生きている行を抜くと不一致が出るうえ、★ **報告書の文は `label` を読まないので、惜しい行が消えても読者には見えない**（惜しさは書き手側の感情であって体験の損失ではない）。★ **削る21行は捨てず「退避」として残した。** 以下は設計時の記録として残す。

### 旧・EX-088【2】：語彙表を5行以下に削る案（2026-09-12・設計のみ）
作業したページ：指示ブロック（タスクページなし）。**データもコードも変更していない**（【1】【3】は実施済み・commit `5c8c1f5` / `6b17d8d`）。

- ★ **削るのは死に行そのもの。それなら不一致0が構造的に保証される。** 機械で確かめた——**死に行だけを抜くと、回る工程の並びが変わらない**（9依頼は工程数2/3/4すべてで不変。酒場は `fieldworkPhases: 2` 固定なので2工程で不変）
- ⚠ **これは一般の定理ではなく、現行の `phasesMin: 2 / phasesMax: 4` の下での実測結果。`phasesMax` を動かすとこの前提は壊れる**（実際、酒場を2行に削ると工程数3・4では並びが変わる。いまは到達しないだけ）

| 依頼 | いま | 後 | 削る行 |
|---|---|---|---|
| 辺境教会周辺の定期巡回 | 7 | 5 | 行2「花壇と祈りの跡に触れずに確かめる」／行6「灯りで段差を照らして確かめる」 |
| 森の薬草採集 | 7 | 5 | 行2「使える草と避ける草を見分ける」／行6「小さな獣を遠ざける」 |
| 古い小橋の応急修理 | 7 | 5 | 行2「傷んだ板を外し使える釘を選り分ける」／行6「橋下の桁を照らして確かめる」 |
| 廃屋の片付け | 7 | 5 | 行2「床板を踏んで確かめる」／行6「茶器を包む」 |
| 結婚式の手伝い | 8 | 5 | 行2「酒樽と長椅子を担ぐ」／行4「こぼさずに運ぶ」／行7「夜間の見回り」 |
| 薬草包みの納品 | 6 | 4 | 行2「濡らさずに運ぶ」／行5「荷紐を繕う」 |
| 帰ってこない薬草採りの確認 | 6 | 4 | 行2「笛を鳴らして返事を待つ」／行5「保護して連れ帰る」 |
| 古い石碑の拓本 | 6 | 4 | 行2「苔を削らず読める部分だけ写し取る」／行5「光を斜めから当てて浅い刻みを見る」 |
| 古い道標の確認 | 6 | 4 | 行2「表面を傷めずに読む」／行5「仮に留めて支える」 |
| 隣の酒場に買い出し | 4 | 2 | 行2「樽を受け取る」／行3「樽を担ぐ」 |

- **21行削って 102→81行**。★ **到達しうる行は 63 のままで増えない**。変わるのは**割合で 62%→78%**。★ **残る18行は工程エンジンを通らない4依頼**（畑・納屋・護衛・夜道）の行で、**この案の対象外**（別論点）
- ★ **報告書の文は1行も消えない。** `questSteps` の `label` を読むコードは存在しない（`masterVocab` を読むのは `app.js:7050` の `acts` と `7059` の冒険者だけ）。本文は `lifeQuestEventPools` などが別に持つ
- **失う語は延べ24**（〈運ぶ〉−6／〈察する〉−5／〈留める〉−4／〈確かめる〉−3／〈前に立つ〉−2／〈記録〉−2／〈伝える〉−1／〈手当て〉−1）。★ **いずれも今も一度も効いていない行**なので効き方は変わらない
- **A（推奨）**：死に行をそのまま削る（並び不変＝不一致0。どの行を残すかの判断が要らない）
- **B**：内容で選んで残す（惜しい行＝結婚式「酒樽と長椅子を担ぐ」等を残す）。★ **不一致が出る**——生きている行を抜くと残りの並びが変わる。「時系列が読める」は A でも保たれる（並びが不変）ので利点がない
- **却下済み**：2026-09-11・EX-076「`phasesMax` を 4→5 に上げる」（工程数と難度が結合しているため却下）

## 解決済みの判断待ち（2026-09-12・EX-087 → EX-088／089 に引き継がれて解決）
★ **EX-088（〈確かめる〉の新設）と EX-089（表の圧縮）で決着。** ★ **調査の結論「裁定Aの範囲では分布はほとんど厚くならない」はそのとおりで、実際に効いたのは付け直しではなく圧縮のほうだった**（死に行21行が消え、工程エンジンを通る14依頼の未到達行が0になった）。★ **語彙を厚くする作業はここで打ち止め**（上の進捗を参照）。以下は調査の記録として残す。

### 旧・EX-087：語彙の付け直し（2026-09-12・調査完了）
作業したページ：指示ブロック（タスクページなし）。**データもコードも変更していない**。測定は `tools/sweep.js`（8,400遠征）。

- **論点**：EX-081 の再開条件「語彙の分布を厚くする」を、裁定A（既存102行の付け直し）で満たせるか。★ **調査の結論は「ほとんど満たせない」**
- ★ **102行のうち39行（38%）は担い手の選出に一度も効かない**——工程エンジンを通らない4依頼（畑・納屋・護衛・夜道）の18行＋均等間引きの死に行21行
- ★ **付け直して実際に効く行は3行だけ**（L120 教会「距離を保って観察する」／L85 廃屋「使える物と処分品を見分ける」／L143 夕市「灯りで石段を照らす」）。**うち堅いのは L85 の1行**
- ★ **薄い語4つは既存102行では厚くならない**：〈火と食〉1行（他に火・調理の工程が無い）／〈採る〉1行（候補の薬草採集 行2 は死に行）／〈手当て〉5行中 到達2行（他に傷・介抱の工程が無い）／**〈離れる〉は工程に1行も付いていない**
- **A**：3行だけ付け替えて閉じる（分布はほぼ動かない）
- **B**：工程の文そのものを足す（表の行数が変わるので死に行の位置も動く）
- **C**：戦闘依頼を工程エンジンに載せる（〈攻める〉3行・〈手当て〉2行が一度に生きる。ただし戦闘の作り直し）
- **却下済み**：2026-09-12・EX-087 の裁定で B（依頼を増やして自然に厚くする）は今回採らないと決着済み

★ **副産物：到達しうる行は「表の行数」だけで決まる**（内容と無関係）。工程数が乱数2〜4のとき **5行以下＝全行到達／6行＝2,5が死ぬ／7行＝2,6／8行＝2,4,7**。例外は酒場だけで `fieldworkPhases: 2` 固定のため4行中 行2,3 が死ぬ。**死に行21行の内訳は 6行以上の9依頼で19行＋酒場で2行。**
★ **`data-vocab.js` の冒頭注記が古い**（11行目「この時点では判定に接続していない。読んでいるコードは無い」）。EX-074 で接続済みで、`app.js:7050` が `masterVocab.questSteps` を読んでいる。


## 解決済みの判断待ち（2026-09-12・EX-086 → 裁定A で実装済み・commit 3611d2a）
★ **2026-09-12 に裁定A（`tools/` を新設）で決着。** B（`scripts/` に置く）は却下——場所は1つで済むが、`scripts/` を流すのに playwright の導入が要る環境が出る。★ **線引き（外部依存の有無で置き場が分かれる）は `CLAUDE.md` に明記した。** 以下は設計時の記録として残す。

### 旧・EX-086：検証ハーネスの共通化（2026-09-12・設計のみ）
作業したページ：指示ブロック（タスクページなし）。コードもデータも変更していない（設計案のみ）。

- **論点**：43本の使い捨てハーネスの骨格を切り出すとき、**外部依存（playwright とコンテナ固有の絶対パス）を repo のどこに置くか**。現行 `scripts/` の11本は**全部が外部依存ゼロ**で、そこが唯一そろっている性質
- **A（推奨）**：`tools/` を新設して置く（`scripts/` を「クローンしただけで流せるもの」に保てる）
- **B**：`scripts/` に置く（置き場が1つで済み、EX-032 の名目に近い。ただし外部依存ゼロが崩れる）
- **トレードオフ**：A＝場所が2つに増えるが依存の有無で線が引ける／B＝場所は1つだが、`scripts/` を流すのに playwright の導入が要る環境が出る
- **却下済み**：なし（この論点での既出案はない）

★ 形は4本（`sweep`／`compare`／`tally`／`scan`）。★ **効きはパターンで大きく違う**——不一致0の比較 51→14行（−73%）／分布の測定 47→15行（−68%）／**文面の走査 70→45行（−36%）**。走査は正規表現の表19行が問いそのもので減らない。
★ **EX-032 は閉じない。** 未達項目2つ（`scripts/` のハーネスの DevTools 前提／CLAUDE.md への実行時間の追記）はどちらも EX-086 では解消しない。


## 解決済みの判断待ち（2026-09-12・EX-080 → 裁定C で決着）
★ **2026-09-12 に裁定C（前提が違う）で決着。** ★ **作業行と工程は別物であり、合わせにいかない**——A（主語を担い手に差し替え）も B（作業行の選択を工程に一本化）も採らない。**そもそも嘘になっていない**（本文は「誰がやったか」を工程の担い手として書いていない）。この裁定は `docs/CURRENT_SPEC.md` に明記した（commit `d5e30e3`）。以下は測定の記録として残す。

### 旧・EX-080：語彙が決めた担い手と、本文に出る人物のズレ（2026-09-12・調査完了）
作業したページ：指示ブロック（タスクページなし）。測定は読み取り専用で、コードもデータも変更していない。

- **論点**：工程の担い手（語彙×育成値で選ぶ）と、本文の作業行の主語（職・気質・前職で選ぶ）が**別々の仕組みで決まっている**ため、同じ工程を別人がやっている報告書が出る。結婚式の「厨房を手伝う」は担い手ガッドなのに、本文ではエルネが厨房に立つ（4人編成の一致率 10.7%）
- **A（推奨）**：本文の作業行の主語を、その工程の担い手に差し替える（文の骨格は据え置き、主語だけ担い手へ）
- **B**：本文の作業行の選択そのものを工程に合わせる（`lifeQuestEventPools.workEvents` を `masterVocab.questSteps` に一本化し、二重に持っている工程の名簿を1つにする）
- **トレードオフ**：A＝小さく効くが、役割前提で書かれた文（薬草師が薬草束を見る等）に別人を入れると文が壊れるので、文面の書き直しが要る／B＝癒着そのものが消えるが、本文の行数（3〜4）と工程数（2〜4）が揃うので**報告書の長さと体感が変わる**（現状 本数一致は 40.0%）
- **却下済み**：なし（この論点での既出案はない）

★ 現状維持も選べる。ただし**第二段（支給品の使用優先）を接続すると照合相手が消える**——道具が効いた工程の担い手と所持者が一致するのは **12.9%**（工程に紐づく666件中86件。古地図・油紙は工程ループの前で立つので phase を持たず、比較対象外）で、工程ログの道具の行は 1,060件すべてが人名を持たない。


## 解決済みの判断待ち（2026-09-12・EX-077 作業3 → 裁定A で EX-078／079 を実施）

★ **2026-09-12 に裁定A（対応付けだけ直す）で決着。** B（疲労を直して工程数を語彙に合わせる）は却下（理由3つは `docs/DECISION_LOG.md` 2026-09-12 のエントリ）。★ **残った癒着2つ（丸めの段差／遠征あたり1回きりの支給品）は「未解決・着手条件＝工程数を動かすとき」として DECISION_LOG に記録した。** 以下は測定の記録として残す。

日時：2026-09-11 19:17 JST／HEAD `b6d0e0c`。

- ★ **仮説「比率化後なら完遂率が崩れない」は外れ。癒着は3つあり、切れたのは1つだけ**：
  1. **丸めの段差**（切れていない）：完遂に許される滞りは 2〜4工程で0件、5〜8工程で1件。**一律4工程 58.4% ＜ 現行 70.4% ＜ 一律5工程 76.8% と逆転**し、疲労を切っても残る。★ **2〜4工程では新旧の閾値が同一なので、比率化はこの帯を助けていない**
  2. **疲労の累積**（切れていない）：1工程あたりの滞り率が 13.1%（現行）→ 25.7%（8工程）。止めると 9.4% で平ら
  3. **遠征あたり1回きりの支給品**（切れていない）：笛は滞りを1回打ち消す予算なので工程数が増えるほど薄まる。支給品なしの測定では見えない
- **工程数を語彙の行数に合わせると**：全体 完遂 70.4%→64.7%・未達 6.9%→12.1%。★ **酒場 79.2%→43.2%**（4行）、結婚式 49.2%→20.4%（8行）、廃屋 49.2%→31.6%。一方で夕市 85.8%→93.0%、道標 91.0%→92.0% と**上がる依頼もある**（段差で1件ぶん許容をもらうため）
- ★ **語彙を救うだけなら工程数は要らない**：`steps[phase-1]` の対応付けを直せば32行は全部生きる。実害は **18依頼中12依頼で「到達不能行にしか出ない行為の語」がある**こと（EX-075 で付けた〈留める〉は9箇所中5箇所が到達不能＝薬草・道標・教会では一度も効かない）
- **A（推奨）**：対応付けだけ直す（案1・均等間引き）。工程数も疲労も触らない
- **B**：疲労の積み方を直して工程数を語彙の行数に合わせる。★ **キャップ（32以上）なら現行は恒等にできる**（現行で到達しうる疲労の最大は 1人4工程で 31.5）。総疲労の正規化だと現行の分布も動く
- **却下済み**：2026-08-06「工程の負荷を調整して未達がほぼ出ないようにする」（却下。理由は「保証にならず例外がデータから読めない」で B は保証機構ではないので当たらない。★ 同じ決定の**初回強制完遂は実装未着手**＝酒場の 43.2% は今日の初回プレイでそのまま起きる）／★ 2026-08-01「生活・記録の未達12〜14%は育っていないことの表示。触らない」——**B は誰の育成値も動かさずにこの帯を動かすので触れる**（EX-077 で純比率を却下した理由①と同じものが B にも当たる）／2026-08-06「成長側が `quest.fieldworkSteps` を直読みする＝対応表が2つに割れる」——対応付けの案3はこれと同型

## 滞りの閾値を比率化する設計（EX-077・2026-09-11 → 裁定A で実装済み・commit b0164af）

★ **設計案のみ。コードもデータも変更していない**（指示どおり）。日時：2026-09-11 13:09 JST／HEAD `bf469ed`（origin と一致・0/0）。
★ EX-076 は**裁定C（A も B も採らない。本当の問題は閾値が絶対件数であること）**で決着し、このタスクが引き継いだ。

- **現行の 1/2 件を比率に直すと**：未達（>=2件）は 2工程=1.00 / 3工程=0.67 / 4工程=0.50、部分（>=1件）は 0.50 / 0.34 / 0.25。★ **工程数につれて下がる**＝一定の比率では表せない
- **分布を保てる純粋な比率は存在しない**（構造で言える。比率は定数なのに、再現したい値が工程数で動く）。丸め3種（ceil / floor+1 / round）を 0.001 刻みで総当たりしても、部分=1件を満たす比率はあるが**未達=2件を 2・3・4工程すべてで満たす比率は無い**
- ★ **ただし不可能なのは2工程だけ**。3工程以上では `ceil(0.5×工程数)` が既に現行と一致する
- **A（推奨）**：未達＝**滞り2件以上 かつ 半数以上**（`max(2, ceil(0.5×工程数))`）／部分＝1件以上（`ceil(0.25×工程数)`。★ この式は全工程数で1以上なので下限は一度も効かない）。**現行の全依頼で恒等**（工程数は最大4、`tier` は乱数を消費しない位置）。5工程から現行と分かれる
- **B**：純粋な比率（`ceil(0.5×n)` / `ceil(0.25×n)`）。A と違うのは2工程だけで、1件滞れば未達
- **端数**：ceil（切り上げ）。2〜8工程の閾値は 1/2・1/2・1/2・2/3・2/3・2/4・2/4
- **B のコスト**（基準4人・支給品なし・昼/晴れ・N=500/依頼）：酒場の未達 **0.6%→20.8%**・部分 **20.2%→0%**（2工程固定なので「部分」という結末が消える）。手紙 14.2%→25.4%、廃屋・結婚式 12.4%→23.6%。★ **結末だけでなく文面も動く**——未達の文面を持たない13依頼は `fieldworkOutcome` の引き返し分岐が `pickOne` を消費しないため乱数列がずれる。成長倍率も 0.85→0.7
- **却下済み**：直接の再提案なし。隣接3件——2026-08-06「工程の負荷を調整して未達がほぼ出ないようにする」（却下）／2026-07-31「結末サマリ型のログ」（却下）／★ 2026-08-01「**生活・記録の未達12〜14%は難度設定ではなく育っていないことの表示。触らない**」（裁定）。B は誰の育成値も動かさずにこの帯を倍にするので、3件目の読み方に触れる

## 解決済み：語彙の工程行と工程エンジンの対応付け（EX-076・2026-09-11 → 裁定C で EX-077 へ引き継ぎ）

★ **調査のみ実施。コードもデータも変更していない**（指示どおり）。日時：2026-09-11 12:52 JST／HEAD `710d413`（origin と一致・0/0）。

- **論点**：語彙（`data-vocab.js` の `questSteps`）は `steps[phase-1]` で**先頭からN行しか読まれない**。上限4（`phasesMax`）で **102行中32行が到達不能**、さらに工程数は乱数2〜4なので **row3＝約2/3・row4＝約1/3** と末尾ほど薄い（薬草採集の唯一の〈採る〉は約1/3の回で回らない）。直すべきは**数（4）**か、**対応付け（phase N ↔ 行 N）**か
- **A（推奨）**：今は直さない。事実を `docs/CURRENT_SPEC.md` に確定し、検査スクリプト（到達可能行／到達不能行／到達不能行にしか出ない語）を足して閉じる
- **B**：対応付けを直す。工程数は据え置きで「どの行を回すか」を選ぶ（回す数ぶんを語彙行から選ぶ／語彙行に必須の印を付ける／写像を依頼データに持たせる）
- **トレードオフ**：A＝挙動ゼロ・不可逆ゼロ・不一致0だが末尾の薄さは残る／B＝書いた行が生き、**工程数を触らないので完遂率・未達率は動かない**が、乱数列が動くので不一致0は満たさない
- **工程数を触る2案は先送りを提案**：「到達不能32行を削る」は不可逆で EX-074 の第二段・第三段が未着手／「語彙に合わせて全部回す」は完遂 72.1%→38.6%・未達 7.9%→32.7%（結婚式は完遂5%）。原因は `setbacksForPartial`／`setbacksForFail` が**絶対件数（1／2）**で工程数と難度が構造的に結合していること。先に**閾値の比率化**が要る＝単独・直列のタスク
- **却下済み**：直接の再提案なし。隣接2件——2026-08-06「工程の負荷を調整して未達がほぼ出ないようにする」（却下）／2026-07-31「結末サマリ型のログ」（却下）
- ★ **4という数に裁定の記録は無い**（DECISION_LOG 2026-07-31 は「工程ログ型」を決めただけで、回数の理由は書かれていない）。導入は `bb5e78d`（EX-042）

## 解決済みの判断待ち（2026-09-11・probe の信頼性／PR 運用）

★ **2026-09-11 に裁定どおり実施して解決済み**（EX-073。commit は下記）。記録として残す。

- **probe の信頼性 → 裁定A（fetch に一本化）**：★ **old＝new の probe は存在しない文字列でも成功を返す**＝照合していない。当たり確認は `notion-fetch` の読み直しだけにし、**`notion-search` も使わない**（書き込み直後は索引が古い）。★ **old≠new の「No matches」は信用できるので、「消えたことの確認」にだけ使ってよい**。**B（往復置換）は却下**——確認のために正本へ無駄な書き込みを残し、途中で失敗すると壊れる。**過去分の洗い直しは不要**（08-19 に fetch で読み直し済み）
- **PR 運用 → 裁定A（マージして main を追いつかせる）**：PR #1 を main へマージし（マージコミット `53ebb85`）、**main が現HEADを含むことを機械確認**した。session-end に **④-b**（push 後に PR をマージし、main が HEAD を含むことを確認）を新設。**以後も `claude/…` ブランチ運用**。**B（main 直押し）は却下**——Web UI から push 先を選べず、既存セッションを捨てて作り直すことになる
- ★ **⓪ の早送りは症状への手当てで、④-b の代わりにはならない**（⓪＝古い木で立ち上がったときに直す／④-b＝古い木を作らせない）


## 解決済みの判断待ち（2026-08-18・EX-057 の追加作業）

★ **2026-08-18 に裁定どおり実施して解決済み**（commit `1c6b689` → `94b56d2` → `26fa909`）。記録として残す。

- 停止理由は2つだった：(1)「効いた瞬間」が依頼単位の担い手しか読まず **per-step の `humanOnly` がどこにも効かなかった**／(2) 宣言を足すと**工程数が固定されて不一致0を満たせなかった**
- 裁定は **A→B を両方**（切り離し → 工程単位化）。**依頼単位で分ける案は却下**（exploration の文面が犬に使えない問題が残るため）
- **迷い3件の裁定**：道標＝`humanOnly`（文字を確認する行為）／**薬草納品＝`humanOnly` にしない**（においで薬草を見分けるのは犬の領分＝エルシーの見せ場）／夕市＝`humanOnly`。★ **夕市に「はぐれた子を探す」類の工程は現行に存在しない**ので、犬に開ける工程は無しと確認した

## 解決済みの判断待ち（2026-07-30・撤退の作り直しで残った歪み2件）
★ **2026-07-31 に裁定どおり実施して解決済み**（commit `2525a83` → `67bf12b`）。記録として残す。

タスクページ `3ada8a5d-fd45-815d-81dc-fc8b08edd648`。**4段階の実装自体は完了・検証済み（commit `291752b` / `a993005`）。**

### 歪み1：煙幕が段階4で強すぎる
- 3人＋エルシーの交戦後撤退が **1.3% → 35.1%**（煙幕を持たせただけ）。**勝率98%の編成の3割を離脱に変えている**
- 原因は構造：**段階4は毎ラウンド回るので +30 が乗ると機会が積み上がる**（旧実装では煙幕は発火0件の定期判断にしか乗っていなかった＝実質死んでいた）
- ★ 関連して**自分で決めた1点**：**段階1では煙幕とエルシーの補正を数えないことにした**（数えると3人＋エルシー＋煙幕で回避100%＝「依頼に行かないゲーム」になったため）。設計判断なので覆してよい

### 歪み2：勝ち目のない編成が段階1を通り抜ける
- ミナ＋ガッドは比 1.21（やや不利）で段階1を通るが、**実際の勝率は 1.6〜16%・戦闘不能 69〜81%**
- 原因は**成立条件**：2人編成では過半数＝2票なので、**閾値90の豪胆な戦士（ガッド）が単独で押し切れる**。キャラとしては正しいが、プレイヤーから見ると止められない
- → **少人数のときの成立条件（何票で成立するか）か、閾値そのものの見直し**が要る

### あわせて報告済み（裁定待ち）
- **基準4人の重（撤退）が 2.4% → 0%** になり、撤退・煙幕・エルシーの分岐は「勝てない編成」でだけ起きるようになった。旧実装のドラマは 78〜84% が誤作動だったので**これが正しい姿**だが、基準編成でドラマを戻すには**中盤帯の敵**が要る（発動条件つきで固定済み）
- **野盗と大熊の接敵スコアの差は1点だけ**（−18 対 −17）。大熊は総ダメージ量を野盗と揃えた敵なので見立てがほぼ同じになるのは正しいが、**敵の「型」を見分けたいなら総量ではなく「誰が何ラウンドで落ちるか」を見る式が要る**（未対応）
- **タブのバグ**（2つ開くと遠征が消える／同じ冒険者を二重派遣）＝**別タスクに切る・優先度高めと裁定済み**。修正方式（他タブの書き込みを検知して読み直す／後から開いた側を読み専用にする）は A/B で聞くこと

## 解決済みの判断待ち（2026-07-29・クリティカル実装後の軽の帯）
★ **2026-07-30 に裁定Aで解決済み**（`lightWoundRatio` 0.26。軽 24.8%）。記録として残す。

タスクページ `3aca8a5d-fd45-813e-a159-d340fc676156`。**クリティカルは実装済み（commit da24e4e）。軽の帯だけ裁定待ちで停止。**

- 判定基準4つのうち **深手（0%→53.2%）・分岐（bail/煙幕286件/エルシー1041件）・分散（4人に散った）・即死なし（12000戦闘で戦闘不能0件）は満たした**
- ★ **軽だけが 30.7% → 19.4% に動いた**（20-30帯の下限割れ）ので停止
- ただし基準が挙げた停止理由「第3段階の回復モデルの前提が崩れる」は**成り立っていない**。設計点は「拒否は個人の14.5%」で、現在の重症は個人 8.2%。崩れたのは**結末の物語の方**
- ★ 原因は総ダメージの増加ではない（平均累積被ダメ率は発生率を振っても 0.274〜0.277 で一定）。**深手ガードの性質が変わったこと**＝以前は深手と累積が同じ戦闘を弾いていた（重複）が、今は無相関なので2条件が独立に軽を二重に削る
- 裁定待ち＝**A（`lightWoundRatio` 0.25→0.26。軽24.8%・分岐を一つも失わない。推奨。ただし 07-29 に一度却下された案）**／B（発生率5%→3%。軽24.9%だが深手が53%→36%に薄まる）／C（軽の判定から累積条件を外す。軽が49%に跳ねる）／D（軽 20-30帯という目標自体を見直す）
- ★ 別件で見つかった（今回は直さない）：**小さいダメージのクリティカルが読み物として不自然**。「致命の一撃を受けた！（6ダメージ！）」が出て、かすり傷で重症60分になる

## 次にやること
※ ★★ **DECISION_LOG の写しに3件を同期する**（**2026-08-08 に完了**）。**初回強制完遂／エルシーの疲労値／性格の統合（`personality` 廃止）**。★ **Notion の DECISION_LOG（正本 `3a9a8a5dfd4581e3a8cfc3872a300766`）に全文があるので、そこから写した**
※ ~~★ EX-063：判定用語彙に行為を2語足す~~ → **2026-08-18 に完了**（13語。commit `d4d37a8`）
1. ★ **判定用語彙を判定に接続する**（EX-061 から続く本丸。**同一シードの不一致0を保てる形を設計してから**着手する）。現状 `data-vocab.js` を読んでいるコードは無い。★ 接続すると `tags`・工程・支給品・冒険者が初めて噛み合う
※ ~~★ `tags` を判定に使うかを決める~~ → **2026-08-06 に決着**（表示用のまま据え置き、判定用語彙を新規定義）
2. ★ **EX-049 の調査結果から、1件あたりの所要時間を決める**（帯ごと）。★ **指標は総時間ではなく1件あたり**で、総時間は成り行きに任せると確定済み。決めたら `data-time.js` の `masterDurationBands` を触るだけで全依頼に効く。判断材料は EX-049 の「調べた結果 7」節（`3afa8a5d-fd45-810a-8c4e-f920e97189e5`）
3. ★ **体験版の範囲を決める作業の続き**（EX-049）。未決は 6枠の内訳と選択規則／体験版に入れるジャンル／古参3依頼の専用フロー化／エコロジーログ／クールタイムの具体値（**受付嬢の名前は 08-03 に確定**）。★ **畑→納屋の断層（回復待ち 4.2%→69.0% の16倍）もここで決まる**。納屋→野盗は1.3倍しかないので、**納屋が重いのではなく畑が軽い側**
4. ★ **デザイン方針を `docs/DECISION_LOG.md` から集める**（**新規に詰めるのではなく収集**）。frontend-design スキルは**ブリーフの言葉が常に勝つ**と定めているので、渡すブリーフが無いとスキルが自分の美意識で走る。**導入より先に、既に決まっているビジュアル方針が何か**を洗い出す
5. **包帯の判断への重み（−20）を下げるかどうか**（上の判断待ち。裁定が要る）
6. **体験版②-2（時間の見せ方）**：昼夜の切り替え・帰還までの残り時間/現在の日付の表示・`departConditions.timeOfDay` の時計連動。あわせて `daysPassed` の表示先を決める（②では内部値の加算まで）
7. ★中盤帯の敵の実データ化（HP480/threat54〜56帯＝スライス7ガイド）。**成長の実測で混合90回時点に野盗が無風化（軽100%・2.2T）＝緊急度高**。★**着手前に下記のエルネ問題を解決すること**。★撤退の作り直しで**基準編成のドラマがここに依存するようになった**（勝てる編成は正しく引かないため）
8. **包帯の層分け**（保留中）。★ **着手する前に必ず測り直す**＝旧勾配（包帯0=19.3%／1=8.2%／2=5.8%）は**いつのものか不明**に落としてある
9. ~~隊商護衛の報告書を直す~~ → **2026-08-04 に EX-052 で完了**（嘘 0件・被弾行の重複 −92%・斥候の不揃いを解消）。★ **残るのは攻撃行の重複 63.75% だが、これは構造的なもの**（毎ラウンド出るうえ隊商護衛が最長の戦闘）で、完了条件から外した
10. ★ **Unity 移行前にシミュ/描画分離をやる**（EX-032 が抱えたまま進行中）。`app.js:73` が `document.getElementById` を呼ぶので Node 単体では読み込めず、測定はすべて Playwright 経由。**あわせて `scripts/` に残るハーネス**（現状6本はすべて DevTools 貼り付け前提）**と CLAUDE.md への実行時間の追記**も EX-032 の未達項目
11. 新着の印：新しい依頼が現れてもカードが増えるだけで気づきにくい（②-3で見送り）
12. 総点検フェーズ2C（未発注）: 掴み体験仕様ページの個別監査ほか監査残課題
13. 演出の続き: 大ヒットの文字サイズ強調・揺れ等（スライス4は色分けまで実施済み）
14. エンジンの「敵HP0のラウンドでも反撃が実行される」挙動の見直し（判定結果が変わるためバランス調整として別タスク。スライス3.5では描画層のみ修正）
15. ~~Drive ざらい 第3束以降~~ → **2026-08-04 に完了**（4文書すべて読了・台帳に分類）。★ **残っているのは「引き上げ候補（台帳 B）の統合をどこまでやるか」の裁定**で、特に **B-2 反ミーム終盤設計は超常怪異3体構造に合わせた改稿が必須**、**B-6 ダムナティオ・メモリアエ装置は生活依頼→終盤導線の背骨**
16. 小ネタ: 報告書一覧の「交戦記録 添付あり」バッジ（最小差分）／ADV風テキスト送り（別タスク）

## 未確定項目

### ★ 2026-08-03 に確定したこと（体験版の位置づけとオープニング）
- ★ **受付嬢の名前は「アルメナ」**（由来はローマの出産・記録の女神カルメンタ。一次案のカルミナから音を変えた）。★ **記録の女神という由来が、プレイヤー＝ギルドの記録係という中心とそのまま重なる**
- **あだ名は段階を分ける**：冒険者のあだ名変更を先に通す → そのあとアルメナ側のイベント → **彼女から言い出す** → **断れる** → **過去ログは当時の呼び方のまま残す**。★ **未同定対象の命名と同じ仕組みなので実装コストはほぼゼロ**
- ★ **体験版は身内向けでアルファ未満。公開配布ではない**
- **将来の配信先は Steam か DLsite**（DLsite の Steam 配信支援を使う方向）
- **仮イラストは NovelAI で代替してよい。** ただし **DLsite の AI フラグには露出ペナルティ**があるので、**配信時は外部コミッションへ差し替える前提**で置く
- **体験版に入れる絵は受付嬢とギルド内の2枚程度**
- ★ **指標は総時間ではなく1件あたり**（08-01 の確定を再確認。総時間は成り行きに任せる）

### ★ Drive から出てきた設計（2026-08-03・未実装だが却下ではない）
出典：ゲームUI段階的解放設計／遠征ゲームの依頼分類設計。**どれも現行実装は持っていない。**

- ★ **UI そのものが段階的に解放される（5フェーズ）。** 救助画面は**初の行方不明**で、墓地は**初の死亡**で解放。**隠すことで喪失が重くなる**という設計。★ **体験版で行方不明・死亡が起きるかが解放条件に直結する**ので、体験版の範囲と一緒に決める必要がある
- ★ **最初の依頼は10〜30秒**（根拠は A Dark Room の開発者コメンタリー）。現行の最短は廃屋の**実1分**なので、方向は合っているが**まだ長い**
- ★★ **`tags` が設計の核だった。** 環境・要求スキル・社会的文脈・対象物の性質で分類し、**冒険者の属性と支給品がタグに反応する**。現行実装の `tags` は**掲示板の表示専用で死蔵**（CURRENT_SPEC に明記した）
- **支給品は「ナラティブな鍵」。** 例：`#暗闇` の依頼で **なし→ペナルティ／ランタン→標準／発光苔の瓶→隠し要素発見**の三段階
- **JSON データモデル案に `logs` フィールドがある。** 2026-08-01 の結末データ化（EX-047）は**これに追いついた形**
- **8大分類 対 現行9ジャンル**。差は**「護衛」だけ**（文書では保全に含まれる）
- **「欠点」という概念**：前職＝できること／性格＝やり方／**欠点＝予期せぬ失敗**。実装では `traits[].type === "flaw"` が該当しそうだが、**実装状況は未確認**

### ★ 2026-08-03（夕）に見直しが必要になったもの
- ~~支給品の効き方は `category` から導く~~ → ★ **元設計は `tags`。** 対応表を増やさない利点は両方にあるが、**表現力は `tags` が上**（`#暗闇` のような横断的な条件が category では書けない）。**どちらを採るかは未決**で、支給品の設計より前に決める

### ★ 2026-08-01 に確定した一般則（今後の判断の土台。覆すときは理由を書く）
- ★ **「Notion が正本」は不正確。向きは「誰が書くか」で決まる。** 人やチャットが決めて書くもの＝**Notion**／Claude Code が生成するもの＝**repo**／実装から導出できる事実＝**コード**（正本は Notion `02_operations/ドキュメント運用規約`）
- **見出しは原則を要約する。要約が原則とずれると、見出しだけを読んだ人が間違える**（「Notion が正本」がまさにそれで、1日ぶんの反映漏れを生んだ）
- **条件式を JSON に持ち込むと、JSON がコードになる。** データに出せるのは名札と条件名の列挙まで
- **番号は呼ぶためのもの、タイトルは中身を知るためのもの。両方要る**（EX 番号制）
- **手で書いた日付は、書いた時点からずれ始める**（だから生成物の宣言はスクリプトが出す）

### ★ 2026-08-01 に訂正されたもの（古い記述を根拠にしないこと）
- ~~モックは捨てる前提の検証品~~ → **誤り。モックをベースに Unity で作り直し、移行できるものはそのまま使う。** 全企画共通の前提だが、どこにも書かれていなかった（`ARCHITECTURE_PRINCIPLES` を訂正済み・チャット側が記入）
- ~~結末のハードコード「45箇所」~~ → **47行の数え間違い**（タスク本文は書き換えていない。訂正の記録だけ残す）
- ~~支給品の判定場所は支給品ごとに1箇所~~ → **「支給品 × 経路」で見ないと正しくない**（同じ包帯でも戦闘と工程で別の場所が見ている）
- ~~古参3依頼（薬草採集・道標・手紙）は初期公開4件に含まれている可能性が高い~~ → **含まれていない**（初期公開は 結婚式・廃屋・夕市・夜道）。ただし**深さ1〜2にいるので2〜3件目には当たる**。あわせて**専用ハイライトを持たないのは3件ではなく9件**（EX-049 で実測）
- ~~初期公開の近場に長い帯が割り当てられている可能性~~ → **ない**。初期公開4件は全部「近」（1/10/10/10分）で、伸びる形は既に実現されている（EX-049 で実測）

### ★ 2026-08-01 に確定した体験版の範囲（EX-049。決まっている分だけ）
- 大枠は**オープニング → 隊商護衛のクリア**＋9ジャンル一通り
- 掲示板は**最大6枠**。ツリー状に解禁し、解禁可能なものだけ表示（**現在は全件リストで未実装**）
- **クールタイムは消化数1〜3。** 時間が進むのは依頼中と回復中だけなので、**放置では進まず、他ジャンルをこなすのが必然になる**
- **時間帯は4値のまま。** 夕方＝祈りの時間で、天使様（IGPJ_02）と世界観が接続しているため
- **冒険者のランダム生成は体験版に入れない。** 師弟システムと引退も自動的に範囲外
- ★ **指標は総時間ではなく1件あたり。** 総時間は成り行きに任せる

### ★ 2026-07-31 に確定した一般則（今後の判断の土台。覆すときは理由を書く）
- **判断に入るのは「勝てるか」に関わるものだけ。「逃げやすいか」は撤退の成否にだけ効く**
- **対策アイテムは勝敗の見込みに、回復アイテムは粘れる時間に効く**（包帯の層分けはこの線で見る。実施は保留）
- **性質の違う2つを同じ入れ物に入れると、片方の規約がもう片方に伝染する**
- **同じ概念には同じ値。ただし当てる単位は概念ごとに決める**（クリティカルは1発あたり、負傷は累積）
- **値は定義から導く。目標値から逆算しない**（逆算した値は、目標値の根拠が崩れた瞬間に存在理由ごと消える）
- **5% にも 255 にも意味はない。経験的に決めた値だと文書に明記する**
- **測定結果には日付・時刻・HEAD ハッシュを併記する**（正本は `02_operations`。遡及はしない＝ハッシュのない既存値は「いつのものか不明」として扱い、根拠にする前に測り直す）

### ★ 2026-07-31 に訂正されたもの（古い値を根拠にしないこと）
- ~~深手 53%~~ → **18〜19% が本来の値**。旧53%の 64.9% は後衛のかすり傷を深手に数えていた分
- ~~軽 20〜30帯／深手 50〜70帯~~ → **帯ごと取り下げ**（根拠が汚染された値だった）
- ~~納屋は野盗と同格~~ → **同格ではない**（回復待ち 69.0% 対 91.3%）。threat だけ見た推測誤り
- ~~包帯勾配（重症率）19.3/8.2/5.8~~ → **いつのものか不明**。層分けに着手するときは先に測り直す

### 保留・未着手
- ~~★ 受付嬢の名前が未定~~ → **2026-08-03 に「アルメナ」で確定**（オープニングの前提が外れた）。**実装は未着手**で、あだ名の段階（冒険者のあだ名変更 → アルメナから言い出す → 断れる → 過去ログは当時の呼び方のまま）も未実装
- ★ **frontend-design スキルはモック段階で暴発しうる**（2026-08-03。導入タスク `3b1a8a5dfd4581638cd6e06226192456`）。**掲示板6枠・オープニング・報告書のADV表示が控えており、発火する場面がすぐ来る**。`CLAUDE.md` の適用範囲の節（モック段階には適用しない）を**導入と同時に必ず入れる**こと
- ★ **遠征の確定したビジュアル方針が DECISION_LOG にあるか未確認**（2026-08-03）。**ブリーフがなければスキルは自分の美意識で走る**。スキル自体が「ブリーフの言葉が常に勝つ」と定めているので、**渡す言葉を先に集めるのが導入の前提**
- ★ **「表示層は捨てる」の線引きが要る**（2026-08-03）。額面どおり適用すると、**2026-07-30 に確定した報告書の4パターンと表示順序まで捨てる**ことになりかねない。捨てるのは**描画の実装**であって**何をどの順で書くかの決定**ではない、という線を引く必要がある
- ★ **身内向けアルファが「配布ビルド」に当たるか未確定**（2026-08-03）。当たるなら `schemaVersion` の個別移行関数の発動条件を満たす。**配る前に必ず決める**（CURRENT_SPEC のデータ保存節にも注記済み）
- ★ **体験版で行方不明・死亡が起きるかが未決**（2026-08-03）。**UI の段階的解放の条件に直結する**（救助画面＝初の行方不明／墓地＝初の死亡）。現行はロストの発火経路が捜索チェーンにしかなく、体験版の範囲と一緒に決める必要がある
- ★ **`tags` を判定に使うかが未決**（2026-08-03）。**支給品の設計を左右する**。現行は表示専用の死蔵で、CURRENT_SPEC に明記済み
- ★ **`recommended` を復活させるか**（2026-08-03）。全17件が `tags` を持つので現状は到達不能。`tags` の扱いを決めるときに一緒に見る
- ★ **`observationTarget` を全件に入れるか**（2026-08-03。未確認・未着手）
- ★ **「欠点」の実装状況が未確認**（2026-08-03）。設計上は 前職＝できること／性格＝やり方／**欠点＝予期せぬ失敗**。実装では `traits[].type === "flaw"` が該当しそうだが、**実際に失敗を起こしているかは測っていない**
- ★ **6枠の内訳と選択規則が未決**（解禁済みが6件を超えたとき何で選ぶか）。現在は上限なしの全件リストで、解禁済みが減らないため枚数が増え続ける（最大15件）
- ★ **古参3依頼（薬草採集・道標・手紙）が汎用フローのまま**。初期公開ではないが**深さ1〜2で2〜3件目には当たる**。★ 実際は**専用ハイライトを持たない依頼は9件**（結婚式・廃屋・教会巡回・隊商3件も含む）なので、直すなら3件では足りない
- ★ **「夕方＝祈りの時間」の世界観接続が `00_shared` に書かれているか未確認**（2026-08-01）。**遠征のコードを読んでも分からない**ので、書かれていなければ次に誰かが「夕方は要らない」と判断する
- ★ **「夕方」の表記ゆれ**（掲示板のモック用ボタンは `夕方`、報告書の生成側は `夕`）。**時間帯でゲートを掛けるなら先に揃える**（2026-08-01・EX-049）
- ★ **`recommended` は到達不能**（掲示板カードは `tags` を表示し、`tags` が無いときだけ落ちるが全17件が持つ）。**依頼カードに「解放条件」も「残りクールタイム」も出す場所がない**（現在は 分類／危険度／地域／所要時間／観察対象 の5項目）
- ★ **危険度と所要時間が連動していない**（低危険の教会巡回120分 対 中危険の納屋60分）。所要時間は距離で決めている形だが、**地域と帯の対応表がどこにもない**ので新しい依頼を足すときの拠り所がない（2026-08-01・EX-049）
- ★ **支給品の状態を書けるのは包帯だけ**（2026-07-31）。笛とランタンは「使った」を判定できる場所がなく（ログ文の抽選で決まる）、煙幕は畑・納屋では持たせられない
- ★ **工程経路の危険度表示がずれている**（2026-07-31）。「最後の手がかり」は**高危険なのに重症0%**。工程経路は仕様上、軽症までしか出ない。表示を「負傷の重さ」として読ませるなら中・高を付けられない
- ★ **dangerLine の出口／タブのバグ**（2つ開くと遠征が消える／同じ冒険者を二重派遣）＝ **Unity 移行で解決見込みのため保留**
- ★ **貼り付けブロックを数回まわして粒度を確認する。** 足りなければマスター（`02_operations/ドキュメント運用規約`）を改訂する。**改訂はマスターが先、写しは後**
- ~~★ session-end スキルへ貼り付けブロックを反映する~~ → **2026-08-01 に完了**。写しをマスターと突き合わせ、未配布だった 07-29 の改訂（停止7項目・完了5項目・SESSION_STATE の向きは企画ごと・Notion が正本の企画では全置換禁止）と 08-01 の CURRENT_SPEC（③-b / ⑤-c）を反映した
- ★ **天使様・2on2 への配布**：パス修正（`00_shared`→`02_operations`）＋ session-end の改訂 ＋ 貼り付けブロックを**1セッションでまとめて**やる。配布時に運用規約の共通文面（SESSION_STATE も Notion へ反映、の行）も直すこと
- ★ **新規企画用テンプレ一式の作成**（`02_operations` にタスクあり）
- ~~監査 節3の実害（軽の結末0%／煙幕が実質アンチアイテム）~~ → 2026-07-29 に**測り直して解消**（軽は26%あった＝回帰は現存しない／煙幕は完全成功率をほぼ動かさず仕様どおり）。監査ページ節3に失効注記済み。**残る論点は「包帯と煙幕で2択の価値が非対称」だけ**（下記）
- ★ **包帯と煙幕の2択の価値が非対称**（包帯は完全成功を72.6%→93.7%に上げるが、煙幕は72.4%とほぼ動かさない）。支給品の段階開放を実装するときに一緒に見る
- ★ **監査結果にも賞味期限がある**（07-29 の一般則）。監査・棚卸しの実測値を根拠に実装を動かすときは、**先に現HEADで確かめる**。棚卸しページには失効注記済み、監査ページ節3も07-29に注記済み
- ~~非戦闘依頼16件の経路移行が未着手~~ → 2026-07-31 に**完了**（`simulateFieldwork`。`quest_barn_bite` / `quest_field_mystery` の `enemyId` の食い違いも同時に解消）。**夜道の灯りだけ据え置き**（時間帯の扱いが後回しのため）
- ~~負傷するのは常に前衛1人だけ~~ → 2026-07-29 に**原因を特定して H1-a を投入・分散した**（上の進捗を参照）。残る論点は下の2つ
- ~~深手が現在 0%（H1-a の副作用）~~ → 2026-07-29 に**クリティカルを実装して解消**（深手 53.2%・重症・撤退・煙幕・エルシー・包帯の価値がすべて戻った）。残るのは軽の帯だけで、上の「判断待ち」へ
- ★ **中盤帯の敵（threat 50以上）を入れる前に、役職ごとの前衛シェアか薬草師の前衛順位を解決する**（2026-07-29 に発動条件として固定。`schemaVersion` の移行関数と同じ扱い）。**エルネの maxHp 79 が前衛シェアに対して小さく、threat 56 ではクリティカルなしでも前衛2ラウンドで 78/79 に届く**。H1-a で全員が前衛に回るようになって表に出た問題で、クリティカルの上限をやめたので隠れずに残っている
- ★ **撤退と捜索チェーンの未確定2件**（アイデアメモ `3aca8a5dfd45818fac80dc588b9f3664`）：**全滅時の帰結**（ゲームオーバーか受付嬢の出撃か。このゲームで初めての「終わり」を持つかどうかの判断）と、**外部機関への依頼**（金で買えると自滅回収が最適になるため保留。金ではなく取り返しのつかないものを払わせる方向）。実装はクリティカルの後
- ★ **しきい値では解けないことは掃引で確認済み**（深手発生 58.1%→0% と分散が単調に逆相関し、中間に両立点がない）。**今後「交代のしきい値を調整しよう」という案が出たら、この掃引を根拠に却下する**
- ★ **後衛の頭割り（H2-b）は未着手のまま保留**。4人編成が後衛を安全地帯に入れる構造は残っている
- ★ **配布ビルドを作る前に、`schemaVersion` の個別移行関数を必ず入れる**（2026-07-29 に発動条件として固定。今は不一致なら初期化のまま）
- ~~監査 節2：却下案の記録漏れ4件の遡及記録が未対応~~ → ★ **2026-08-04 の棚卸し（EX-051）で、実際には 2026-07-25 に4件とも記録済みと判明**（DECISION_LOG「却下案の遡及記録（総点検フェーズ2B・記録欠落の補完4件）」）。**この保留自体が古かった**
- ★ **Unity 移行前にシミュ/描画分離をやる**（2026-08-04 の棚卸しで判明）。測定はすべて Playwright 経由で、`app.js:73` が `document.getElementById` を呼ぶため **Node 単体では読み込めない**。`ARCHITECTURE_PRINCIPLES` の「シミュ/描画分離＝〇」は**誤り**（決定性は〇）。EX-032 は閉じずにこの発見を抱えたまま進行中
- CURRENT_SPEC「遠征の所要時間」表の**「該当依頼」列**が同型の腐る表として残っている（07-27は横展開せず列挙のみ）。生成スクリプト側に所要時間帯の列があるので実質は足りている
- ~~Notion CURRENT_SPEC の依頼一覧表が13件時点のまま~~ → 2026-07-27 に解決（更新ではなく**表ごと削除**して生成スクリプトに置き換え）
- ~~`QUEST_DURATION_BANDS` を data 側へ移設する~~ → 2026-07-28 に解決（`data-time.js` を新設。抜き出し方式も廃止）
- ★ **app.js に残っている同型の定数**（07-28 に列挙のみ・移設は未着手）：`PLAYER_PERSONALITIES`（83行・気質マスタ）／`GROWTH_*` 一式（204〜253行・成長の対応表とチューニング値）／`BATTLE_TUNING`（6030行）／`questEventPools`・`lifeQuestEventPools`（2228・2251行・ログ文言のプール）／`ROSTER_STAT_LABELS`・`TENDENCY_STAT_LABELS`（1352・1368行・表示ラベル）
- ~~このセッションの記録を書き戻す Notion ページID が未確定~~ → 2026-07-26 に解決（`CLAUDE.md` の「Notion ページID」節に登録済み）
- ★部分成功の報酬・信頼値は完全成功より明確に低くする（報酬システム実装時の必須条件。弱編成+煙幕の効率もその時に再検証＝フェーズ2A裁定）
- 包帯の入手性の確認（包帯0=成功72%の設計は「包帯が十分入手できる」前提。希少なら見直し＝スライス9裁定の依存条件）
- ~~ロストの発火経路が実質消えた~~ → 2026-07-29 の棚卸しで**生きていることを確認**（斥候なし・エルシーなしの最後の手がかりで lost 300/300）。将来の広げ方は撤退と捜索チェーンのアイデアメモへ移した
- ~~包帯で無傷勝利70%化~~ → スライス6で解消（stage=累積被ダメ基準）
- ~~「！」閾値の手触り~~ → スライス6でスケール非依存化（与=期待値×1.08／被=maxHp12%）
- 将来の敵データ設計時：中盤・終盤帯の敵は HP だけでなく threat も味方HP帯に合わせて上げる（合成敵シミュで軽勝利に寄った）
- ~~納屋（barn_biter）の stage 分布は simulateBattle 移行時に要調整~~ → 2026-07-31 に移行済み。実測は 軽84.1%／中16.0%（`lightWoundRatio` 0.30・基準4人）で、軽100%にはなっていない
- 4人編成だと交戦行が長め（1:1方針で確定済み）。長さが気になれば演出スライスで緩急を付ける
- 敗北（全員戦闘不能）経路のエルシー囮ビートの扱い
- `debugBattleSim` の一時イベント出力を残すか整理するか
